# SP-3.1 → SP-4 Handoff

**Date:** 2026-05-26
**Purpose:** Hand a fresh Claude Code session the state needed to brainstorm + build **SP-4 (Weekly Research Uplift)** without re-deriving the SP-2/SP-3/SP-3.1 work. SP-4 depends on SP-2 (universe) + SP-3 (asset-class rails) + SP-3.1 (crypto) — **all three are now COMPLETE and LIVE.**

---

## 1. Where the program stands

| Program | Scope | Status |
|---|---|---|
| SP-1 | Provider cutover (Alpaca/FMP/EDGAR; AV purged) | ✅ shipped |
| SP-2 | Universe expansion (predicate-per-strategy, 5y backfill, universe-recs, predicate-at-mint) | ✅ A→D shipped + live |
| SP-3 | Asset-class expansion MVP (`instrument_class` enum: equity/option/etp routed; crypto/futures reserved) | ✅ shipped + live (gate-OFF inert MVP) |
| SP-3.1 | Crypto + 24/7 (A=exec lane, B=regime detector, C=redeploy, D=ref strategy + activation) | ✅ A→D shipped + **LIVE** |
| **SP-4** | **Weekly Research Uplift — teach the Saturday research stack the broader universe + new asset classes** | **NOT STARTED — brainstorm next** |

## 2. What went live today (SP-3.1) — the crypto stack

- **Live strategy:** `S_btc_momentum` (`src/strategies/implementations/S_btc_momentum.py`, `instrument_class='crypto'`) — single-asset BTC-USD 90d momentum + absolute-momentum cash gate + 20% vol-target. Backtest Sharpe **1.02** / MaxDD **65%** / +1369% (10y). Promoted candidate→live.
- **Three gates ON** in prod `.env` (lines 137-139): `OPENCLAW_INSTRUMENT_CLASS_ROUTING=1`, `OPENCLAW_CRYPTO_REGIME=1`, `OPENCLAW_CRYPTO_REDEPLOY=1`.
- **Migration 119** `alpaca_submissions.qty INT→NUMERIC` (applied live; fractional crypto qty now stored — validated: a real $20 order wrote `qty=0.000261957`).
- **Crypto promotion thresholds** `PROMOTION_THRESHOLDS['crypto']={min_sharpe:0.5, max_drawdown:0.70}` — **0.70 is a deliberate operator sign-off** (BTC is a 60-80% DD asset; not gamed). `crypto ∈ ROUTED_INSTRUMENT_CLASSES`. `INSTRUMENT_COST_BPS['crypto']=25.0` (record-only).
- **Broker stops:** only `stop_limit` works for crypto (plain `stop`→422); placed after entry sized to the **actual filled qty** via `_poll_crypto_fill` (entry fills async + short on fees). Fill-poll bumped 10s→**45s** (`7b00a56`) after the smoke showed a >10s fill skipped the stop. `_route_crypto_close` cancels open crypto stops first (a resting stop reserves the position → close 403s). Both call sites guarded vs `subprocess.TimeoutExpired`.
- **Crypto regime detector:** 24/7 4-state HMM, hourly cron (gated), `data/master/crypto_bars_1h.parquet` (9888 BTC+ETH hourly bars), `crypto_regime_states` (migration 118), `.agents/crypto-market-state/crypto_regime_latest.json`. Dashboard tile at `/api/regime/crypto`. Currently LOW_VOL.
- **First real strategy trade fires on the next 10 AM ET daily cycle** (the dedicated `redeploy_crypto.py` is rebalance-only and no-ops until the daily `signals` step computes the new strategy's signal). A one-shot verification timer (`crypto-first-trade-verify.timer`, systemd, local) is armed for **2026-05-27 15:00 UTC** to post results to Discord.

## 3. SP-3 rails SP-4 must exercise (the foundation)

- `instrument_class` enum on `StrategyRecord` (top-level, always-emitted; `from_manifest`/`to_dict` silent-strip-safe). `VALID_INSTRUMENT_CLASSES={equity,option,etp,crypto,futures}`; `ROUTED_INSTRUMENT_CLASSES={equity,option,etp,crypto}` (futures still reserved/raises).
- Sizing dispatcher `src/execution/instrument_class_sizer.py` (equity/etp/crypto pass-through; option scales notional by |delta| fail-open; futures raises). Gated behind `OPENCLAW_INSTRUMENT_CLASS_ROUTING`.
- Per-class promotion thresholds `PROMOTION_THRESHOLDS` in `lifecycle.py` (equity/etp 0.5/0.20; crypto 0.5/0.70; **option still a placeholder = equity values, `TODO(SP-4): calibrate`**).
- Class-aware backtest cost model `INSTRUMENT_COST_BPS` + `run_backtest(strategy_id, *, instrument_class=...)` → writes `strategy_backtest_runs` (`total_sharpe`, `total_max_dd_pct` **as a PERCENTAGE** — promotion metadata + thresholds are FRACTIONS, so promotion must pass `total_max_dd_pct/100`).
- Reference strategies (candidate-or-live, proof the rails flow): `S_commodity_etp_momentum` (etp, candidate), `S_btc_momentum` (crypto, live).
- A new strategy needs: `S_*.py` + `S_*.requirements.json` + a `registry.py:_IMPL_MAP` entry (`id → (module, ClassName)`) + a manifest entry with `instrument_class`. The registry entry is auto-synced at runtime but **must be committed** or a redeploy reverts it.

## 4. SP-4 — Weekly Research Uplift (scope to brainstorm)

**Goal:** the Saturday research workflow (corpus curator + PaperHunter swarm + StrategyCoder + MasterMind reviewer) learns that the broader universe (SP-2) and new asset classes (SP-3/3.1) are in scope — so it can *originate* options/crypto/commodity/broad-universe strategies, not just equity-momentum.

**Current Saturday/Sunday research stack (all systemd timers, ET):**
- Sat 10:00 — `openclaw-saturday-brain.service`: paper ingestion + PaperHunter swarm + MasterMind corpus curation.
- Sat 18:00 — `openclaw-strategy-review.service`: Opus 4.7 1M comprehensive per-strategy review.
- Sat 19:00 — `openclaw-position-recs.service`: deterministic sizing recs (no LLM).
- Sat 20:00 — `openclaw-universe-recs.service`: SP-2 Phase C per-strategy universe predicate recs (gated `OPENCLAW_UNIVERSE_RECS`).
- Sun 08:00 — `openclaw-paper-expansion.service`: Opus + WebSearch open-ended source discovery.
- PaperHunter swarm has 4 rejection gates: abstract blueprint-signal regex → implementability score → data availability → backtest pass. arXiv cats: q-fin.ST/PM/TR/CP/GN/RM + cs.LG/AI/CL + stat.ML.

**Likely SP-4 scope (confirm/refine in brainstorming):**
1. **PaperHunter implementability gate** — accept options/crypto/commodity/etp papers as implementable (today it skews equity-momentum). Infer `instrument_class` at mint (parallels SP-2 Phase D's predicate-at-mint).
2. **StrategyCoder templates** — per-asset-class scaffolds (delta-neutral options, crypto carry/momentum, commodity momentum). Emit the correct `instrument_class` + `.requirements.json` + registry mapping.
3. **MasterMind corpus filters** — `mode=corpus` prompt recognizes broader asset-class candidates.
4. **arXiv expansion** — consider q-fin.PR (derivatives pricing), math.PR (vol models).
5. **Calibrate per-class thresholds** — replace the option `TODO(SP-4)` placeholder with researched values; revisit crypto's 0.70 once there's live data; wire per-class thresholds into StrategyCoder + comprehensive-review.
6. **Backtest fidelity end-to-end** — prove PaperHunter→StrategyCoder→backtest works for a non-equity archetype (esp. options greeks: SP-3 deferred greeks-aware option sizing/backtest — that's a likely SP-4 dependency or a sibling SP-3.2).
7. **Comprehensive review (Sat 18:00) + position-recs (Sat 19:00)** — verify both work for non-equity strategies (asset-class-aware sizing/review).
8. **Universe interaction** — PaperHunter must evaluate "does this paper fit a universe slice we can build?" (SP-2 predicate-per-strategy).

**Decisions to brainstorm:** prompt-level vs structural changes to PaperHunter (cheaper vs robust); arXiv category expansion scope; new strategy-template scaffolding tool; whether options greeks (SP-3.2) is a prerequisite or folded into SP-4. **Cost/time constraints:** the Saturday brain is already a 4-6h job; Opus 4.7 1M passes are ~$8/call; broader corpus = more passes; ~52 live + ~58 candidate strategies today, new classes add dozens more.

## 5. Operating constraints (carry into SP-4)

- **Live VPS, real (paper) money** — surface before any merge/deploy; surface paper-order/live ops for OK before firing; confirm LLM-budget headroom before heavy subagent cycles.
- **NEVER delete from master DB** — `data/master/*.parquet` + canonical Postgres tables (`execution_signals`, `signal_pnl`, `alpaca_submissions`, `crypto_bars_1h.parquet`, …) are append-only. Columns/tickers ADD only; date axis grows only.
- **Never `git add -A` / never commit secrets** — `.env*` is gitignored; stage specific files. The integrity manifest (`src/agent/config/integrity-manifest.json`) is gitignored/local-per-VPS; after editing a tracked, manifest-covered file (e.g. CLAUDE.md) run `./scripts/regen-integrity-manifest.sh` on the VPS to clear the startup `[SECURITY_ALERT] integrity: HASH MISMATCH`.
- **Process** — superpowers brainstorm → writing-plans → subagent-driven-development in a git worktree (native EnterWorktree). Spec → `docs/superpowers/specs/`, plan → `docs/superpowers/plans/`. Ground every named convention (env var, runner cmd, signature) against live source before dispatching subagents. **Worktree gotcha:** `data/master` is gitignored, so `ln -s /root/openclaw/data/master <worktree>/data/master` for anything needing the parquets; `POSTGRES_URI`/`ALPACA_*` live in `.env` (grep them, don't `source` — unquoted parens break bash).
- **DB:** psql NOT installed on the VPS — apply migrations via psycopg2. `psycopg2.connect(os.environ['POSTGRES_URI'])`. The `migrate()` startup runner has a non-idempotent wart (008/027/069 poison the txn → later migrations log "transaction aborted" + skip) — apply new migrations manually and verify.

## 6. Known issues / outstanding (none block SP-4)

- **First crypto trade unverified until 2026-05-27 ~15:00 UTC** (verification timer armed; or it fires on the next daily cycle).
- **Crypto resting-stop reliability** — fill-poll now 45s; if fills routinely exceed that, consider an async stop-on-reconcile redesign (a fast-follow, not yet done).
- **Options greeks** — SP-3 deferred greeks-aware option sizing/backtest (option sizer scales by |delta| fail-open; backtest cost is record-only). SP-4 (or an SP-3.2) likely needs real greeks for option research to be meaningful.
- **Pre-existing, unrelated:** `test_regime_stratified_backtest.py` 2 failures (fail on clean main too); doctor `cboe_vol_indices_freshness` FAIL (VIX stale); latent `webhook_urls DB update (transaction aborted)` cascade on restart.
- **Security:** the 2026-05-25 `.env.bak*` credential-leak incident is contained (tokens rotated, SSH key-only); residual: rotate Postgres/Redis (localhost-only), revoke the old Polygon key. See `[[feedback_credential_leak_webhook_impersonation]]`.

## 7. Pointers

- **Memory:** `project_sp31_crypto` (crypto, this work), `project_sp3_asset_class_expansion` (rails), `project_sp2_*` (universe), `feedback_*` (gotchas), `reference_alpaca_cli`.
- **Specs/plans:** `docs/superpowers/{specs,plans}/2026-05-26-sp3.1-phase-{a,b,c,d}-*`, `…sp3-asset-class-expansion*`, `docs/superpowers/specs/2026-05-21-data-provider-overhaul-handoff.md` (§4 = SP-4 origin).
- **CLAUDE.md** Recent Changes — top entries cover SP-3.1 A→D + activation.
- **Init prompt for the SP-4 session:** `docs/superpowers/specs/2026-05-26-sp4-init-prompt.md`.
