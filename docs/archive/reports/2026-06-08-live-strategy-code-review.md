# MasterMind strategy-code review — 54 strategies, $37.15

**broken=0  fix_suggested=29  ok=25  unparsed/error=0**

## FIX_SUGGESTED — S_reversal_momentum_transition_earnings [live]  trades=79334 sharpe=NaN
- [med] off_by_one: Quintile selection can overlap when n_q > len/2 (here unlikely at 20%, but `ranked >= len - n_q + 1` plus `ranked <= n_q` is fine only if 2*n_q <= len). Safe at q_frac=0.20 with >=20 names, but no guard exists if parameters change. (line ~136)
- [med] nan_handling: Earnings attenuation block mutates the input `earnings` DataFrame in-place via `earnings[date_col] = pd.to_datetime(...)`. Across rolling backtest calls this could mutate shared aux_data state. (line ~92)
- [low] other: `date_col` detection picks the first column containing 'date' which could grab `announcement_date` vs `report_date` ambiguously; deterministic but provider-dependent. (line ~85)
- [low] other: Cutoff uses `pd.Timedelta(days=ea_win)` (calendar days) while `earnings_window=5` is documented as trading days; minor semantic drift. (line ~97)
- [low] other: `reversal_window=21` measures return over 21 trading days inclusive of today's close (no skip-1 for bid-ask bounce); hypothesis description doesn't require skip-1, so acceptable but worth noting vs typical reversal implementations. (line ~75)
- proposed_fix: Copy `earnings` before mutating (`earnings = earnings.copy()`); optionally use business-day offset for ea_win; add guard `if 2*n_q > len(adjusted_reversal): n_q = len // 2` to prevent long/short overlap if quintile_frac is tuned upward.

## FIX_SUGGESTED — S_ptree_panel_tangency [live]  trades=78976 sharpe=1.4727
- NOT faithful to hypothesis
- [high] other: Strategy is named/described as P-Trees panel tangency (Cong et al. 2025) — a tree-partition + mean-variance tangency on leaf portfolios. The actual implementation is an explicitly-documented substitution: a 4-factor cross-sectional rank composite (Value/Profitability/Momentum/Investment), long top quintile, short bottom. No recursive partitioning, no leaf portfolios, no covariance/tangency step is performed. The docstring acknowledges this, but the code does not implement the stated hypothesis. (line ~10)
- [low] nan_handling: Tickers without financials get NaN for value/prof/inv; `_rank` then fills with 0.5 (neutral). With sparse fundamentals, the composite degenerates toward a pure momentum tilt — silently — rather than excluding under-covered names. Not a crash bug, but masks data-coverage problems. (line ~113)
- [low] nan_handling: Momentum NaNs are filled with 0.0 before ranking (`mom.reindex(tickers).fillna(0.0)`). Tickers with missing prices at the -21 or -252 lookback get a middling rank instead of being dropped, which can place data-sparse names into top/bottom quintiles. (line ~117)
- [low] other: `last_prices` is computed but unused — dead code, harmless. (line ~86)
- proposed_fix: Either (a) rename/redescribe the strategy to reflect the actual 4-factor rank composite (drop the P-Trees framing in id/description/hypothesis) so manifest truth-in-labeling matches code, or (b) replace the body with an actual recursive characteristic-split + leaf-portfolio tangency (shrinkage-regularized covariance, e.g. Ledoit-Wolf) as the JFE 2025 paper specifies. Secondary: require ticker membership in financials (or non-NaN value/prof) before quintile selection, instead of imputing 0.5, to prevent fundamentals-missing names from being picked.

## FIX_SUGGESTED — S_intl_momentum_attention_regime [live]  trades=46871 sharpe=2.0698
- [med] off_by_one: Momentum window uses end_px = px.iloc[-skip-1] and start_px = px.iloc[-(mom_lb+skip)-1], which spans mom_lb bars (e.g. 252) rather than the intended 252-21=231 bars after the 1-month skip. The 12-1 momentum convention is return from t-(12m+1m) to t-1m; here both endpoints are shifted by skip, so the lookback length is wrong (12 months total instead of 11 months between endpoints). Should be end_px = px.iloc[-skip-1], start_px = px.iloc[-(mom_lb)-1] (or similar) so the window length equals mom_lb-skip. (line ~94)
- [low] universe_gate: Universe is filtered to tickers in prices.columns, but the strategy is named 'international momentum' — there is no explicit check that the universe contains international names. This is a config/universe_filter concern rather than a code bug, but worth flagging as the implementation will happily run on any universe passed in. (line ~68)
- [low] other: Market-state proxy uses cross-sectional mean of pct_change summed over 63 days (rets.mean(axis=1).rolling(mkt_win).sum()). This is an equal-weighted average return, not a true market return — acceptable as documented proxy, not a bug. (line ~80)
- proposed_fix: Fix the momentum window so end-to-start span = mom_lb - skip (e.g., set start_px = px.iloc[-mom_lb-1] and end_px = px.iloc[-skip-1]) to encode the canonical 12-1 momentum (t-12m to t-1m return), not a 12-month return shifted back by 1 month.

## FIX_SUGGESTED — momentum_12_1 [live]  trades=21853 sharpe=1.6642
- [low] off_by_one: Momentum computed as s.iloc[-skip]/s.iloc[-need]-1 where need=lookback+skip=273. s.iloc[-21] is the price 21 bars before the end (skipping the last month correctly), and s.iloc[-273] is 273 bars before the end. The window between those two points spans 252 bars, which matches the 12m lookback intent. Minor: using s.iloc[-skip-1] vs s.iloc[-skip] is a stylistic choice; current form is acceptable but boundary-sensitive if skip=0. (line ~74)
- [low] other: min_mom_thresh filter is applied BEFORE nlargest, so top_n can be reduced silently when fewer than 10 names clear 0.03. Not a bug per se, but the description says 'top-10' unconditionally. (line ~82)
- proposed_fix: Optional: rank full universe by momentum first, then take top_n, then apply min_mom filter — or document that top_n is post-threshold. No correctness bug otherwise.

## FIX_SUGGESTED — S_vp_macd_index_sensitivity [live]  trades=2369 sharpe=-0.2173
- NOT faithful to hypothesis
- [high] other: P* formula uses r_t = |close-open|/(high-low), which is unsigned — so P* is always non-negative regardless of bar direction. The hypothesis describes an intraday directional strength weighting; without a sign, up-bars and down-bars contribute identically to P*, defeating the directional intent of the VP-MACD construction. (line ~60)
- [high] other: P* formula omits volume entirely. The hypothesis is explicitly 'volume-volatility-intraday-weighted price (VP-MACD)', but the implementation computes P* = close * sigma * r with no volume term. Comment claims 'volume cancels single-bar sum' but that justification is incorrect for an EMA-based MACD — volume should be a per-bar weight. This is unfaithful to the stated hypothesis. (line ~40)
- [med] other: P* is on a very different scale from price (close * small_fraction * small_fraction). MACD on this tiny-magnitude series is mathematically valid but the λ-adjusted crossover threshold (lam=0.89) applied to such small values may not behave as the paper intends; if the paper's λ was calibrated on a price-scale P*, the threshold semantics differ. Not necessarily a bug but worth verifying against paper §3.3 P* definition. (line ~70)
- [low] universe_gate: Universe is hard-coded to SPY/QQQ/DIA and ignores the `universe` argument passed into generate_signals; this is intentional per docstring but bypasses any upstream universe filter. (line ~109)
- [low] other: FLAT signals are emitted with position_size_pct=0.0 and stops==entry==targets==current_price; harmless but unusual encoding for an exit signal. (line ~150)
- proposed_fix: Re-derive P* per the paper to include (a) signed intraday direction sign(close-open) and (b) a volume weighting term (e.g., volume normalized by rolling mean), so P* reflects 'volume-volatility-intraday-structure-weighted price' as the hypothesis states. Load 'volume' in _load_ohlcv and update _compute_vp_macd accordingly.

## FIX_SUGGESTED — S_3d_pca_characteristic_factors [live]  trades=1760 sharpe=3.6511
- NOT faithful to hypothesis
- [med] other: PC1 score uses in-sample SVD on the current cross-section's group-score matrix (X @ Vt[0]) — the sign of PC1 is arbitrary (SVD sign indeterminacy), so LONG-top vs SHORT-bottom decile may be inverted on any given month depending on which direction Vt[0] happens to point. Hypothesis says rank by PC1 score but does not pin the sign; should orient PC1 so its loading on a known 'good' characteristic (e.g., momentum or quality group) is positive. (line ~134)
- [med] other: score_df.fillna(0.5) imputes missing characteristic ranks with the median for tickers that had <2 chars, but dropna(thresh=2) earlier only kept rows with ≥2 non-NaN char ranks; combined with .reindex(tickers) before dropna, tickers missing entire groups still pass into PCA as 0.5 medians, biasing composite toward the cross-sectional mean and diluting the decile sort. Minor faithfulness issue vs. a clean 3D double-sort. (line ~124)
- [low] other: Group definitions map 'bm' (book-to-market, a value char) into the 'quality' bucket alongside ROE, and 'inv' (investment) into 'risk' alongside vol. Lettau (2024)'s 3D double-sort typically uses size×value×profitability or size×investment×profitability blocks; the grouping here is a loose approximation, not the Kronecker block structure the docstring claims. Code is internally consistent but only loosely faithful to the paper's restricted-PCA structure. (line ~119)
- [low] off_by_one: Monthly rebalance gate compares prices.index[-1].month to prices.index[-2].month — fires on the first trading day of each new month, which is correct for monthly cadence; no bug, just noting the gate effectively skips ~95% of bars (consistent with 1760 trades over years on a ~30-name decile). (line ~48)
- [low] other: vol12 uses last 63 trading days of pct_change (~3 months) but is named 'vol12' suggesting 12-month vol; cosmetic naming inconsistency, no logic bug. Sign is negated (low vol → high rank), consistent with low-vol anomaly. (line ~68)
- proposed_fix: Orient PC1 deterministically: after SVD, flip Vt[0] sign so a chosen anchor loading (e.g., momentum or quality group) is positive, ensuring LONG=top consistently encodes 'good' characteristics across months. Optionally tighten dropna(thresh=N_groups) and remove the 0.5 fillna so the decile sort isn't diluted by median-imputed missing characteristics.

## FIX_SUGGESTED — S_price_earnings_momentum_drift [live]  trades=68933 sharpe=1.5565
- [med] other: SUE uses ungrouped chronological order: df.groupby('ticker') preserves row order but the input earnings frame is not explicitly sorted by report_date before groupby. surprises[-1] is assumed to be the most recent quarter, but if earnings rows arrive unsorted (or sorted by ticker then random), the 'latest' surprise and the trailing-4Q std become wrong. Should sort by report_date ascending and take the last 4 (or N) rows per ticker. (line ~160)
- [med] other: SUE std uses ALL historical surprises per ticker (full history), not 'trailing 4 quarters' as the docstring claims. This dilutes the standardization and is inconsistent with the stated SUE convention. (line ~165)
- [low] lookahead: earnings frame is consumed as-is with no as_of/report_date filter — relies entirely on the backtest harness to pass a point-in-time slice. If the harness passes the full earnings panel, SUE on the latest report could include rows reported after the signal bar. (line ~145)
- [low] off_by_one: Price momentum window is t-6m to t-1m: end_idx = len-SKIP_LAST-1, start_idx = end_idx-126. This yields a 126-bar window ending one bar BEFORE the t-21 skip point, slightly shorter than the intended 'skip last month' convention (off by 1 bar; benign). (line ~71)
- [low] other: top_cut/bot_cut via quantile can yield duplicate boundary values; score>=top_cut and score<=bot_cut can overlap when many ties exist (e.g., all-equal pm-only ranks at boundaries), potentially placing same ticker in both longs and shorts. No de-dup check. (line ~98)
- proposed_fix: In _compute_sue: (1) require/derive a report_date column, sort df by report_date ascending before groupby, and (2) restrict per-ticker history to the trailing 4 quarters (grp.tail(4)) for both std and the 'latest' surprise. Optionally also filter rows to report_date <= as_of from prices.index[-1] to guarantee point-in-time. Add a longs = [t for t in longs if t not in shorts] guard after quantile slicing.

## FIX_SUGGESTED — S9_dual_momentum [live]  trades=8706 sharpe=2.4286
- [med] off_by_one: Lookback return uses prices.iloc[-1 - trading_days_skip] / prices.iloc[-trading_days_lb] where trading_days_lb=(lb+skip)*21 and trading_days_skip=skip*21. This measures only `lb*21` trading days of return (e.g., 12 months) rather than the intended Antonacci 12-1 (i.e., 11 months ending 1 month ago). The denominator index should be -(trading_days_lb) i.e. -(lb+skip)*21 vs numerator -(1+skip*21) — currently the span is exactly lb months, not lb-skip. Effectively this is a 12-month return ending ~1 month ago, not a 12-1 momentum. (line ~78)
- [low] universe_gate: active_in_regimes includes HIGH_VOL but the docstring/description says 'Active in LOW_VOL and TRANSITIONING regimes' — minor inconsistency, not a bug since should_run governs. (line ~20)
- [low] other: top_n is fetched via .get() but not declared in default_parameters(), so it silently defaults to 5 regardless of manifest overrides unless params explicitly include it. (line ~53)
- proposed_fix: Clarify the 12-1 lookback: use numerator at iloc[-1 - trading_days_skip] and denominator at iloc[-1 - trading_days_skip - lb*21] (or equivalently set trading_days_lb=(lb+skip)*21 and use denom -trading_days_lb only if numerator is at -trading_days_skip*21 — pick a consistent convention). Add top_n to default_parameters(). Align active_in_regimes with the docstring or update the docstring.

## FIX_SUGGESTED — S12_insider [live]  trades=17 sharpe=-0.9015
- concerns: low_trade
- low_trade_diagnosis: **bug**
- [high] other: HIGH-confidence gate (distinct_insiders>=5 AND net_buy_value>=$2M) silently overrides the documented hypothesis (>=3 insiders, >$500K). The stated thresholds in default_parameters (min_insiders=3, min_net_buy_value=500_000) are effectively dead code — any cluster that meets the hypothesis but doesn't hit 5 insiders/$2M is dropped with 'continue'. This is the most likely cause of trade_count=17. Either remove the HIGH-only gate or update the hypothesis/defaults to reflect the real thresholds (5/$2M). (line ~127)
- [med] universe_gate: active_in_regimes includes HIGH_VOL but the latest backtest shows 0 trades in HIGH_VOL/LOW_VOL/CRISIS and 17 in TRANSITIONING. Combined with the HIGH-only gate above, this suggests the signal is so rare it only fires opportunistically — consistent with the gate bug, not a regime-gating defect per se. (line ~21)
- [low] nan_handling: cutoff uses calendar days (lookback_days * 1.5) as a proxy for trading days. Minor; loose bound is intentional ('calendar buffer') and only over-includes, doesn't drop signals. (line ~68)
- [low] other: transactionType matching uses substring 'BUY' or 'PURCHASE'. If SEC Form 4 'S-Sale' or 'A-Award' rows ever contain 'BUY' substring (unlikely) they'd be mis-classified. Low risk given Form 4 vocabulary. (line ~91)
- proposed_fix: Remove the HIGH-confidence-only gate (drop the 'if conf == MED: continue' block) so the strategy actually fires on the documented >=3 insiders / >$500K threshold. Alternatively, raise min_insiders to 5 and min_net_buy_value to 2_000_000 in default_parameters so the hypothesis text matches what the code enforces. Current state is silently stricter than advertised, which explains the suspiciously low trade count.

## FIX_SUGGESTED — S23_regime_momentum [live]  trades=4941 sharpe=0.7896
- [med] universe_gate: Strategy uses an inline SPY-RV regime sensor (lo/hi thresholds 0.15/0.28) rather than the framework `regime` argument, so per-regime backtest attribution is decoupled from the actual branch taken. The framework's regime tag will not match the strategy's internal mode (momentum/defensive/blend), making the per-regime stats misleading and the `active_in_regimes`/`should_run` gate orthogonal to the actual logic. (line ~63)
- [low] other: TRANSITIONING blend uses `set(...)` which is non-deterministic in ordering across Python runs (insertion order is preserved per-set but `set()` from a list union does not guarantee mo-first ordering); the final `[:n_long]` truncation can therefore drop the momentum pick rather than the vol pick. Use `dict.fromkeys(mo_pick+vl_pick)` or list-dedup to keep ordering stable. (line ~94)
- [low] off_by_one: Momentum lambda guard `len(x.dropna()) > mom_days // 2` allows a ticker with as few as 32 valid observations in a 63-day window to produce a momentum number computed only over the available subset (iloc[-1]/iloc[0]-1), so tickers with sparse history get compared on unequal lookbacks against full-history tickers. Not lookahead, but uneven measurement. (line ~76)
- [low] nan_handling: `spy_rv` falls back to a hard-coded 0.18 (TRANSITIONING band) when SPY is absent or has too few rows; this silently routes the strategy into the blend branch regardless of true market state. Should at minimum log/skip when SPY data is unavailable for a regime-sensor strategy. (line ~68)
- proposed_fix: Either (a) drive the branch off the framework `regime['state']` instead of the inline SPY-RV sensor so per-regime attribution matches behavior, or (b) keep the SPY sensor but document that `active_in_regimes` and per-regime stats are advisory only. Replace `list(set(mo_pick+vl_pick))[:n_long]` with `list(dict.fromkeys(mo_pick+vl_pick))[:n_long]` for deterministic ordering. Tighten the momentum lambda to require ~mom_days valid points (e.g. `>= mom_days - 2`) so tickers are compared on equal lookbacks.

## FIX_SUGGESTED — S25_dual_momentum_v2 [live]  trades=15478 sharpe=1.1813
- [low] off_by_one: Momentum window uses ts.iloc[-mom_w] as start and ts.iloc[-skip_w] as end, giving a span of (mom_w - skip_w) = 231 bars rather than the canonical 12-1 month (252-21=231) — this is actually correct in span, but the start point is mom_w bars back from the LAST bar, not from the skip point. Strict 12-1 momentum would use ts.iloc[-(mom_w+skip_w)] as start and ts.iloc[-skip_w] as end (273 bars total) to match the min_lookback=273 declaration. As written, only 252 bars are actually consumed, contradicting the min_lookback budget. (line ~74)
- [low] other: min_lookback=273 is declared but the actual data consumed is only mom_w=252 bars; the len(prices) < self.min_lookback guard is stricter than needed but not incorrect. (line ~24)
- proposed_fix: If the intended 12-1 momentum is (price 21 days ago) / (price 273 days ago) - 1, change start_val = float(ts.iloc[-(mom_w+skip_w)]). Otherwise update min_lookback to 252 and the docstring to reflect a (252-21)-bar span. No lookahead, sign, gating, or NaN-handling bugs found; regime gate, absolute-momentum filter, top-N selection, and signal emission all faithful to the stated hypothesis.

## FIX_SUGGESTED — S_HV16_gex_regime [live]  trades=0 sharpe=null
- concerns: no_trades
- low_trade_diagnosis: **bug**
- [high] wrong_column: Strategy reads aux_data['options'][ticker]['gex'] but the pipeline's options aux payload does not expose a precomputed dealer gamma exposure ('gex') field per ticker. With gex always None, the `if gex is None: continue` short-circuits every candidate, producing 0 trades across the entire backtest. (line ~27)
- [med] nan_handling: `iv_rank = opts.get('iv_rank') or 50.0` and `vrp = opts.get('vrp') or 0.0` silently coerce missing/falsy values to defaults; a default iv_rank of 50.0 below the 45.0 SELL_VOL gate is harmless, but masking missing vrp as 0.0 means SELL_VOL is unconditionally blocked when vrp is absent, contributing to the no-signal outcome. (line ~30)
- [low] other: Targets/stops are encoded as a generic long-equity bracket (target_1/2/3 = +8/14/20%, stop = -5/-4%) regardless of BUY_VOL vs SELL_VOL direction; this is not a faithfulness bug to the GEX-regime classifier hypothesis but is inconsistent with a volatility long/short position payoff. (line ~52)
- proposed_fix: Verify that the options aux_data feed actually populates a 'gex' (dealer gamma exposure) field per ticker; if not, either compute GEX from the available open-interest×gamma×spot² chain data inside the strategy (or upstream in aux_data builder) before the predicate, and replace the `or` defaults with explicit None checks so a missing vrp doesn't masquerade as 0.0 and silently disqualify SELL_VOL candidates.

## FIX_SUGGESTED — S_HV19_iv_surface_tilt [live]  trades=2104 sharpe=2.1586
- [high] inverted_sign: Targets and stops are direction-agnostic. For SELL_VOL (short-vol), targets at price*1.07/1.12/1.18 and stop at price*(1-0.04) treat the position as if the underlying must rally — but a short-vol thesis profits from IV mean-reversion / range-bound price, not upside. More importantly, if SELL_VOL is interpreted as a short position, the stop below entry is on the wrong side (a short loses when price rises) and targets above entry are unreachable as profit. Brackets should be conditioned on direction. (line ~55)
- [med] nan_handling: `iv_rank = opts.get('iv_rank') or 50.0` coerces a legitimate 0.0 iv_rank to 50.0, allowing BUY_VOL signals (which gate on iv_rank>=50) to fire when iv_rank is actually 0. Same `or` pitfall on vrp (less consequential since vrp isn't gated). (line ~27)
- [low] other: `scale = min(score*4.0, 2.0)` has no lower bound; if score is negative due to upstream feed quirks the size formula could go below the 0.012 floor (though current gating ensures score>0 in practice). (line ~47)
- proposed_fix: Make bracket levels direction-aware: for SELL_VOL, invert stop/targets relative to entry (stop above, targets below) or replace with a vol-based exit; and replace `opts.get('iv_rank') or 50.0` with an explicit None check so iv_rank=0 isn't silently boosted to 50.

## FIX_SUGGESTED — S_HV20_iv_dispersion_reversion [live]  trades=2185 sharpe=0.0236
- [high] other: Stop/targets are direction-agnostic: stop_loss=price*0.95 and targets=price*1.07/1.12/1.18 are configured for a long-underlying trade, but signals are SELL_VOL/BUY_VOL on options. For SELL_VOL (short vol/short premium), an upside target and downside stop on the underlying price do not represent the trade's P&L; for BUY_VOL (long vol/long straddle-like), the bracket is also inverted relative to vol P&L. Brackets should be on the option leg or symmetric on the underlying for vol trades, not biased long. (line ~58)
- [med] other: Z-score is computed against the full universe each bar but the same z-score thresholds (±1.5) and TOP_N//2 caps are applied without any persistence/holding-period logic, so a name can re-fire daily as long as it remains in the tails — likely contributes to the 2185 trade count and churn but not a correctness bug per se. (line ~45)
- [low] nan_handling: vrp is coerced via `opts.get('vrp') or 0.0`, which silently treats missing/zero/negative-falsy vrp as 0.0. For SELL_VOL the VRP_SELL_MIN=0.03 gate then rejects, which is intended; but for legitimate zero vrp this conflates 'missing' and 'zero'. Minor. (line ~40)
- proposed_fix: Make brackets vol-trade-appropriate: for SELL_VOL use a tighter symmetric stop and IV-rank-mean-reversion-based target (or option-leg P&L brackets); for BUY_VOL invert (stop above, target below on IV/vol terms). At minimum, do not hard-code upward-biased underlying targets for both directions. Optionally add a min-holding-period or de-duplication to reduce churn.

## FIX_SUGGESTED — S_markov_frontier_regimes [live]  trades=9107 sharpe=0.2872
- [med] off_by_one: Label-to-month alignment is fragile. _frontier_coefficients iterates t in range(12,n) and `continue`s on NaN/singular-D/LinAlgError. When any t is skipped, len(coeffs) < n-12, so label_start = len(monthly) - len(labels) > 12, and `full_weights[label_start:] = np.where(labels == s, 1.0, 0.15)` assigns labels to the LAST len(labels) consecutive months. But labels are NOT a contiguous tail — they correspond to whichever months survived the skip. Result: regime soft-weights get applied to the wrong rows whenever a skip occurs (rare given ffill+median fill+cov regularization, but possible). Fix: track the surviving month index list inside _frontier_coefficients, return (coeffs, idx) and place labels at those indices in full_weights. (line ~75)
- [low] lookahead: Clustering is fit on the entire coeffs array including the last (current) window, so the cluster boundaries used to label past months are influenced by today's coefficient row. The label of any single month doesn't see future returns (each coeff is computed from monthly[:t]), but the partitioning is a single global ward fit, so historical labels can shift as new data arrives. This is unsupervised structure and standard for regime clustering, but it does mean the transition matrix is computed with full-sample-fit labels rather than a strictly walk-forward labeller. Not a hard bug; flag as advisory. (line ~102)
- [low] nan_handling: `monthly = monthly.fillna(monthly.median())` fills with the column-wise median over time of each ticker (median of a Series), not a cross-sectional median as the comment claims. Effect is benign (per-ticker time-median fill is a reasonable gap filler) but the comment is misleading. (line ~48)
- proposed_fix: Make _frontier_coefficients return (coeffs, surviving_month_indices) so labels can be placed at the correct rows of full_weights via fancy indexing instead of a tail slice. Optionally also restrict ward clustering to coeffs[:-1] to avoid the latest window influencing historical regime boundaries.

## FIX_SUGGESTED — S_price_path_convexity [live]  trades=0 sharpe=null
- concerns: no_trades
- low_trade_diagnosis: **bug**
- [high] universe_gate: Strategy is configured to run live but 0 trades over the backtest suggests the universe param is not being supplied at backtest time. The code filters `universe` against `prices.columns` and bails when `tickers` is empty; if the backtest harness passes an empty/None universe (or a list disjoint from prices columns) this strategy silently emits 0 signals every bar. Hypothesis says 'SP500 cross-section' but the strategy never falls back to `prices.columns` when universe is empty. (line ~48)
- [med] off_by_one: After a 5-day rolling mean with min_periods=5 and dropna(), `smoothed` has length len(series)-4. The window slice is `prices.iloc[-min_lookback:]` = last 65 rows, so smoothed length is 61 — enough for the LOOKBACK=60 slice, but the smoothing eats 4 bars of the intended 60-day path so the second-derivative endpoints are not over the originally-claimed 60-day window. Minor faithfulness issue, not signal-killing. (line ~71)
- [low] nan_handling: `series.dropna()` then taking the last 60 smoothed values can mix non-contiguous calendar dates if a ticker has interior NaNs, distorting the 'path' convexity. Unlikely to zero out signals though. (line ~67)
- proposed_fix: Fall back to `prices.columns` (or intersect with a default SP500 list) when `universe` is empty/None, so the cross-section is always populated at backtest time. Also consider using raw (non-smoothed) prices or extending the window to LOOKBACK+SMOOTH_WIN-1 so the second-derivative spans the full 60 bars.

## FIX_SUGGESTED — S_sparse_basis_pursuit_sdf [live]  trades=20991 sharpe=1.637
- NOT faithful to hypothesis
- [med] other: Hypothesis claims ~32 nonzero positions via L1 basis pursuit with monthly rebalance; implementation hardcodes top_n=10 LONG positions and emits a signal every call (rebalance cadence governed by caller, not enforced here). Not faithful to the '~32 positions / monthly' description, though the L1/RFF mechanic is present. (line ~24)
- [med] other: Target y is the cross-sectional 12-month momentum used as both a FEATURE (feat_df['mom_12m'] feeds RFF via X) and the regression LABEL. Lasso on RFF(features-including-mom_12m) -> mom_12m is essentially fitting the target to a nonlinear expansion of itself; scores reduce to a ranking proxy of mom_12m rather than a true SDF projection. Logic 'works' but does not encode sparse SDF estimation as claimed. (line ~146)
- [low] off_by_one: Momentum lookbacks use s.iloc[max(-N, -len(s))]; when len(s) < N this collapses to s.iloc[-len(s)] = first available bar (often near IPO), producing extreme/biased momentum for short-history names. Not a lookahead but a data-quality wart. (line ~108)
- [low] nan_handling: feat_df.fillna(0.0) for missing fundamentals injects 0 (a meaningful value, not 'missing'), which then feeds RFF. Cross-sectionally biases scores for names without fundamentals coverage. (line ~135)
- [low] other: Fundamentals merge uses groupby(ticker).last() without filtering 'date' <= as_of date; if aux_data['financials'] contains future-dated rows this would peek. Pipeline convention typically point-in-time filters upstream, so likely safe in practice but not defensively enforced here. (line ~129)
- proposed_fix: Either (a) align top_n to ~32 and document monthly rebalance to match the hypothesis, or (b) revise the hypothesis text. More importantly, decouple the Lasso target from the feature set — e.g. use forward returns held out / a separate pricing-kernel target — so RFF→Lasso actually performs sparse SDF estimation rather than self-prediction. Also point-in-time filter financials by date<=as_of and represent missing fundamentals with column-wise medians instead of 0.

## FIX_SUGGESTED — S21_iv_hv_spread [live]  trades=5112 sharpe=-2.1563
- NOT faithful to hypothesis
- [high] other: Hypothesis is 'vol arbitrage / variance risk premium' (sell expensive IV, buy cheap IV vs realized), but the signals route to LONG/SHORT positions in the UNDERLYING EQUITY, not to option structures (no option_spec emitted). Shorting the stock when IV/HV is high is a directional/contrarian equity trade, not a VRP harvest — the IV-premium edge is not actually captured. To faithfully implement the hypothesis, the signal must carry an OptionSpec (e.g., short straddle/strangle when ratio>1.35, long when <0.75) — the SP-4 Phase 0 option lane now supports this. (line ~74)
- [low] universe_gate: active_in_regimes excludes CRISIS, yet backtest shows 120 CRISIS trades. Likely the backtest's regime gate / should_run permits it, but worth confirming intent — CRISIS shows Sharpe 3.66 here, so excluding it from active_in_regimes is leaving the only positive regime off the live whitelist. (line ~24)
- [low] off_by_one: HV20 uses `s.pct_change().iloc[-20:].std()`. The first row of pct_change after dropna is still valid (NaN already dropped from prices). Length>=22 gate ensures 20 returns exist. Minor: std uses sample (ddof=1) — fine but worth knowing. (line ~50)
- [low] nan_handling: `rv_20` in aux_data is documented as None, so code recomputes HV20 from `prices`. If `prices` panel is wide-but-sparse, columns with <22 non-NaN rows are silently skipped — acceptable, but a candidate may quietly miss eligible tickers if its price column happens to have gaps. (line ~48)
- proposed_fix: Either (a) rename/relabel the strategy to a directional equity contrarian-IV signal so the hypothesis matches what the code does, or (b) emit OptionSpec on each Signal (single='straddle' for SELL_VOL with hedge='delta', long straddle for BUY_VOL) and set instrument_class='option' so the trade actually expresses vol. Also add CRISIS to active_in_regimes (or document why it's excluded despite being the only positive-Sharpe regime in the backtest).

## FIX_SUGGESTED — S22_quality_momentum [live]  trades=17535 sharpe=1.4463
- [med] off_by_one: Momentum window uses s.iloc[-skip] as the recent endpoint, which is the price 'skip' bars before the last bar. With skip=21 the recent endpoint is t-21 and the older endpoint is t-(252+21)=t-273, giving a 252-bar return ending 21 days ago — i.e., it skips the most recent ~21 days but the return is measured from t-273 to t-21, not the classic 'last 12 months excluding last 1 month' (which would be t-252 to t-21, a window of 231 bars). The 252+21=273 lookback effectively over-extends the long leg by one month. Minor but a deviation from the standard JT (12-1) momentum spec. (line ~118)
- [low] universe_gate: active_in_regimes includes HIGH_VOL but the docstring/description says 'Long the top-N quality-momentum names in LOW_VOL and TRANSITIONING regimes.' Code and doc disagree; code is the source of truth at runtime but the hypothesis statement is inconsistent. (line ~24)
- [low] nan_handling: _quality_scores_from_financials catches all exceptions silently and returns empty scores, which then falls back to the price-based Sharpe proxy — acceptable fail-open but masks data-shape regressions. (line ~60)
- proposed_fix: Use s.iloc[-(lookback)] (not -(lookback+skip)) as the older endpoint so the 12-1 momentum spans exactly 252 bars ending at t-skip; align active_in_regimes with the docstring (add HIGH_VOL to description or remove from list).

## FIX_SUGGESTED — S_ivol_mispricing_asymmetry [live]  trades=55309 sharpe=0.1728
- [med] other: FF3 factor proxies are degenerate: SMB and HML are hardcoded to zero series, and MKT is the equal-weight cross-sectional mean of the same universe being regressed. Regressing a stock return on the universe mean yields a residual that is the stock's deviation from the cross-sectional mean — not a true idiosyncratic residual. This still captures cross-sectional 'idio-like' dispersion, but it is NOT FF3-residual IVOL as the docstring claims. (line ~86)
- [med] off_by_one: Momentum 12-1 uses closes.iloc[-21] / closes.iloc[-252] - 1, which is the return from t-252 to t-21 (a 12-1 month skip). That is correct per Jegadeesh-Titman convention. However, the price level at iloc[-1] (the signal-bar close) is used as entry_price while the IVOL window is recent_ret.iloc[-IVOL_LOOKBACK:] which includes iloc[-1] return. No lookahead since these are all <= signal bar. (line ~128)
- [low] nan_handling: ret = closes.pct_change().fillna(0) — filling NaN returns with 0 inflates residual-variance denominator and biases IVOL downward for tickers with missing/short history. Should use NaN and let dropna in _ivol handle it (it already does). (line ~91)
- [low] other: misp_components.append(self._pctrank(mom)) for momentum: higher momentum is typically associated with UNDERpriced (continuation/winner) in Stambaugh's MISP composite — momentum is one of the 11 anomalies where HIGH momentum = LONG leg = underpriced. Here higher momentum percentile -> higher misp_score -> classified as overpriced (short). This is an INVERTED SIGN for the momentum component. (line ~141)
- [low] other: Accruals/asset-growth/NSI signs align with Stambaugh (higher = more overpriced). ROA and GP correctly inverted. But momentum sign appears inverted (see above). (line ~151)
- proposed_fix: Invert the momentum component in misp_components: use 1.0 - self._pctrank(mom) so high-momentum names get LOW misp_score (underpriced/long leg), consistent with Stambaugh's MISP. Also stop fillna(0) on returns before _ivol — pass raw NaNs so the regression's dropna handles missing bars correctly. FF3-proxy degeneracy is a deeper modeling caveat but not breaking.

## FIX_SUGGESTED — S_extreme_intraday_reversal_nasdaq [live]  trades=33858 sharpe=NaN
- [low] other: Uses close-to-close return as proxy for intraday open-to-close move. Documented in comments as a deliberate proxy (Pearson ~0.92), so not a bug, but it does mean the strategy is technically an overnight+intraday reversal, not strictly the Zawadowski intraday-only signal. (line ~13)
- [med] off_by_one: Decile fallback uses n_decile = max(2, int(len(today_ret) * 0.08)) which is an 8% tail per side (top+bottom = 16%), not a true decile as the comment claims. Logic is internally consistent (correct sides selected) but the naming/comment is misleading. Not a correctness bug for the contrarian direction. (line ~86)
- [low] nan_handling: roll_vol.replace(0, np.nan) followed by today_ret / roll_vol produces NaN z-scores for zero-vol tickers; extreme_mask correctly drops them via dropna(). today_z.reindex(today_ret.index) is a no-op since today_z already shares that index — harmless. (line ~76)
- [low] other: Final return signals[:self.MAX_SIGNALS] truncates after building both sides; combined with MAX_SIGNALS_SIDE=20 per side this can cap to fewer than 40 total. Not incorrect, just a soft cap interaction worth noting. (line ~136)
- proposed_fix: Rename/clarify the 8% tail fallback (comment says 'decile' but code uses 8%); optionally use open-to-close return when intraday OHLC is available to match the hypothesis more literally. No correctness fix required — signs, lookbacks, and gating are all correct.

## FIX_SUGGESTED — S_ma_tsmom_crossover [live]  trades=95057 sharpe=0.7711
- NOT faithful to hypothesis
- [high] other: Hypothesis claims an MA *crossover* vs TSMOM comparison, but the code only checks price > SMA(k) for 5 windows (a price-above-MA breadth score). There is no crossover detection (no comparison of current vs prior state, no fast/slow MA pair), and no TSMOM signal at all — so the implementation does not encode the stated hypothesis. (line ~58)
- [med] other: Vol-scaling formula deviates from description: weight = min(TARGET_VOL/rv * scale, base_size * 2.0) caps the vol-target by 2x base_size rather than letting target_vol/rv stand on its own (and then by MAX_WEIGHT). This silently shrinks vol-target sizing whenever base_size is small (e.g., many ranked names), which is not what Marshall-style vol-scaling does. (line ~89)
- [low] other: `scale` is applied twice in the vol-scaled branch (base_size already includes scale, and target_vol/rv is also multiplied by scale), so the vol-target leg double-counts the regime scale relative to the base leg. (line ~89)
- proposed_fix: Either (a) rename/redocument the strategy as a price-above-MA breadth long-only filter (drop the crossover+TSMOM claim), or (b) implement a true MA-crossover signal (e.g., fast SMA crosses above slow SMA on the latest bar with prior bar below) and optionally compare/combine with a TSMOM signal (sign of 12m return). Also fix the vol-scaling: weight = min(TARGET_VOL/rv, MAX_WEIGHT) * scale, without the base_size*2 clamp and without double-applying scale.

## FIX_SUGGESTED — S_macro_risk_momentum_ip_beta [live]  trades=74229 sharpe=0.6267
- NOT faithful to hypothesis
- [high] other: _get_ip_growth reindexes a DatetimeIndex (ip_monthly) with range(n_rows) (integers). Integer keys never match the date index, so reindex returns all-NaN, then .fillna(0.0) makes ip_daily a vector of zeros. This propagates into _compute_betas where ip_dm = 0 and ip_var = 0, triggering the zero-beta fallback. Result: betas are always zero, IP-growth beta plays no role in ranking — strategy degenerates to pure momentum long/short despite the hypothesis demanding IP-beta tilting of the deciles. (line ~143)
- [med] other: Even if ip_growth were correctly aligned, the betas are computed over a daily-frequency, forward-filled IP-growth series in which most days have value 0 (only month-end has nonzero change). Regressing daily returns on a step-function makes betas mechanically driven by the monthly-rebalance day returns, not a clean IP-growth loading. Should align by month or resample monthly returns before OLS. (line ~165)
- [low] other: long/short selection sorts by IP-beta then truncates to max_per_side, but since betas are zero (above bug), Python's stable sort preserves original order from ranked.index — selection ends up arbitrary (alphabetical-ish) within the decile rather than IP-beta-tilted. (line ~108)
- proposed_fix: Fix _get_ip_growth: align IP-growth to the price index, e.g. ip_monthly.reindex(prices_sub.index, method='ffill').fillna(0.0), so the daily series carries real values. Optionally resample returns to monthly before regressing on monthly IP-growth pct_change to get an economically meaningful beta. After the fix verify betas vary across tickers (np.ptp(betas) > 0) and that sort actually reorders the decile.

## FIX_SUGGESTED — S_pca_etf_stat_arb_reversion [live]  trades=74095 sharpe=-0.4221
- [med] other: Betas are estimated using all T returns (Xty = factors.T @ R), but factors are derived from demeaned returns R_dm. The intercept of the regression is dropped, so betas absorb a small bias from the row-mean of R. Residuals resid = R - factors @ betas thus inherit a non-zero mean, which the cumulative-sum X_ou drifts on. The OU fit partially corrects via its own intercept a, but the m = a/kappa center can still be biased. Minor — does not invalidate the signal but is not textbook Avellaneda/Lee. (line ~70)
- [low] nan_handling: R = ret_mat.values is taken AFTER dropna() on returns, but the SVD/OLS is run on R rather than R_dm for the Xty step. This is internally consistent (since R_dm only differs by a constant per column, and the column means of factors are ~0 by construction of SVD on demeaned data), but mixing R and R_dm is fragile and easy to misread. (line ~73)
- [low] other: sigma_eq = sigma / sqrt(2*kappa) uses the residual std of dX (one-step OU innovations), which is the conventional Avellaneda approximation; correct. (line ~95)
- [low] other: s_score sign convention: ss < -S_ENTRY → LONG (residual cheap, expect mean reversion up), ss > S_ENTRY → SHORT. Matches contrarian hypothesis. No inversion. (line ~115)
- proposed_fix: Compute betas from demeaned returns consistently: use Xty = factors.T @ R_dm so residuals are zero-mean by construction; cumulative residuals then have no spurious drift and m estimation is cleaner. Otherwise the implementation is faithful.

## FIX_SUGGESTED — S_prism_vq_cross_section_factor [live]  trades=23237 sharpe=2.1138
- [med] other: 5-day rebalance gate uses `len(prices) % 5 == 0`, which depends on the absolute length of the prices history passed in. In live use this means the strategy only fires on calendar-arbitrary days where the total bar count happens to be divisible by 5, not on a stable weekly cadence aligned to a specific weekday. Backtest harness loops with step=5 so it always passes, masking this in metrics; live will be irregular. (line ~60)
- [med] nan_handling: `quintile_code` maps NaN ranks to the median bucket 2.0 (not dropped), so tickers with missing momentum/vol features still receive composite scores and can land in long/short deciles. This silently includes data-poor tickers in selection rather than excluding them. (line ~90)
- [low] other: `quality` and `value` factors default to a constant 2.0 for every ticker when financials are missing for that ticker, meaning those columns contribute a uniform additive offset and effectively don't differentiate — composite is driven almost entirely by price factors unless financials populate. Faithful to hypothesis as a graceful fallback, but worth noting. (line ~100)
- [low] other: Value proxy `book_eq / (cp * 1e6)` treats `totalStockholdersEquity` as if it's in dollars and divides by price-times-1e6 — this is a rough scale-only proxy (no shares outstanding), so the cross-sectional ordering depends on equity magnitude rather than true book/price. Logic is internally consistent for ranking purposes since `quintile_code` only uses rank, but mislabeled as B/P. (line ~113)
- [low] other: `mom_12_1m = psub.iloc[-22] / psub.iloc[-253] - 1` is correct 12-1 momentum (price 1m ago vs 12m ago); confirmed not lookahead-biased. No issue, just confirming. (line ~80)
- proposed_fix: Replace `len(prices) % REBAL_DAYS != 0` with a weekday-anchored gate (e.g. fire only when last index is a Monday) or use a stored last-rebalance date, so the 5-day cadence is calendar-stable in live use. Optionally drop NaN-feature tickers from the composite ranking universe rather than median-imputing.

## FIX_SUGGESTED — S_growth_inflation_sector_timing [live]  trades=2196 sharpe=0.4127
- NOT faithful to hypothesis
- [high] other: Inflation signal compares RAW PRICE LEVELS — mean(close[XLE,XLI,XLF,XLB]) vs mean(close[XLU,XLV,XLP]) — rather than relative strength/returns. Sector ETFs trade at very different nominal prices (e.g., XLE vs XLU), so this comparison is dominated by static price-level offsets and acts as a near-constant signal unrelated to inflation regime. The hypothesis states 'inflation-beta sector strength' which requires normalized momentum / returns (e.g., trailing return, ratio vs SMA, or z-score) — not raw close levels. (line ~99)
- [high] other: Same raw-price-level inflation signal is reproduced in _run_backtest (pos_vals / neg_vals built from absolute closes), so backtest metrics also reflect the broken proxy rather than the stated hypothesis. (line ~214)
- [low] off_by_one: Backtest SMA window uses iloc[i-SMA_W:i+1] (253 obs) while live uses iloc[-SMA_W:] (252 obs). Minor inconsistency; not a correctness bug but live/backtest are not byte-identical on the growth proxy. (line ~204)
- [low] other: Backtest records pnl as (exit_px - prev_target_px)/prev_target_px and r_multiple as pnl/0.20 — the 20% 'risk unit' is arbitrary and inflates R-multiples; not a logic bug but worth noting for downstream consumers. (line ~237)
- proposed_fix: Replace raw-price-level inflation proxy with a normalized strength measure — e.g., 63d (or 252d) total return per ETF, or close/SMA(63) ratio, then compare mean of those for INFLATION_POS vs INFLATION_NEG. Apply identical change in both generate_signals and _run_backtest. Optionally align the live/backtest SMA windows (use exactly 252 obs in both).

## FIX_SUGGESTED — S_value_momentum_everywhere [live]  trades=71925 sharpe=1.2442
- NOT faithful to hypothesis
- [med] off_by_one: Momentum 12-1m return uses ts.iloc[-(MOM_SKIP+1)] as the numerator (price 22 bars before today) and ts.iloc[-(MOM_LOOKBACK+MOM_SKIP)] as the denominator (273 bars before today), giving a 251-bar return over a ~12.0m window with a 1m skip. Standard 12-1 momentum is the return from t-252 to t-21 (≈11 months). The numerator/denominator indices are also asymmetric (-(SKIP+1) vs -(LOOKBACK+SKIP), not -(LOOKBACK+SKIP+1)), introducing a 1-bar mismatch in the window length. Minor but a real off-by-one. (line ~63)
- [high] other: Hypothesis claims a multi-asset-class composite across eight asset classes (equities, bonds, commodities, currencies, country indices, etc., per Asness-Moskowitz-Pedersen 2013). Implementation is single-asset-class equity-only (INSTRUMENT_CLASS='equity', sp500 universe). The 'everywhere' claim is not faithfully encoded — this is a US-equity-only value+momentum composite. (line ~15)
- [low] nan_handling: When BTM is missing, composite falls back to z_mom*0.5 (half-weight momentum) rather than z_mom alone. This is a deliberate down-weight but is not described in the hypothesis. Not a bug per se, just an undocumented design choice that biases composite scores of value-missing tickers toward 0 and may push them into the middle tercile instead of being ranked on pure momentum. (line ~94)
- [low] other: financials dict is treated as a point-in-time snapshot keyed by ticker; if upstream aux_data['financials'] is the latest-known-value rather than as-of-bar, there is potential look-ahead via fundamentals. Cannot verify from this file alone, but worth confirming aux_data['financials'] respects as_of at the engine boundary. (line ~56)
- proposed_fix: (1) Fix the momentum index pair to symmetric 12-1m: numerator ts.iloc[-(MOM_SKIP+1)], denominator ts.iloc[-(MOM_LOOKBACK+MOM_SKIP+1)] so the window length is exactly MOM_LOOKBACK-MOM_SKIP bars. (2) Either rename the strategy to reflect equity-only scope, or extend the universe via the etp/multi-asset instrument_class to actually trade the eight asset classes the hypothesis describes. (3) Optionally: when BTM is missing, use pure z_mom (not z_mom*0.5) to keep momentum-only tickers comparable to composite-ranked ones.

## FIX_SUGGESTED — S_idiosyncratic_vol_puzzle [live]  trades=72309 sharpe=NaN
- NOT faithful to hypothesis
- [med] other: Hypothesis specifies Fama-French 3-factor residuals (MKT, SMB, HML) over prior month; implementation uses only a beta=1 equal-weight market proxy residual. The code's docstring acknowledges this simplification, but it is not faithful to the stated hypothesis (no SMB/HML, no beta estimation, no size/B-M/momentum/liquidity controls). (line ~74)
- [low] off_by_one: window_ret takes IVOL_WINDOW+1 rows of pct_change; the first row after pct_change in that slice is valid (not NaN) because pct_change is computed over the full prices_sub before slicing. So std uses 23 daily returns instead of the intended 22. Minor — affects window length by 1 bar, not signal direction. (line ~78)
- [low] other: Comment claims 'annualised std' but std is not annualised (no sqrt(252) factor). Pure ranking is unaffected since it's monotonic, so signals are still correct — cosmetic doc mismatch only. (line ~85)
- proposed_fix: Either (a) update docstring/description to accurately state 'market-residual (beta=1) std-dev' rather than 'Fama-French 3-factor residuals' to match the implementation, or (b) implement true FF3 residualization using MKT/SMB/HML factors via OLS regression per ticker. Optionally tighten window to IVOL_WINDOW rows post-pct_change and fix the 'annualised' comment.

## FIX_SUGGESTED — S15_insider_opportunistic_short [live]  trades=929 sharpe=7.8634
- [low] other: _personal_stake_pct overestimates prior_holdings when an insider has multiple sales in the window. Implementation uses max(sharesOwnedAfter) + sum(shares_sold). For ordered sales, max(sharesOwnedAfter) corresponds to the earliest sale (X - s1), and adding sum(all sales) yields X + s2 + s3 + ..., not X. Result: pct is biased DOWN, so conviction stake-test is harder to pass than spec intends. Conservative (creates false negatives, not false positives), but mathematically wrong. Correct prior = max(sharesOwnedAfter) + shares_sold_of_that_specific_txn, OR min(sharesOwnedBefore) if available. (line ~138)
- [low] other: Stage 1 uses transactionDate without referencing filingDate. Form 4 filings are due within 2 business days of the transaction, so most transactions in a 30-day lookback would be public, but there is a small window where a very recent transactionDate (last 1-2 days) may not yet be public knowledge on ref_date. Not a true lookahead bug given the 30-day window but worth noting if aux loader source includes unfiled txns. (line ~237)
- [low] other: classify_insider returns 'opportunistic' for an insider with zero qualifying sales in the t-15..t-3 month window. This matches the docstring ('new insider default') but means insiders with substantial M-Exempt/F-InKind activity but no S-Sales also classify as opportunistic. Faithful to the stated implementation, but slightly broader than the Cohen-Malloy-Pomorski paper which keys off any insider activity. (line ~55)
- proposed_fix: Fix _personal_stake_pct: iterate through txns and for each compute (sharesOwnedAfter + this_txn.shares) as a per-txn 'prior' estimate, then take max across the insider's txns as the true prior_holdings; OR sort sales by date and use sharesOwnedAfter of the LAST sale plus shares_sold_total. No changes needed to gating/regime logic — strategy is otherwise faithful to spec.

## OK — S_custom_jt_momentum_12mo [live]  trades=10926 sharpe=1.7391

## OK — S24_52wk_high_proximity [live]  trades=4708 sharpe=0.4338

## OK — S_HV8_gamma_theta_carry [live]  trades=20989 sharpe=0.6371

## OK — S_epistemic_rank_gate [live]  trades=65872 sharpe=1.2341
- [low] other: Hypothesis mentions LightGBM scores but implementation uses a deterministic 12-1 momentum proxy — explicitly documented in comments/description, so faithful as a proxy, not a logic bug. (line ~56)

## OK — S_tr_02_hurst_regime_flip [live]  trades=71289 sharpe=1.8444

## OK — S_tr_06_eod_reversal [live]  trades=78816 sharpe=NaN
- [low] other: ROD3 in Zarattini & Staley is the prior-close→15:00 ET intraday return; the EOD proxy uses the full-day open-to-close (actually close-to-close via pct_change) return. This is acknowledged in the docstring as a daily-data proxy, not a bug, but it does change the signal's economic meaning (close-to-close reversal vs intraday reversal). (line ~62)
- [low] nan_handling: vol.get(ticker, 0.25) returns NaN (not 0.25) when ticker exists in the Series with a NaN value because Series.get only uses the default for missing keys. The subsequent np.isnan check correctly handles this, so behavior is safe, but the default in .get is misleading. (line ~92)

## OK — S_tr_03_bocpd_change_point [live]  trades=25319 sharpe=0.3647
- [low] other: BOCPD changepoint mass uses log_h + log_pred[0] (prior predictive only) rather than the canonical Adams & MacKay form log_h + logsumexp_r(log_pred[r] + log_R[r]). This is a documented simplification (the inline comment claims it matches eq. 3 but eq. 3 actually sums over r_{t-1}). It biases recent_cp downward when the run-length posterior has mass at long runs whose predictive is high, but does not invert direction or zero out signals; the code still generates ~25k trades and the changepoint probabilities remain monotonic in the underlying surprise. (line ~68)
- [low] other: Direction is taken from arr[-most_likely_rl:].mean() where most_likely_rl is argmax of the FINAL run-length distribution (after the last bar). This is point-in-time correct (only uses data up to t=T which is the signal-eval bar — no peeking), and faithfully encodes 'post-break direction'. Note the most_likely_rl==0 fallback to 1 forces a one-bar direction read when a CP just occurred, which can be noisy but is not a logic bug. (line ~140)
- proposed_fix: Optional: replace log_R_new[0] = log_h + log_pred[0] with logsumexp(log_1mh? no — log_h + log_pred[finite] + log_R[sl][finite]) to match the canonical Adams & MacKay CP update. Not required for correctness of the stated hypothesis; the current code is internally consistent and free of lookahead, sign, gating, or NaN-handling defects.

## OK — S_barbell_trend_horizon [live]  trades=114199 sharpe=NaN
- [low] other: Composite is initialized to 0.0 for all tickers; tickers with insufficient/NaN momentum at a given horizon retain 0 contribution rather than being excluded, which can let low-data tickers rank into top/bottom decile. Not a correctness violation of the stated hypothesis but mildly distorts the cross-section. (line ~60)
- [low] other: _horizon_weights flattens cross-sectional pct_change returns across all tickers into one Sharpe; this is a pooled-ticker Sharpe rather than a per-horizon strategy Sharpe. Matches the loose 'Bayesian-inspired' description but is a coarse proxy. (line ~105)
- [low] other: Skip-21 is applied only when lb>21; the 21-day horizon itself uses p_now=last bar with no skip. This matches the code's stated intent (avoid JT reversal only for longer horizons) but means short-horizon momentum may include the most recent bar's reversal noise. (line ~64)

## OK — S_nonstationarity_adaptive_selection [live]  trades=67290 sharpe=-0.0545
- [low] other: Tournament uses a SINGLE held-out eval period (last 21 bars) per call, not a true rolling cross-validated sweep. It is 'rolling' only in the sense that the strategy re-runs daily. Logic still correct. (line ~108)
- [low] other: When no window satisfies the gates in _tournament_select, best_window falls back to WINDOWS[0]=21 with best_ic still -inf. Benign because the outer min_rows check guarantees enough data, but the fallback is silent. (line ~105)
- [low] universe_gate: Backtest shows 18,621 LOW_VOL trades despite active_in_regimes excluding LOW_VOL and the explicit should_run() short-circuit. This is a framework/backtest aggregation behaviour, not a bug in this file — the code itself correctly returns [] for LOW_VOL. (line ~38)

## OK — S25_dual_momentum [live]  trades=15478 sharpe=1.3713

## OK — low_volatility_us [live]  trades=58639 sharpe=NaN

## OK — S_cross_sectional_price_momentum [live]  trades=57437 sharpe=0.6364

## OK — S_pairs_trading_jump_diffusion_intraday [live]  trades=6233 sharpe=-0.0823
- [low] other: sigma_eq divides by sqrt(1-b^2) to get unconditional std of OU process; this is consistent with stationary AR(1) and correctly normalizes the z-score. No bug. (line ~33)
- [low] other: Pair leg j reuses leg i's z-score sign convention via 'zscore': -zscore in params, but direction dir_j is correctly inverted from dir_i. Cosmetic only. (line ~104)

## OK — S_long_term_price_reversal [live]  trades=37835 sharpe=1.085

## OK — S_btc_gold_dual_momentum_rotation [live]  trades=1513 sharpe=1.1742
- [low] other: _composite_sig uses 'len(weekly_series) > lb' which requires lb+1 observations to compute a return over lb periods — correct (needs index -1 and -1-lb). No off-by-one. (line ~33)
- [low] other: Tie-breaking favors GLD when sig_gld == sig_btc (including both at -inf, but the >0 filter prevents selection in that case). Minor but intentional-looking. (line ~76)

## OK — S_visibility_graph_rsi [live]  trades=81624 sharpe=-0.1769
- [low] other: VGRSI loop iterates i in range(1, j), excluding i=0. This is defensible (avoids p[v-1] wrap-around when computing chg at the oldest visible node) but does drop the very first bar's visibility contribution. Symmetric across LONG/SHORT so no sign bias. (line ~33)
- [low] nan_handling: s_minus<1e-10 or n_minus==0 returns 99.0 (extreme overbought) — this can synthetically trigger SHORT crosses when the next bar happens to dip back below 70. Rare in practice and symmetric in spirit but a slight asymmetry vs. the s_plus==0 case which is handled implicitly (ra=0 → VGRSI=0). (line ~51)

## OK — S_price_filter_rule_trend [live]  trades=57924 sharpe=1.163
- [low] other: Filter rule per Sweeney (1988) traditionally tracks the most recent local extremum since last signal flip, not a fixed 252-day rolling min/max. The implementation is a reasonable adaptation but is not literally Sweeney's path-dependent filter — it triggers off the rolling extremum of the prior 252 bars, which fires more readily after long trends. Still encodes the ½%-above-trough / ½%-below-peak intent. (line ~53)
- [low] nan_handling: rolling_min/rolling_max use pandas min/max with default skipna=True; if a ticker has all-NaN over the lookback window the result is NaN and is filtered out correctly. No incorrect drops observed. (line ~55)

## OK — S_labor_day_week_momentum_reversal [live]  trades=9 sharpe=4.7845
- concerns: low_trade
- low_trade_diagnosis: **genuine**

## OK — S_fomc_presell_spy_long [live]  trades=5 sharpe=4.6535
- concerns: low_trade
- low_trade_diagnosis: **genuine**
- [low] other: generate_signals re-reads SPY OHLC from disk on every call to fetch high/low for the midpoint filter; the runtime `prices` panel typically only has close, so this is necessary, but it adds I/O. Not a correctness bug. (line ~96)
- [low] nan_handling: If the SPY OHLC parquet read fails or `today` is missing from its index, the midpoint filter is silently skipped (signal still fires on just the -1% close rule). Hypothesis requires bottom-half-of-range; this is a fail-open path. Minor — production parquet reliably contains SPY rows. (line ~130)
- [low] other: Fallback `next_td` advance skips weekends but not market holidays; if today is the last bar in the panel and the real next session is a holiday-shifted FOMC, the comparison could miss. In practice `prices.index` always contains the next trading day so the fallback branch is unreachable. (line ~113)

## OK — S_bppp_bayesian_parametric_weights [live]  trades=28020 sharpe=-0.0529
- [low] other: OLS label is the trailing 21-day return ending at iloc[-1] (the decision bar). This is in-sample regression on past realized returns — not lookahead (no future data is used) — but the rvol feature window (last 63 days) overlaps the label window, so the rvol coefficient is fit with mild contemporaneity. Momentum features (mom12, mom6) end at iloc[-21] and are cleanly prior to the label window, so they are unaffected. (line ~107)
- [low] nan_handling: ey_arr → z_ey: NaNs in the raw earnings-yield array participate in nanmean/nanstd, then z values are post-hoc nan_to_num'd to 0 AFTER clipping. This is benign (zero is the cross-sectional mean of the standardized series) but means tickers with missing fundamentals receive a neutral value-tilt rather than being excluded. (line ~98)
- [low] other: signal_frequency='monthly' is declared but generate_signals does not internally gate on month boundary — relies entirely on the engine's cadence scheduler to enforce monthly rebalancing. Not a bug if the scheduler respects the field, but worth confirming. (line ~34)
- proposed_fix: Optional: shift the OLS label to a forward-looking sample built from PRIOR cross-sections (e.g. fit theta on a rolling history of (Z_{t-k}, ret_{t-k → t-k+21}) pairs) so the rvol/label overlap is removed; or shorten rvol window to <21 days. Not required for correctness — current code has no lookahead and faithfully implements BPPP soft-thresholding.

## OK — S_fama_french_anomaly_dissection [live]  trades=63059 sharpe=1.4479
- [low] nan_handling: Missing accruals/NSI default to neutral rank 0.5, so stocks with only momentum data still enter the composite. Not a bug — a documented design choice — but it means the composite can be momentum-only for stocks lacking fundamentals, which dilutes the 3-factor claim for those names. (line ~105)

## OK — S_commodity_etp_momentum [live]  trades=2459 sharpe=0.925
- [low] off_by_one: Trailing return uses series.iloc[-lookback] as the base, which spans (lookback-1) intervals, not 90. Minor and consistent across tickers so ranking is unaffected; magnitude/confidence threshold is only marginally biased. (line ~117)
- proposed_fix: Optionally use series.iloc[-(lookback+1)] as the base to get a true 90-day return; not required since ranking is invariant.

## OK — S_btc_momentum [live]  trades=1490 sharpe=0.9897

## OK — S_options_flow_confirmed_momentum [live]  trades=8755 sharpe=0.7328
- [low] universe_gate: Strategy declares active_in_regimes=['TRANSITIONING'] and gates via should_run(), yet per-regime backtest reports trade_count>0 in CRISIS/HIGH_VOL/LOW_VOL. This is a backtest-aggregator behavior (entry_regime partitioning vs. the engine's regime-gate enforcement), not a defect in this strategy's code — should_run() correctly returns [] outside TRANSITIONING. (line ~47)
- [low] other: entry_price=cur uses the signal-day close, which under the 2026-05-30 t+1 fill model is overridden by the simulator (bracket re-anchored). Not a bug — matches the documented contract. (line ~74)

## OK — S_news_sentiment_long_short [live]  trades=34467 sharpe=1.3354
- [low] other: active_in_regimes excludes CRISIS, yet backtest reports 1718 CRISIS trades — likely the backtest harness labels trades by exit/realized regime rather than signal-gen regime; strategy code itself correctly returns [] when should_run is False, so not a bug in this file. (line ~29)
- [low] other: Uses prices[t].iloc[-1] as 'cur' without verifying the last bar corresponds to the same as-of date as the sentiment row; relies on the engine to pass point-in-time aux_data['sentiment']. No peeking inside the strategy itself. (line ~59)
