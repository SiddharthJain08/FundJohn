# OpenClaw operational changelog (moved from CLAUDE.md 2026-07-17)

Dated, per-change engineering log kept by the operator agents. Entries are
point-in-time: later entries supersede earlier ones (several early entries are
explicitly corrected by the 2026-07-16 status-correction entry). For current
architecture see /README.md and /ARCHITECTURE.md.

## Recent Changes

- **2026-08-29: benchmark-relative sizing landed (spec `docs/specs/2026-08-29-benchmark-relative-sizing-spec.md`, plan `docs/superpowers/plans/2026-08-29-benchmark-relative-sizing.md`, commits `67afceb..ecaad15`).** SPY's regime Sharpe (`S_m`) is now a SIZING input only: the R1 benchmark leg was REMOVED from candidate→live promotion (`promotion_service.js`), `lifecycle.can_transition`, `regime_qualification.qualifies_regime` and both activation/eligibility assigners (D1); `strategy_backtest_regimes.benchmark_sharpe` is still written, informational. Weights: `strategy_weights.daily_weight = effective_sharpe` — the √avg_holding_days cadence normalization is retired from sizing, `backtest_panel.effective_sharpe` and the dashboard's `blend_scope`/Eff.Sharpe follow (`OPENCLAW_STRATEGY_CADENCE_WEIGHT_NORM=1` reverts all three; D2); the √(ln n/ln 1000) trade-count factor is OFF (`OPENCLAW_TRADE_WEIGHT_FACTOR=1` restores; D3); weights rebuilt 17:58 UTC (109 rows, trigger `cadence_norm_removed`). New beta sleeve `S_beta_spy` (long SPY every bar, hold 21, `BaseStrategy.benchmark_sleeve=True`, mirrored into `strategy_registry.parameters` at registration; `_IMPL_MAP` entry); the sizer reads the flag from the registry (`execution/benchmark_sleeve.py`) and net-direction-qualified benchmark tickers are exempt from `min_acting_strategies`, from the `S_adj − S_m` hurdle, from the per-ticker conviction cap, and are excluded from asset-correlation cluster capping (D6, no beta cap). Rule C (`execution/benchmark_sizing.py`, wired in `_sharpe_cadence_path` right after the S_adj rebuild): alpha tickers sized on `sign(S_adj)·max(|S_adj| − S_m, 0)`, ties dropped, shorts face the same hurdle (D7); `S_m` = SPY regime Sharpe over 2016-04-11..today cached in `pipeline_config.benchmark_regime_sharpe`; `OPENCLAW_BENCH_RELATIVE_SIZING` unset = SHADOW (a `bench_sizing.shadow[REGIME]: …` line every cycle, posted to #botjohn-log while `corr_cumsharpe_log_until` is live), `1` = APPLY. Similarity: pairs containing a benchmark sleeve use return-correlation only at ≥60 overlapping obs (D9). Read-only parity artefact: `scripts/bench_relative_sizing_replay.py --nav <equity>` (mirrors the live lane per `OPENCLAW_SAMEDAY_SIGNAL_TARGET`, stubs broker/Discord/Redis). Rollout state at commit time: code landed; `beta-sleeve-finish` transient unit armed for 2026-08-30 02:00 UTC (panel rebuild → register `S_beta_spy` pending_approval → smoke + full backtest); OWED: operator promotes `S_beta_spy` on the :3000 dashboard, ≥2 shadow cycles from Mon 2026-08-31 15:00 ET, replay diff, then flip the flag + user-scope johnbot restart — only once `S_beta_spy` has a current LOW_VOL weights row (flag ON with no benchmark ticker and every alpha ≤ S_m empties the LOW_VOL book into the zero-conviction flatten).

- **2026-08-28 23:26 UTC: `OPENCLAW_EXIT_HOOK_LIVE=1` flipped (operator).** johnbot restarted (user scope); JS promotion guard and the engine both read `1`. No `exit_hook` strategy is live yet — nothing changes in the book until one is promoted (X1 stays PARKED: benchmark gates + hold-cap alignment). Next signals step with the hook armed: Mon 2026-08-31 15:00 ET — expect `[exit_hook] enabled` in the engine log.

- **2026-08-28: per-bar exit hook — Phase 2 (live mirror) landed, flag OFF.** `engine.update_pnl` evaluates `should_exit` + the per-signal time stop for `exit_hook` strategies (hook-strategies only, D6) after stop/target inference, behind `OPENCLAW_EXIT_HOOK_LIVE` (default 0); `days_held` for the hook is a bar count (parity); closes reuse the existing signal_pnl path so the 15:55 sizer emits orphan_close. Shared `configured_max_hold_days`; digest line; parity test (LONG/SHORT/time stop) green; live replay on run `3a470001`: AGREEMENT 11/11 (23 live-only max_hold = hold-cap config mismatch 21 vs 30; 3 early strategy_exit unexplained — flip blocked pending diagnosis). Flip runbook in the Phase 2 spec §4; first hook strategy's activation carries the one-cycle watch. Final review: `_bars_held` union-calendar fix, hook-glue guard, intraday lane gate, hold-cap promotion guard, replay recovery via partner leg (`aaac49b..ceeb8a0`) — the 3 early exits were the replay harness inheriting a closed sibling's signal, not a live-code defect. **F7 replay re-run (same run/dates, post-fix):** AGREEMENT 11/11 unchanged; live-only closes dropped from 26 (23 max_hold + 3 early strategy_exit) to 6 (all max_hold, hold_days 25–30 > the live 21-day cap, backtest exit later than live close in every case — the pure hold-cap mismatch, now guarded by `exit_hook_hold_cap_mismatch`); 0 early strategy_exit divergences — trade_seq 1073/1082/1086 now recover the correct partner-implied pair (`BBJP/AVEM`, `WES/TRGP`×2) and hold through their sampled dates like the backtest. Flip UNBLOCKED pending the operator's §4 runbook.

- **2026-08-28: per-bar exit hook — Phase 1 (backtest) landed.** `BaseStrategy.exit_hook` + `should_exit()`; `backtest/open_book.py` stepper (bracket → hook → time) used only by opt-in strategies, all others byte-identical (equivalence test); per-signal `hold_days` honored; `config_json.exit_hook/hook_exits`; `promotion_service` refuses `exit_hook` runs until `OPENCLAW_EXIT_HOOK_LIVE=1` (`exit_hook_live_disabled`). X1 got `z_revert`/`pair_decohered` exits; run #3 result: total Sharpe +0.30 (first positive run, vs. −0.24/−0.08 for runs 1/2), 1,069 hook exits (374 `z_revert` / 695 `pair_decohered`) at median hold 5 d, but LOW_VOL/TRANSITIONING/HIGH_VOL Sharpe all still trail their SPY benchmarks — still fails every promotion gate. Spec `docs/specs/2026-08-28-per-bar-exit-hook-spec.md`; Phase 2 (live `update_pnl` mirror) still owed.

- **2026-08-28 (later): X1-D1 applied — spread-implied per-leg stops (`7ee5ae9`); re-run `32ff3475` Sharpe −0.08 (from −0.24), still fails gates ⇒ X1 PARKED.** Stops = wider of spread-implied distance to |z|=4 and 2σ_leg·√hold (median 13.9 % from entry); exits now 975 time / 68 stop / 3 target; LOW_VOL −0.58, TRANS +0.16 vs bench 1.14. The spec's |z|≤0.5 reversion exit is not engine-expressible (no per-bar exit hook) — the remaining owed engine feature. Post-commit OOM under MemoryMax traced to in-process `backtest_panel.rebuild` (ERRORS.md).

- **2026-08-28: X1 pair ledger complete (156/156 weekly scans) + first `S_coint_pairs_sector_v2` backtest — FAILS gates as run, diagnosis = per-leg stops.** Run `655c4bdb` (2023-09-04..2026-08-27, tier_liquid): total Sharpe −0.24, 1,107 trades; LOW_VOL −1.74 / TRANS −0.03 / HIGH_VOL +1.82 (59 trades). 772/1,107 exits were the base-class 2×ATR per-leg stop (median 1.5 % from entry), 73 the intended time exit — the engine measured a per-leg stop/target system, not the spread trade (exit gap already declared in the strategy docstring). Operator decision X1-D1 (pairs-leg stop policy + ~10-min re-run) in `docs/superpowers/plans/2026-08-24-five-repo-adoptions.md`. `4dfe878`: relative `--strategy-file` no longer aborts at the loaded-from log line.

- **2026-08-24: five-repo diligence → 12 adoptions landed on main (`11011b4..8347511`).** Reviewed PyPortfolioOpt / QuantResearch / AutoHedge / KidQuant-pairs / ziplime against an audit of the sizing, research and backtest stack (report artifact 9e8ebbaa; plan `docs/superpowers/plans/2026-08-24-five-repo-adoptions.md`). Sizing: **P1** Ledoit-Wolf shrinkage (`execution/shrinkage.py`; `OPENCLAW_ASSET_CORR_LW` '0'/shadow/'1', .env=shadow soak; `strategy_similarity` stores `+lw_gamma=` on `trigger`; `orthogonalization.resolve_tangency_gamma` wired into the live tangency solve behind `OPENCLAW_TANGENCY_LW`, [0,1] clamp, default bit-identical), **P2** HRP shadow re-armed (`OPENCLAW_PYPORTFOLIOOPT_SHADOW=1`; ticker-row gross-scaling bug fixed; NEW method `hrp_strategy` with in-window ≥60-obs floor; first rows 08-24), **P3** `tail_sortino`/`cvar_5` per sleeve (mig 148, advisory). Research gates: **R1** benchmark-relative promotion leg — sleeve Sharpe must beat regime-conditioned SPY (`backtest/benchmark_baseline.py`, mig 149 `benchmark_sharpe`, twins value-synced; ⚠ enforced at candidate→live only — activation/eligibility assigners NOT wired, operator decision), **R2** pre-backtest factor prescreen (`backtest/factor_prescreen.py`; conservative: only `constant_output` hard-blocks today; aux-dependent strategies skipped), **R3** QuantStats tearsheets (`scripts/generate_tearsheet.py`; engine hook OPT-IN via `OPENCLAW_BT_TEARSHEET=1`, research candidates rendered out-of-band by the orchestrator), **R4** parity-oracle pattern applied to X1, **R5** paperhunter gate text fixed. Swarm: **S1** mandatory red-team gate on generated strategy code (`curators/strategy_redteam.js`, calibrated on a shift(-1) fixture; claude-bin de-privileged to claudebot when root), **S2** N-variant strategycoder tournament (flag `pipeline_config.tournament_variants`, default OFF; registry.py never mutated at runtime), **S3** non-veto promotion dissent (`curators/promotion_dissent.js`, mig 150 `promotion_dissents`, posts to #botjohn-log). Strategy: **X1** `S_coint_pairs_sector_v2` (registry-only) + weekly `pipeline/pairs_scanner.py` (industry buckets → EG both directions → BH-FDR q<0.10 with 2-scan persistence → OU half-life [5,30]d → per-round-trip ≥2× honest-cost gate; `data/derived/pair_ledger.parquet`, replace-on-rescan). New statuses `redteam_blocked` / `prescreen_failed` have no sweep/resurrect/dashboard consumers yet (follow-up). Data: `openclaw-fmp-profiles.timer` (deployed 08-23) had never fired — sector/industry NULL everywhere; one-off sweep run 08-24 (13,374 symbols); scanner falls back to `universe_config` + `data/.cache/fmp_profile.json`. statsmodels 0.14.6 pinned. Ops: `johnbot.service` runs as root (no `User=`) — root-spawned claude-bin calls must de-privilege (mastermind_code_review.js still has the gap).
- **2026-08-23 (later): FMP instrumented in `data_provider_health`.** Every FMP call path now records per-(endpoint, hour) outcomes: the JS collector (`store.recordProviderCall` through the pg pool — income-statement / ratios / key-metrics / balance-sheet / insider search / historical-price-eod / quote), the generated tool module `workspaces/<ws>/tools/fmp.py` (behind `backfillers/fmp.py`, `intraday_financials.py` and every sub-agent FMP tool), the insider `/latest` stream, the intraday earnings calendar, quotes, profiles, universe metadata and the backfiller insider walk. Classification is shared (`provider_health.classify_http`): a 402 'Special Endpoint' (tier-gated SYMBOL) and 404 are the provider answering correctly and count as success; quota 402 / 429 / 5xx / transport count as errors — otherwise the tile would cry wolf on ~30 preferred/warrant names per cycle. Same fix applied to the two remaining '402 = quota' sites (insider walk, generated `_get` → `FmpSymbolGated`, `refresh_fmp_profiles` no longer aborts the sweep on a gated symbol). New `system_checks` probe `fmp_provider_health` (pipeline): WARN ≥25% / FAIL ≥75% error ratio over ≥20 calls in 24h; WARN on weekday silence after the 16:15 ET collect. Dashboard `/api/data-health` shows FMP per endpoint.
- **2026-08-23 (later): `S_ast_fed_model` re-pointed to macro.parquet.** 10Y yield now `aux_data['macro']['DGS10']` (was `^TNX` in prices, frozen since 05-21) and the risk-free is the time-varying `DGS3MO` series (was a fixed 2%); both old inputs remain as fallbacks and `signal_params` records `bond_yield_source` / `rf_source`. Re-backtest (honest-cost harness, run `80a1a312`): total Sharpe 0.53 (was 0.62 on 07-29 with the 2% rf flattering 2023–25 excess returns); eligible regimes unchanged and strong — HIGH_VOL 1.00, CRISIS 0.81. Timer audit: the four new streams sit outside the 14:30→16:15 critical window (pre-open reference pulls 06:40/07:10; post-collect captures 17:00/17:45 after the 16:30 archive); none has a tier-1 acting adapter yet — `run_acting_ingest` will WARN if an acting strategy declares one.
- **2026-08-23: provider audit (OpenBB / ruflo fit review) → six pipeline defects fixed, four keyless data streams added.** Audit verdict: neither repo replaces FMP or Alpaca (OpenBB's FMP provider is FMP on the same `/stable/` key; no alpaca/polygon provider; AGPL-3.0, imports FastAPI on any fetcher import; ruflo's orchestration/"neural"/consensus layers are labels or stubs with a promo/upsell funnel — ignored). Defects found on our side and fixed: **D1** `collector.js` read every FMP 402 as daily quota — it is also the per-SYMBOL "Special Endpoint" refusal for `.PR*/.WS/.U` names, so the fundamentals phase halted at ~30 tickers every cycle since ≥08-13 (`_classifyFmpError`, pipeline_runs-backed freshness scope, per-cycle cap `fmp_fundamentals_max_per_cycle`=1500); **D2** `earnings.parquet` frozen since 08-06 on yfinance 429s while FMP `/stable/earnings-calendar` worked — re-sourced (94,702 → 98,613 rows, 2,465 actuals filled); **D3** in-cycle options flush SIGKILLed daily since 08-11 at parquet_store's flat 30 s timeout (2.8 GB orphan `.tmp`) — per-op timeouts, in-cycle fetch opt-in (`OPENCLAW_COLLECT_OPTIONS_INCYCLE=1`), the 16:30 archive now writes `data_coverage`; **D4** insider phase re-walked all ~11.8k tickers daily (date-range freshness can never fire) — daily global `/insider-trading/latest` → master (`ingest_insider_stream.py`) + weekly per-symbol reconciliation (`fmp_insider_refresh_days`=7, `fmp_insider_max_per_cycle`=2000); **D5** every `/api/v3` caller retired/ported (universe sync retired; `earnings-surprises` → `/stable/earnings`); **D6** 8-K scan cap 50 → 400, weekly `openclaw-edgar-shares` + `openclaw-fmp-profiles` timers. New streams (all verified live, no key — `docs/reference/free-data-streams.md`): **FINRA** biweekly short interest → `short_interest.parquet` (1.27M rows since 2024-01, timer 07:10 ET), **Nasdaq** earnings calendar → `earnings_calendar_nasdaq.parquet` (06:40 ET), **CBOE** delayed chains with OPEN INTEREST + greeks → `cboe_chains/date=*.parquet` + `cboe_chain_aggregates.parquet` (17:00 ET; first capture 553 underlyings / 673k contracts), **FRED keyless + NY Fed** rates/credit/FX → `macro.parquet` (4 → 29 series, 17:45 ET). Also: `sync_data_ledger._stats` now streams through DuckDB (5.3 GB → 119 MB; the old read OOM-killed at 48M options rows) and provider labels corrected (`polygon` → `alpaca`). ⚠ First production cycle for D1/D4 is Mon 08-24 16:15 ET — read the phase counters (`tier-gated`, `no-statement`, `Insider walk: N of M`).
- **2026-08-23: earnings.parquet refresh re-sourced to FMP `/stable/earnings-calendar` (D2 — master frozen since 08-06).** `src/ingestion/ingest_earnings_master.py` (collector.js `runEarningsMasterMerge`, daily) fed BOTH the forward calendar and the actuals from yfinance, which has returned "Too Many Requests" on every run since 08-06 — every earnings consumer (engine `earnings_dte`, S_earnings_sue_pead, S_pre_earnings_vol_runup, S_ast_earnings_announcement_premium) was reading a 17-day-stale file. Its docstring's "both FMP earnings endpoints 403" was only true of the dead `/api/v3/` paths; `/stable/earnings-calendar` (bulk, ≤7-day span per call) and `/stable/earnings?symbol=` return 200 on the current key (probed 08-23). New PRIMARY path: one bulk sweep per run over [today−10d, today+120d] in contiguous 7-day windows (19 calls, 1 s pacing, 429 → Retry-After backoff, 30 s timeout; a failed window is counted and skipped, FMP is "unavailable" only when the key is missing, EVERY window fails, or nothing lands in-universe), rows restricted to master tickers ∪ `universe_config.active` (∪ `--universe-file`), merged by `merge_rows` (exact (ticker,date) match, else ±1d bmo/amc skew; NaN-fill in place; an UNREPORTED forward row also takes the revised consensus estimate; a reported row's estimate is frozen; unmatched rows appended; nothing dropped — the master's 101 legacy duplicate keys survive). yfinance demoted to FALLBACK (taken only when FMP is unavailable; unchanged `tickers_needing_actuals` → `get_earnings_history`). Local `earnings_calendar.parquet` forward merge kept (it is a file read, so the master no longer depends on yfinance succeeding; `ingest_earnings_calendar.py` itself is untouched). Write goes through `parquet_store._file_lock` + `_atomic_write` (tmp + os.replace); `--dry-run` now runs the REAL path — fetch, merge, tmp serialisation — and skips only `os.replace` (the old dry-run skipped the fetch, i.e. was a different program). Counters printed every run (`fmp_rows_fetched / fmp_windows_ok / fmp_windows_failed / fmp_rows_in_universe / rows_new / rows_updated / actuals_filled / tickers_touched / master_max_last_updated`); exit 1 only when FMP AND the fallback both fail or the merge would shrink the master. New flags `--lookback-days`, `--horizon-days`, `--pace`, `--universe-file`. Tests: `tests/ingestion/test_ingest_earnings_master_fmp.py` (19, no network) + the legacy file re-pinned to a fixed clock and the fallback path. 08-23 dry-run against the live master: 11,968 FMP rows / 19 windows ok / 5,050 in-universe (6,917 foreign-OTC dropped) → +2,660 new rows, 2,225 updated, 554 actuals filled, 4,360 tickers touched, 94,702 → 97,606 rows, 24.6 s. **OWED (operator): first real run with `--lookback-days 20` so reports between the 08-06 freeze and the default 10-day window are back-filled; the real write has NOT been executed.**
- **2026-08-22: multi-bars batching extended to the backfiller, the 30-minute intraday ingest, and the same-day close proxy.** New shared helper `src/pipeline/alpaca_multibars.py` (Python twin of `collector.fillPricesAlpacaBatch`: grammar pre-filter via `partition_symbols`, chunk = `clamp(8000 / (trading_days × bars_per_day), 1, 200)`, page-token loop, `invalid symbol: X` drop+retry, `MultiBarsError` on anything else with `auth_error` for rc=2). (1) `backfillers/universe_prices.fetch_many_ticker_year(symbols, year)` → `{symbol: DataFrame | Exception}`; the 5y driver `_run_prices` is now year-major over Redis-eligible symbols in batches of 200 with the per-(symbol, year) audit/validate/overlap/promote body extracted unchanged into `_process_price_chunk` — a full daily year is ~31 symbols per request, so 5 000 tickers × 5 years goes from 25 000 CLI processes to ~800; chunk failures fall back to `fetch_ticker_year` per symbol. Verified live: frames + SHA identical to the per-symbol fetch for AAPL/BRK-B/MSFT, empty/unknown/malformed cases match. (2) `ingestion/ingest_prices_30m_alpaca.fetch_many` replaces the per-ticker direct-HTTP `/v2/stocks/bars` loop (no retry) with one `multi-bars --timeframe 30Min --adjustment raw` request per ~200 symbols × 3 days; `backfill()` uses it, `fetch_ticker` is a thin wrapper. Verified live: 40 tickers in 1.0 s, rows identical to the master parquet for the window. (3) `close_proxy_snapshot._CHUNK` 50 → 150 (matches the collector's multi-snapshots chunk; ~100 → ~35 calls on the 15:00 hot path; poison-eviction path unchanged). The intraday regime prefetch (`refetch_prices.py` → `--intraday-snapshot` / `--prices-only`) was already batched and already on the new daily path. Tests: `tests/pipeline/test_alpaca_multibars.py` (12), new cases in `tests/scripts/test_backfill_universe_prices.py` (driver tests now mock `fetch_many_ticker_year`) and `tests/ingestion/test_ingest_prices_30m_alpaca.py`.

- **2026-08-22: Daily price fill batched through `alpaca data multi-bars`** (`collector.runHistoricalPrices` → `_runHistoricalPricesBatched` → `fillPricesAlpacaBatch`). Gap detection runs for every ticker first, items are grouped by identical (from,to) range, symbols chunked at `min(200, 8000 / trading-days)` so each request is ≈ one 10 000-point page, `next_page_token` followed, `bars[api]` fanned back to `store.upsertPrices` keyed by the universe ticker (BRK-B→BRK.B bridge kept). Live contract encoded: unknown well-formed symbols are silently absent (→ 0 rows, as today); malformed forms (`^DJI`, `CL=F`, `AUDUSD=X`, dash crypto) are pre-filtered to the per-ticker path (existing warn/skip) and an `invalid symbol: X` reject drops X and retries the chunk; any other chunk failure falls back to per-ticker for that chunk only. Per-ticker `logRun`/progress/1000-ticker mid-flush semantics unchanged. Kill switch `OPENCLAW_PRICES_MULTI_BARS=0` restores the old loop verbatim. Measured live (writes stubbed): 604 tickers in 1.74 s / 8 calls (2.9 ms/ticker) vs 80.6 ms/ticker per-ticker — the 5 073-ticker daily fill drops from ~6.8 min of CLI overhead to ~15 s. Tests: `tests/pipeline/test_fill_prices_alpaca_batch.test.js` (12). Not touched: `backfillers/universe_prices.py` (same shape, separate ad-hoc tool).

- **2026-08-22: Alpaca CLI audit (github.com/alpacahq/cli v0.0.9 → v0.0.13) + call-site fixes.** Full inventory of every CLI invocation (docs/reference/alpaca-cli-integration.md rewritten as a live reference). Verified v0.0.13 side-by-side (identical stdout on every read path we use; only delta is pretty-printed stderr error JSON, which every wrapper already parses whole) — binary built, install left to the operator. Fixed: (1) `alpaca order get <id>` POSITIONAL form (rc=1 "--order-id required") at `alpaca_executor._poll_crypto_fill` ×2 and `_wait_for_fill` — since c03291a (05-26) every direction-flip's matched open was skipped ("flip_close did not fill within 15s") and no crypto protective stop was ever attached; (2) `order list` without `--limit` (silent 50-row default on a 238-order book) in `system_checks/checks/broker.py` and `afterhours_tp.reconcile_afterhours` → keyset-paginated `--limit 500 --direction asc --after-order-id`; (3) `regime_liquidator._submit_extended_hours_close` now passes `--client-order-id`; (4) all five shared wrappers (`alpaca_cli.js runAlpaca`, executor/liquidator/replace_stop/stop_reattach `_run_cli`) append `--quiet` (stderr stays a pure JSON error document) and `--timeout <subprocess−1>` and surface `auth_error` (rc=2). New: `src/agent/tools/mcp/alpaca.js` generates a READ-ONLY `tools.alpaca` module (multi-bars batching, `--nested --limit 500` keyset order reads, next_page_token pagination, cycle cache; no order functions) — sub-agents previously had an empty HTTP stub while the tool-guide named Alpaca P1; tool-guide updated. Tests: `tests/execution/test_alpaca_cli_contract.py`, `tests/agent/test_alpaca_tool_module.py`, broker pagination + JS wrapper cases (167 py + 7 js green). Owed: multi-bars batching of the per-ticker daily price fill (~5 180 processes/day → ~30 calls), executor early-abort on `auth_error`, operator `go install …@v0.0.13` + johnbot restart for the JS wrapper.

- **2026-08-22: Conviction gate → per-regime MINIMUM ACTING-STRATEGY COUNT** (operator directive). The sizer's ticker-selection gate is no longer the corr-adjusted cumulative-Sharpe floor; a ticker is taken only when at least `min_acting_strategies` (regime_sizer_params, migration 147, integer [1,10], default 1) DISTINCT strategies act on it in its NET direction (sign of S_adj, falling back to the naive Σweight×direction). Opposing-side contributors do not count toward the minimum (they still net inside S_adj); repeated cadence-window rows of one strategy count once; synthetic `__close_orphan__` markers never count. At 1 the gate block is skipped — byte-identical to the pre-change book (all four regimes had `min_corr_cum_sharpe` at 0.0, i.e. the old gate was already fully open). Everything else is untouched: S_adj still drives the sizing weight and the `(|S|+1)` per-ticker cap, dust floor, asset-corr de-gross, net cap, λ, brackets; the zero-conviction flatten keeps its wiring (a HEALTHY carried set fully rejected by the NEW gate flattens when `OPENCLAW_FLATTEN_ON_ZERO_CONVICTION=1` — which IS set live — so a very high setting in a thin regime liquidates the book exactly as a high floor would have; never on intraday redeploy). Code: `regime_blended_sizer._resolve_min_acting_strategies` / `_net_signs` / `_acting_counts` replace `_resolve_min_corr_cum_sharpe`; `corr_cumsharpe.live[...]` diag line now carries `acting_min= acting_dist= acting_keep=`; emitted orders carry `acting_strategies` (None on closes). `strategy_similarity` report keep-sets + `keep_curve_backtest_matrix` (now per min 1..10) use the same rule so no diagnostic reads the retired floor. Column `min_corr_cum_sharpe` is retained (append-only DB) but unread/unwritten; the PUT route rejects it with a pointer. Dashboard (:3000 Strategies page): the same four Conviction Gate slider cards now bind `min_acting_strategies` with `min=1 max=10 step=1` ("1 · any" … "10 · consensus"); position tiles gain an `acting = N` badge (distinct same-side contributors from the sizing stamp) and the `|S_adj|` tooltip now says it sizes rather than gates. Migration 147 applied live (all regimes = 1; CHECK verified; idempotent re-run ok); the live wrapper loads params via `SELECT *` so the column flows without further wiring. Tests: `test_resolve_min_acting_strategies.py` (replaces the floor resolver test; 8: clamps, fallbacks, distinct/net-direction counting, retired symbol absent), `test_corr_cumsharpe_integration.py` rewritten (min 2 keeps two confirmers, min 3 drops them, opposing side excluded, repeated rows count once, min 1 open + S_adj cap unchanged + order stamp), `test_sizer_flatten_zero_conviction.py` fixtures moved to `min_acting_strategies` (5 gates every single-strategy fixture ticker, 1 gates none) — 28 green on the three suites. `test_regime_blended_sizer.py::test_intraday_single_survivor_capped` re-expressed: its nine 0.5-Sharpe dummy tickers were silently gated by the LIVE DB's `pipeline_config.min_corr_cum_sharpe` (the helper never stubbed the similarity matrix); now STX carries two rho=1 contributors (S_adj≈3.5, tangency PSD ridge tolerated), `min_acting_strategies=2` drops the single-strategy dummies, the matrix is stubbed, and the cap is pinned against the order's stamped S_adj. Full `tests/execution` run: 1733 passed / 6 failed — 4 pass in isolation (known full-suite cross-contamination), 1 was the test above, and `test_sizer_class_consolidation.py::test_headroom_scales_options_down` FAILS ON PRE-CHANGE HEAD TOO (verified in a throwaway worktree: option scaled to $25k headroom vs the $10k the fixture expects — pre-existing, not touched). Dashboard restarted (`systemctl --user restart johnbot` — :3000 is served inside it) and exercised: GET returns `min_acting_strategies`; PUT 0/11/2.5/retired-key → 400; PUT 2 → DB 2 → restored to 1.
- **2026-08-22 (iii): Agent models → Claude 5** (operator directive "upgrade all agent models to the latest versions"). Every Sonnet 4.6 seat → `claude-sonnet-5` (BotJohn orchestrator/direct chat/maintenance runs, TradeJohn/TradeDesk, PaperHunter, StrategyCoder, swarm defaults, git_strategy_ingest extractor, mastermind `debator`/`extractor` tiers, context-management default, batch fallback); every Opus 4.7 seat → `claude-opus-5` (`[1m]` where it was 1M: MastermindJohn `judge`/`synthesizer` tiers, corpus-curator, `_opus_oneshot`, universe_recommender, the MasterMindJohn chat service — unit `Environment=MASTERMIND_CHAT_MODEL=claude-opus-5[1m]` updated in docs/systemd AND the live /etc unit, daemon-reload + restart, boot log confirms `model=claude-opus-5[1m]`; `curator_runs.model` label and the Discord persona display model → `claude-opus-5`). Haiku 4.5 (`claude-haiku-4-5-20251001`) stays — it is still the newest Haiku (compaction/summarizer model). All three target ids were probed FIRST through the exact production path (`claude-bin` as uid 1001, `HOME=/home/claudebot`, `-p … --output-format json --model X`): each returned `OK` in ~8s with `modelUsage` keyed by the requested id. Pricing tables refreshed to the published list: Sonnet 5 $2/$10, Opus 5 $5/$25, Fable 5 $10/$50, Haiku 4.5 $1/$5 (`scripts/token-budget.js` TOKEN_COSTS + RATE_LIMITS; `src/budget/batch.js` PRICING standard/batch 2.0/10.0 · 1.0/5.0); `models.js` CONTEXT_LIMITS gains the 5-family (bare ids 200k for compaction — conservative, `[1m]` variants 1M). `agent_registry.model` rows (botjohn was a stale `claude-opus-4-6`, mastermind `claude-opus-4-7`, tradedesk `claude-sonnet-4-6`) aligned to the new ids (label column only; nothing reads it for routing). Docs: /root/CLAUDE.md registry + subagent-types line, openclaw CLAUDE.md, ARCHITECTURE.md table, workspaces/default/agent.md, subagent-types.json description. Tests: `test_resolve_model` expectation moved to `claude-opus-5[1m]` (11 green); resolver verified live: synthesizer/memo_writer → `claude-opus-5[1m]`, critics → `claude-sonnet-5`, paperhunter fallback → `claude-sonnet-5`. johnbot restarted (user scope). NOT changed: Fable 5 (`claude-fable-5`, 2× Opus 5 price) is available but was not adopted for any seat — operator call; `CURATOR_OPUS_MODEL` / `MASTERMIND_CHAT_MODEL` env overrides still work for a per-seat pin. Note Opus 5 / Sonnet 5 default `effort=high` on the API/CLI; the spawn sites that pass `--effort` explicitly (botjohn-direct medium, run_maintenance high, swarm per-type) are unchanged.
- **2026-08-22 (ii): Activation sliders apply at the NEXT DAILY CYCLE + dashboard stale-tab guard** (operator directive). (1) The Strategy Activation sliders (`pipeline_config.strategy_activation_min_sharpe` / `_min_trades`) were only applied by the Mon 00:00 ET `weekly_live_sharpe.js` run and the Sunday finale — the daily compute chain read `strategy_regime_params.eligible` (engine `is_eligible`, sizer calendar-edge clause) and `strategy_weights_by_regime` (sizer weights) but never re-derived them, so a slider moved Tuesday traded the following Monday (the card even said so). New FIRST step `activation` in every compute chain (`daily-cycle.js` STEPS_IN_ORDER / STEP_SCRIPTS, `cron-schedule.js` dispatch lists for same-day 15:00 ET, EOD 16:15 ET and legacy 15:10 ET compute, Python twin `pipeline_orchestrator._build_steps`) → `src/execution/activation_apply.py`: honours `OPENCLAW_ACTIVATION_ASSIGNER=1`; compares each slider row's `updated_at` with the new marker `pipeline_config.strategy_activation_last_applied` (stamped by `activation_assigner.stamp_last_applied` after every clean non-dry-run `--all`, with a new `--trigger` label: `weekly_cron` / `sunday_auto_approval` / `daily_cycle` / `manual`); when a slider is newer (or no marker exists) it runs `activation_assigner --all --notify --trigger=daily_cycle` then `strategy_weights --rebuild --trigger=activation_slider` with `OPENCLAW_AUTO_DEMOTE=0` forced for that invocation (weights-only — the registry auto-demote chain keeps its weekly cadence; the rebuild is REQUIRED because a newly-eligible regime has no weight row ⇒ weight 0 ⇒ never sized). No slider change ⇒ no-op in <1s. Failure ⇒ rc 1 with the marker NOT advanced (retried next cycle); `daily_cycle_node.js` gives `activation` the same never-abort exemption as `sentiment` (alert + stderr persist, chain continues) — load-bearing because `OPENCLAW_STRICT_EXIT_CODES=1` is live and rc=1 aborts every other step. `--dry-run` (and `PIPELINE_DRY_RUN=1`) = pending check only. Dashboard: both activation GETs now return `pending` / `last_applied` / `applied_at`; the card status reads "⏳ set … · pending — applies at the next daily compute (15:00 ET)" or "✓ applied … via <trigger>"; PUT confirmations say the same; `pipeline_config` descriptions refreshed on upsert. The server still NEVER runs the assigner without `--dry-run` (Phase 1e guard intact) — the apply lives in the cycle, not the HTTP handler. Live dry-run on 08-22 19:30Z: `strategy_activation_min_sharpe=1` (set 19:00Z by the operator) vs no marker ⇒ PENDING ⇒ would run assigner + rebuild; the Mon 00:00 ET weekly run will apply + stamp first, then Monday's 15:00 ET step sees marker > slider and no-ops. Tests: `tests/execution/test_activation_apply.py` (15: pending logic incl. min-trades, missing marker, fail-safe on read error; apply order, weights-only env, skip-on-assigner-fail; main gate/no-op/dry-run/force/rc≤1), `TestStampLastApplied` in `test_activation_assigner.py` (51 green), `test_daily_cycle_graph` (14 nodes), `test_daily_cycle_node` (+2: activation rc=1/rc=2 in strict mode → warn, no throw), `test_pipeline_logging` maps, `test_resolve_script` + Python twin parity green. (2) Stale-tab guard: the "corr cum sharpe has been retired" text the operator saw under the OLD Sharpe-floor sliders was a browser tab loaded BEFORE the 15:28Z johnbot restart — its pre-change JS kept PUTting `min_corr_cum_sharpe` and rendered the new server's 400; the served page (:3000/:80) was already the new build and :7870 has no conviction sliders. server.js now stamps `DASHBOARD_BUILD` (its own mtime at boot) into the page (`window.__DASHBOARD_BUILD__`), returns it as `X-Dashboard-Build` on every `/api/*` response and at `GET /api/dashboard-build`; the page polls it every 60s and checks it on every slider PUT — a mismatch shows a banner and reloads the tab after 8s. johnbot restarted (user scope) and verified: token present in page + header + endpoint, activation GETs report `pending:true`, zero "next weekly refresh" strings served.
- **2026-08-18 (ii)**: `stop_reattach.py` open-order coverage reads now keyset-paginate (`--after-order-id`, `--direction asc`, dedupe by id, 40-page bound) instead of a single `--limit 500` page — `_fetch_open_orders()` replaces `_OPEN_ORDERS_ARGS`/`_warn_if_capped` at all four call sites (active-stop index, TP-cover map, cancel-stops-for, orphan-stop sweep). First-page failure keeps the old "listing unavailable" semantics; a later-page failure/stall returns partial rows with a loud truncation warning (worst case == old behavior). The per-ticker bracket-history read (`--status all --nested`) additionally gets `--symbols <ticker>` so its 500-row budget is spent on the target ticker, not the account-wide newest-500 window. Motivation is robustness, not a live truncation: the current book is 153 open orders (one page) — the earlier "cap exceeded every pass" claim came from the line-count error corrected in the previous entry — but coverage correctness should not depend on the book staying under any fixed row count (08-12 proved what a silently-truncated naked audit does at the 50-row default). Tests: `tests/execution/test_stop_reattach_pagination.py` (5: page args, cursor advance + termination, first-page fail, partial-on-later-fail, stall guard); live read-only run against the real book: ok=True rows=153 unique_ids=153 in 0.1s; full `--dry-run` pass green. Residual that remains open: `system_checks/checks/broker.py` still lists orders at the 50-row default. from the "trading but not live" / regime-inactive-positions diagnosis. **(A) Auto-demote now syncs the registry** — `lifecycle.auto_demote_negative_sharpe` wrote only the manifest, so every auto-demotion of a promoted strategy stranded a stale `strategy_registry.status='approved'` row (the engine's trade gate; 15 accumulated by 08-18, 11 from the 08-15 sweep). New `_sync_registry_demotion` closes the gate to `pending_approval` (mirrors `REGISTRY_STATUS_FOR[candidate]` in promotion_service.js) BEFORE the manifest transition; a registry-sync failure skips that sid's manifest demotion (registry-first invariant — the only reachable half-state is the safe "not trading" badge direction). One-time cleanup: the 15 stranded rows updated approved→pending_approval; dashboard drift now 0/0. **(B) Orphan-close cancel guard un-truncated** — `alpaca_executor._cancel_open_equity_orders` (and the crypto sibling `_cancel_open_crypto_stops`) listed open orders unfiltered, i.e. the CLI's default 50-row page; once the open book outgrew a page (3,696 open orders) the target's OCO legs were never in it, nothing got cancelled, and every `__close_orphan__` close 403'd "insufficient qty available (available: 0)" (261/356 rejected 08-14, 89/92 on 08-17 — why 08-13 LOW_VOL-wave positions from TRANS-ineligible strategies kept carrying across the regime). Listing now `--symbols <ticker> --limit 500` (server-side filter is the load-bearing part; verified the CLI accepts the slashed crypto form). CORRECTION (same day): the "3,696 open orders / 3,541 positions" figures first reported were JSON pretty-print LINE counts, not object counts — the real book is 153 open orders / 154 positions (`--jq length`). The root cause stands unchanged: the default page is 50 rows and the book has been 150–350+ orders since 08-12, so the unfiltered listing still missed most tickers' OCOs. Tests: `tests/strategies/test_auto_demote_registry_sync.py` (new, registry-first + failure-skip + SQL-shape pins) and 3 pins in `test_rth_close_cancels_resting_orders.py` (2 pagination + 1 settle-race); executor suites 680 green, lifecycle/cadence 64 green. **Live E2E (2 stuck orphans, RTH 08-18)**: first pass on GM exposed a THIRD defect — the settle-poll tracks the cancelled OCO *parent* to terminal, but the broker releases the qty hold only when the *leg* finishes cancelling, which lagged 19s at the open (cancel ACK 13:30:09 → close 403 13:30:22 → leg release 13:30:28). Hardened `_cancel_open_equity_orders` with a bounded (≤10s) poll of the position's `qty_available` — the close's actual precondition. Re-verified one-shot on GMF (stuck since 08-14): OCO cancelled → hold released ~2s → close filled @155.99 → position gone, 0 open orders. GM closed on the follow-up pass (2/3 filled immediately, last share on the working day order). NOTE: these two verification closes ran through `_rth_position_close` directly, so no `alpaca_submissions` audit rows exist for them (the executor's caller writes those). ⚠ Residual flagged: `stop_reattach.py`'s single-page coverage read (bounded fix shipped later same day — see next entry; the "cap exceeded every pass" claim was based on the mis-count above and was wrong at the current 153-order book). Pre-existing debris also noted: `tests/strategies/test_oxf_aroon_breakout.py` + `test_oxf_hull_ma.py` fail collection since the 08-15 auto-research commit removed their strategy modules.
- **2026-08-13 (iii): Calendar-edge mints recorded in NON-qualifying regimes** (operator directive, completes the calendar-edge design). A calendar strategy whose window opens during a regime it is not eligible for previously minted nothing — the edge was lost even if a qualifying regime arrived a day later. Now the engine regime gate RUNS-THROUGH calendar_edge strategies (log: "recording window-open signals anyway"), widening active_in_regimes so should_run cannot veto; the mint lies dormant and sizes only in a qualifying regime within its cadence window. `_REGIME_SCOPE_CLAUSE` restructured into two disjoint branches: rebalancer signals = current-regime mints only AND NOT calendar-edge; calendar-edge signals = mint regime irrelevant, `strategy_regime_params.eligible` for the current regime is the sole gate. The negation is load-bearing: `load_current` has NO eligibility filter (72 weighted vs 31 eligible in TRANS), so a same-regime dormant mint would otherwise trade immediately in the unqualified regime. 16 tests green + live read-only query check.
- **2026-08-13 (ii): Calendar-edge signals port across regime flips** (operator directive, refines the cadence-reset entry below). Calendar-timing strategies (turn-of-month, expiration week, Monday effect, same-month seasonality, July/April/December annual windows) cannot re-mint off-window, so the strict `regime_state = current` scoping would have orphaned their signals on every flip. New `BaseStrategy.calendar_edge` attr (True on those 7); `run_strategies` stamps `signal_params.calendar_edge` at mint; both sizer loaders now share `_REGIME_SCOPE_CLAUSE`: keep current-regime signals OR calendar-edge signals from another regime **while the strategy is eligible in the current regime** (`strategy_regime_params.eligible`) — i.e. a calendar signal holds through possibly multiple regimes within its cadence window as long as each regime is within the strategy's qualification standard. Rebalancers stay strictly scoped (they re-mint on flip day). Live read-only query check green; 15 tests in test_cadence_reset_regime_flip.py + 89 adjacent green.
- **2026-08-13: Regime-flip cadence reset — full book re-mint on regime-change days** (operator directive). On the day the regime-of-record changes, the new regime's book was being built only from strategies whose rebalance calendar happened to land on the flip (08-12: 7 of 31 TRANSITIONING-armed strategies emitted; monthly/weekly rebalancers with weights 1.0–2.1 sat silent mid-cycle, and their prior signals were tagged to the old regime). Three-part fix: (1) `engine._stamp_cadence_reset_on_flip` compares today's regime against the regime of the most recent persisted signal set and stamps `regime['cadence_reset']=True` on mismatch — covers both lanes (EOD signals step and the intraday-redeploy signals fragment) since both run the engine; fires exactly once per flip (the flip-day signals re-tag the regime). Kill switch `OPENCLAW_CADENCE_RESET_ON_FLIP=0`; manual force `OPENCLAW_FORCE_CADENCE_RESET=1`. (2) `StrategyBase.cadence_reset` + 24 rebalance-calendar gate sites OR the flag into their month/week/fortnight boundary checks so every eligible rebalancer emits same-day; timing-edge strategies (turn-of-month, expiration week, Monday effect, July/April/December annual windows, same-month seasonality) deliberately excluded — their calendar IS the signal. (3) Both sizer active-window loaders (`_load_active_window_signals`, `_load_approved_carried_signals` + its weights-gap audit) now add `AND regime_state = %s`: a signal minted under another regime can never feed the current regime's book; cadence persistence restarts from the flip-day signal_date. 13 new tests (`tests/strategies/test_cadence_reset_regime_flip.py`) + 89 adjacent sizer/engine tests green.
- **2026-08-12: Single λ everywhere + per-ticker cap gains a +1 floor** (operator directives). (1) `position_sizing_lambda_intraday` RETIRED — `_load_lambda` reads the dashboard-configured `position_sizing_lambda` for every lane (the 08-12 LOW_VOL→TRANSITIONING intraday redeploy sized the whole book at the stale 1× intraday value, half the intended gross); `OPENCLAW_INTRADAY_REDEPLOY=1` now carries lane semantics only (flatten exclusion, thin-set abort, top-up-lane exclusion, log-spam guard). The old pipeline_config row may linger; nothing reads it. (2) Per-ticker cap is now `0.10 × (|gate_net_sharpe|+1) × λ × NAV` — at TRANSITIONING breadth (28 names, median S_adj 0.27) the pure-|S| cap bound gross to ~40% of λ·NAV; the +1 floor gives every name ≥0.1·λ·NAV headroom so normalization can reach λ·NAV on thin-breadth days while still bounding outliers. Implemented as (|S|+1), NOT literal |S+1|: gate_net_sharpe is signed and |S+1| would collapse the cap exactly on high-conviction shorts. Shadow diagnostic mirrors the formula; 55 sizer tests retuned/green. Same day, earlier: stop_reattach open-order reads paged at the 500-row CLI cap (`4c32efe` — the 50-row default page made 182 resting OCOs read as ~140 naked positions) and `run-graph.js` exits 1 on aborted graph runs (the handoff OOM rc=137 had reported "completed exit_code 0"); periodic co-tenant memwatch in the handoff step (`59a2e1d`).
- **2026-08-07: Conviction-flatten re-armed in the same-day lane; S_adj correlations moved to backtest source; parity audit + determinism fixes** (operator directives). (1) `051972f` — the 07-29 same-day pivot had silently disarmed `OPENCLAW_FLATTEN_ON_ZERO_CONVICTION` (lane predicate admitted only the EOD-register flow); 08-06's fully-gated 327-signal day held a 7-position ~$48k book as a result. Lane test now spans both daily signal-target lanes; intraday-redeploy/thin-set/outage-day exclusions unchanged. (2) `e8d723b` — `strategy_similarity` gains a backtest source (spec 2026-08-05 §3+§4): PnL-corr leg from `strategy_backtest_trades` primary runs (10y, all 4 regimes; CRISIS had ZERO live coverage), co-firing overlap live-where-observed else universe-intersected backtest overlap; `--shadow` diff CLI; rebuild wired as the LAST step of the Saturday finisher + weekend-sat maintenance; cutover armed via `OPENCLAW_SIMILARITY_SOURCE=backtest` (inert until the next scheduled rebuild — all run outside trading hours). Shadow gates passed: signed mean Δρ on live pairs +0.119/+0.003/+0.101 (more conservative), CRISIS 903/903 pairs gain real ρ, keep/drop delta at current floors ≈ nil. (3) `a0bb5fe` — regime-agnostic Sharpe override no longer drops per-regime backtest cadence (S12_insider was the live victim); tier-2 shape normalized. (4) `1acb7eb` + `docs/specs/2026-08-07-backtest-live-signal-parity-spec.md` — backtest≡live audit verdict NOT-same-program (25 divergences); shipped the surgical fixes (aux anchored to run_date, two hash-order strategies made deterministic, wall-clock volume cutoff anchored, options-aggregates staleness loud + opt-in refusal, PYTHONHASHSEED=0); structural unification phased for operator scheduling (baselines reset each phase).
- **2026-07-23: Asset-eligibility execution gate in the sizer + tier_liquid requires fractionable** (operator directive). (1) `regime_blended_sizer._apply_asset_eligibility_gate` — final authority at order emission (`_emit_orders_from_targets`, so it covers the sharpe-cadence AND zero-conviction-flatten paths): positions are only allowed in equities that are `active + tradable + easy_to_borrow + fractionable` per `alpaca_tradable_universe`. Per ineligible ticker: not held → open blocked; opposite-sign target → zeroed (close leg survives, re-open leg dies); same-sign growth → capped at held size; reductions/closes NEVER touched (orphan closes are structurally out of reach — they arise from broker positions absent from targets). Unknown symbol = fail-closed; whole-lookup failure = fail-open with a loud warning (a DB blip must not brick the trade step). OCC option symbols + crypto pairs out of scope. `OPENCLAW_SIZER_ASSET_GATE=0` disables. 11 tests (`tests/execution/test_asset_eligibility_gate.py`). Spawn-per-run — live at the next trade step, no restart. (2) Earlier same day (`f18d764`): `universe_default.liquid_tradable` (and therefore `tier_liquid`'s liquidity leg) now requires `fractionable` — tier_liquid 5,255→5,011 at the 07-22 snapshot; 49 non-fractionable R3000 members remain via the index union (nesting invariant). The sizer gate is the execution-side backstop for those + sp500/r3000/no_otc universes.
- **2026-07-17 (simplification wave 2)**: cleared the entire pre-existing test backlog and reorganized the suite. (1) 14 catalogued pytest failures fixed by 5 parallel agents — every one traced to a cited superseding commit (option-expiry calendar rot, intraday-HMM feature set v2->v3, sentiment writer refactor, reaped/never-registered fixtures), zero module bugs, 101 tests green. (2) Strategies hygiene: removed implementations/decommissioned/ + 2 never-registered orphans; KEPT strategy_template_base (live StrategyCoder code-gen contract) and restored generate_missing_requirements.py (prompt-wired). (3) Dead-provider references (Polygon/Massive) retired from live UI strings + current-tense comments (data_tier_filter routing deferred — live logic). (4) **Near-miss caught**: the Oxford re-hearing manifest write used the wrong history schema ({at,from,to}), which threw in LifecycleStateMachine.from_manifest() — a starve-every-strategy failure mode; caught before the 20:15 UTC EOD (only live from_manifest caller in the window; fleet uses a separate resolver). Fixed the 14 rows (34ff920) AND hardened from_manifest to tolerate/normalize malformed history rows instead of throwing (045aed9, TransitionEvent.from_row + test). (5) Folded 387 py + 25 js tests from the flat ~400-file tests/ root into src-mirroring subsystem dirs (a07ad5d), with all path shims (parents[1]->[2], dirname chains, __dirname fixture refs) bumped. Gates: collection 3,253; execution 1,410; strategies green; JS 222/222.
- **2026-07-17 (later): Oxford re-hearing + branch disposal** (operator directive). History correction: all 30 oxfordstrat strategies WERE developed and landed 06-16 (6ebe59c) — 8 live, 8 merit-deprecated, 14 reaper-ejected 07-14 on the >21d rule, evaluated entirely on the pre-backfill panel. The 14 restored as candidates (dd857d3): files from main history (byte-identical to branch), manifest under lock with FRESH state_since, registry deprecated->pending_approval + lifecycle_events audit rows. New `scripts/backtest_ids.js` (explicit-id backtest driver, fleet discipline); detached run queued behind the fleet (`logs/oxford_rehearing_backtests.log`) to write fresh canonical rows before weekend gates. Standard promotion gates decide their fate — nothing bypassed; the reaper re-ejects any that fail again after 3 weekends. Branches: feat/ensemble-exit-tdom DISCARDED (verdict robust-NULL salvaged to docs/archive/reports/2026-06-22-ensemble-exit-tdom-verdict.md + spec/plan), feat/oxfordstrat-strategies redundant; both tips kept as archive/* tags, branches deleted local+remote. Branch list is now just main.
- **2026-07-17: Repo-wide cleanup campaign** (operator directive: public-quality, clone-replicates-VPS, MIT). Production checkout moved to **main** (d46dec2; feature branch deleted local+remote; worktrees pruned after verifying dirty-state already landed). Secrets scrub 15b658f: trade_agent webhook -> TRADEDESK_TRADE_SIGNALS_WEBHOOK env, VPS IP -> DASHBOARD_PUBLIC_URL, PII redacted; **OWED operator: rotate the 4 Discord webhooks burned in git history (8802366/c9c3db4/b6fa4a9/3412814/c1e0522) + history rewrite before any public push.** Dead code removed 24ce09e (~15k LOC: johnbot/, core/, src/signals/, bflow, top10_strategies_2026/, mcp-servers/, docs-site/, 51 one-off scripts); docs/ restructured 9aaea15 (archive/runbooks/reference; docs/systemd sole unit snapshot); replication gaps closed 1e138b4 (fundjohn-dashboard + mastermind-chat units captured sanitized, docs/nginx/openclaw.conf, scripts/install_systemd.sh); CLAUDE.md slimmed 66KB->10KB 6717e3c (this changelog extracted); README/ARCHITECTURE/AGENTS/bootstrap rewritten + CONTRIBUTING/SECURITY/CI added 743bde2; test suite reconciled 44bed12 (JS 222/222 green; pytest green except 14 catalogued pre-existing failures: options_execution_lane 8, intraday_hmm 3, option_close_path 1, options_backtest 1, run_sentiment_step 1 — fix against live modules post-fleet). send_report import-time load_dotenv leak fixed (was flipping gates inside the test runner). Deferred to phase 2 (post-fleet): strategies/decommissioned + 2 orphan impls, strategy_template_base merge, backtest-engine consolidation, server.js/collector.js/alpaca_executor/doctor splits (restart-coupled).

- **2026-07-16: §7 STATUS CORRECTION — true-MTM is DONE; the clamp is 5.0 not 3.0.** The 2026-07-01 entry below lists the true-MTM engine fix as **OWED** and says "there is currently no trustworthy risk-adjusted metric for ANY strategy until the true fix." **Both statements are STALE and were believed by a later session, nearly causing an unnecessary re-sequencing of work.** Reality, read from the code: `fd41e17` + `5810ce5` (2026-07-01) shipped true daily mark-to-market, and `unified_backtest.py:592` reads `_true_mtm = os.environ.get('OPENCLAW_TRUE_MTM_MARKS','1') != '0'` — **default ON**. `_portfolio_daily_returns` books each `(date, return)` from real `daily_marks` on its actual trading day; the pnl_pct smear survives ONLY as a fallback for `options_backtest` trades and test fixtures. The metric behaves: across 177 canonical rows avg `total_sharpe` = −0.52 and only **2 exceed 3.0**, both degenerate small samples (`S_TR03_bocpd` = 25.87 on **2 trades** / 1.5 avg holding days — a sample-size artifact, not methodology). Also: `pipeline_config.bt_sharpe_plausibility_cap` is **5.0** live (the "clamp-cap→5.0 decision" the entry lists as owed was evidently made); max `bt_sharpe` reaching weights is 4.87. Still genuinely owed from §7: Option-B mirror retirement + F2-round-2 re-triage. **Lesson: read the code before quoting a status doc — this file has now drifted on true-MTM, the clamp value, and `backfill_universe_5y.py`'s scale assumption.**
- **2026-07-16: effective_sharpe is BACKTEST-ONLY; live_sharpe recorded but never blended** (operator directive). `strategy_weights._effective_sharpe` was `(bt_n×bt_sharpe + live_n×live_sharpe)/(bt_n+live_n)`. Retired for two independent reasons. (1) **Attribution**: the book takes AGGREGATED positions — many strategies signal one ticker, the broker holds ONE position, and it exits through ONE Sharpe-weighted stacked bracket (`bracket_stacking.stacked_bracket`). If S1 wants stop −5%/target +3% and S2 wants −8%/+6%, the live exit fires at the blended level and BOTH are booked that shared outcome; **neither strategy's own rule ever ran**, so per-strategy live pnl_pct measures the blend, not the strategy. Measuring a strategy live would need a separate paper account where each fires its own brackets — we have none. (2) **Units**: `bt_sharpe` is an ANNUALIZED daily-return Sharpe (`(mean−rf)/std×√252`); `live_sharpe` is a RAW per-trade ratio (`mu/sigma` over pnl_pct) with no time basis — the blend averaged two different quantities. Concretely the blend let live data **rescue a failing backtest**: bt −0.40 + live 3.00 over 500 trades → **+2.81, sized**. Now `effective_sharpe = bt_sharpe`, and a **live-only strategy is NOT sizeable** (eff None) — deliberate; measured at the change, 0 of 70 current weight rows were live-only (69 bt-only, 1 blended with drag **0.00**), so this was inert-but-latent and deactivated nothing. `live_sharpe`/`live_n` are still computed and persisted for observability and a future **portfolio-level** Sharpe, which IS measurable because it needs no per-strategy attribution. **Takes effect at the next weights rebuild** (`weekly_live_sharpe.js`, weekend_saturday step 7). Tests: `tests/test_effective_sharpe_excludes_live.py`. **Dashboard was already clean** — `/api/strategies` reads `strategy_backtest_panel` (built from canonical `strategy_backtest_runs primary_window`) and touches no `signal_pnl`/`signal_performance`/`execution_signals`/`strategy_weights_by_regime`; only `last_signal_date` + `status` stay live. NOTE the two distinct `effective_sharpe`s: the weights one (above) vs `backtest_panel.effective_sharpe = sharpe/√avg_holding_days` (cadence-normalized, dashboard "Eff.Sharpe") — unrelated formulas, same name.
- **2026-07-16: Activation min-TRADES is now a dashboard slider** (operator directive). The per-regime sample-size floor was reachable only from `regime_qualification.class_thresholds` (hardcoded **100**) or the assigner's `--min-trades` flag, so tuning it needed a code change — unlike its min-Sharpe sibling. New `pipeline_config.strategy_activation_min_trades` + `activation_assigner.get_activation_min_trades()`; resolution order **`--min-trades` > pipeline_config > class gate (100)**. **Fail-safe direction is inverted vs a normal slider and deliberately so**: absent/malformed/negative → `None` → defer to the class gate, NEVER loosen to 0 (an unset key must not activate a regime on a 1-trade sample). `0` IS honoured when explicitly written ("no sample floor") — hence `Optional[int]`, not 0-as-sentinel. Dashboard: `GET/PUT /api/config/activation-min-trades` (range [0,1000]) + a card beside the min-Sharpe slider whose status line names the live SOURCE (`pipeline_config` vs `class_gate`) so an unset key never reads as an active floor. The assigner's pinned header (`threshold=… min_trades=…`, consumed by `activation_preview.js` HEADER_RE) is byte-stable. Tests: `tests/test_activation_assigner.py::TestGetActivationMinTrades`.
- **2026-07-15/16: Freshness-gate starvation FIXED + 5-year price history recovered.** (1) **The halt**: EOD `collect` aborted at `_verifyEquityFreshness` from 07-14, so `signals` never ran, the sizer self-loaded an empty APPROVED carried set, and emitted **0 orders with rc=0 `status=ok`** for five days — starvation looked like success. **Root cause**: the gate ran on `priceEquityTickers` — the wide SP-7 C2 resolver envelope (**12,699**) whose purpose is to keep history accruing for names a strategy may adopt LATER. **650 of the 706 stale names are consumed by no live strategy.** Measured on the 07-14 cohort: envelope 12,699/706 stale = **94.44% FAIL** vs live-strategy union 7,004/56 = **99.20% PASS**. Fetch wide, gate narrow → new `collector._signalsConsumedScope` (union ∩ fetchable, dot→dash bridged); degraded paths fall back to `universe_config`, **NEVER the envelope** (that would reinstate the halt the day the resolver is down). (2) **Symbol normalizer**: the dash→dot bridge was anchored `/^[A-Z]+-[A-Z]$/` — only single-letter classes converted, so every multi-char suffix (`-PRI`,`-PRH`,`-WS`,`-U`) 400'd into `fillPricesAlpaca`'s invalid-symbol branch (warn + return 0) while `updateCoverage` correctly refused to advance on zero rows: the fetch "succeeded", coverage stayed honest, nothing alerted, and **72 dash-form names Alpaca reports active+tradable on lit exchanges (BA-PRA, BAC-PRQ, C-PRN) never received a single bar**. Fixed unconditionally at BOTH sites (`fillPricesAlpaca` + `runIntradaySnapshotPrices`). (3) **data_coverage lied**: `upsertPrices` returns the count pushed to an in-memory BUFFER and `updateCoverage` advanced `date_to` on that, before `flushPrices()` wrote parquet; callers also passed `gap.to` (range REQUESTED) not the max bar RETURNED. 390 rows claimed newer than any bar they held; 131 had a coverage row and ZERO parquet rows (the 07-13 OOM killed the process between push and flush). The gate reads data_coverage, so **it passed on the lie**: 07-15 claimed 12,157/12,699 = 95.73% PASS, TRUE 11,848 = **93.30% FAIL**. Fix chooses the SKEW DIRECTION (two stores cannot be atomic without 2PC): coverage now commits only AFTER the writer resolves, derived from rows actually written (`store._commitPriceCoverage`), so a crash leaves parquet ≥ coverage (getGaps re-fetches, harmless) instead of coverage ≥ parquet (silent permanent hole). ⚠️ `updateCoverage` uses `GREATEST(date_to)`/`LEAST(date_from)`, so the forward fix does NOT self-heal existing lies — verified. Reconciled separately: 390 `date_to` snapped to truth, 131 phantoms removed → **0 lying, 0 phantom**. (4) **Universe hygiene**: 178 broker-confirmed delisted/not-found names + 44 redundant dash-form duplicates flagged `active=false` (reversible; snapshots saved). Classification MUST use `alpaca asset get` with retries and a **strict 404 (code 40410000)** — a first pass that treated any non-zero exit as "not found" called **96 live names dead** (BAESY is `active+tradable+OTC`). (5) **5-YEAR HISTORY RECOVERED**: `DEFAULT_START_DATE='2016-04-11'` means backtests always REQUESTED 10y — the data simply wasn't there (only 849 of 12,508 tickers had 2016 history; 4,355 started at 2021), because the `date_from` lie made getGaps see "covered" and never fetch. New `scripts/backfill_price_history.js` (the sanctioned `backfill_universe_5y.py` does `pd.read_parquet` + full rewrite **per (ticker,year) chunk** — its "394k-row parquet" comment is 24x stale; ~43 days + OOM at 62,155 chunks). Reuses the collector's DuckDB-bounded path: **12,361 tickers, 9,220,986 rows, 0 errors, 38.6 min, RSS <270MB**. Parquet **9.5M → 18.7M rows**; tickers with 2016 history **849 → 4,672**. `date_from` is NOT repaired downward — it heals by becoming TRUE (LEAST keeps the 2016 claim), and snapping it down would make getGaps demand ~15.5M rows through the next 16:15 collect (starving signals) and, for names listed after the claimed date (AA/Alcoa), re-fetch nonexistent data forever. **⚠️ OWED: all 237 canonical backtest rows claim `start_date=2016-04-11` but ran on ~2021+ data, and 25 predate true-MTM while 152 don't — the fleet is weighted by two methodologies over a mislabeled window. Fleet re-backtest + `backtest_panel --rebuild` required.**
- **2026-07-15: SP-7 D1 options_eligible producer MERGED + ACTIVATED** (operator directive; branch `feat/sp7-phase-d1-options-eligible` → **main** `4f29eca`/`3a13f82` AND the **live branch** `b083c61`). Weekly bulk chain-probe (`alpaca option contracts --status active`, 30 pages, ~19s) → `data/.cache/options_eligibility.json` → `run_ticker_metadata_step.py` → `ticker_metadata_snapshots.options_eligible`, which had **no writer at all** (0 of 14,112 names true). Seeded live via `systemctl start`: **5,897 eligible / 14,112 universe, exit 0**, freshness check PASS. Timer `openclaw-options-eligibility.timer` **enabled**, Sat 06:00 UTC — upstream of the daily 13:30 UTC `openclaw-tradable-universe-refresh` (the cache's only consumer) and clear of the 12:00 UTC weekend pipeline; stamp-touched before `enable --now` so `Persistent=true` did not fire an immediate catch-up. **Trading behavior unchanged**: all 155 manifest entries have `universe: null`, so no strategy resolves through an options predicate — this unblocks `options_eligible_only`/`large_cap_options`/`mid_cap_options` for universe_recommender rather than altering any live book. **Three pre-merge corrections** (`aaddda3`), all found by measuring rather than trusting the branch's 06-08 notes: (1) **tradable filter** — `--status active` filters on contract *status* and still returns non-tradable contracts (~1.9%); 34 names whose every contract was non-tradable were being marked eligible (5,931 → 5,897); (2) **silent no-op** — `data/` is root-owned so `claudebot` cannot create `data/.cache`; the producer's never-raise contract turned that PermissionError into exit 1 (== KEPT-PRIOR), which `SuccessExitStatus=0 1` reports as a *clean run* → added privileged `ExecStartPre=+mkdir/chown`; (3) **stale calibration** — the branch's "~676 curated Alpaca-tradable names" is dead (Alpaca expanded ~9x since 06-08: 10 pages → 30); floor 400 now only binds on a first write, `decide_write`'s 50%-of-prior (~2,900) is the real guard. Unit copies relocated to `docs/systemd/` (canonical W8 snapshot; branch predated it) and verified byte-identical to the installed units. **⚠️ Topology note: the VPS runs `feat/intraday-regime-15min-prefetch`, not `main` (every unit is `WorkingDirectory=/root/openclaw`) — a main-only merge deploys nothing, which is why D1 was merged into both.** 32 tests green.
- **2026-07-14: Saturday adjustments FULLY AUTOMATIC + Sharpe-weighted stacked brackets + per-position S_adj tiles** (operator directive; spec `docs/archive/superpowers/specs/2026-07-14-saturday-auto-adjustment-design.md`). (1) **Coupling gate relaxed**: `backtest_coupled_recs` now applies stop/TP/max-hold on ANY strictly-positive backtest Sharpe improvement (`MIN_DELTA 0.10 → strict >0`; `MIN_TRADES=30` unchanged); it also bracket-couples low-confidence `'noted'` recs (backtest is the arbiter regardless of confidence), records its decision in the NEW `coupling_outcome` column WITHOUT touching `action_taken`; **ONE backtest per rec + NO WEEKLY REFRESH** (operator directives, same day): the baseline is READ from the canonical `strategy_backtest_runs primary_window=TRUE` row (median stop/target anchors recomputed via SQL from that run's `strategy_backtest_trades`; max-hold anchor from `config_json`); the single candidate run executes on a deferred-commit connection and on APPLY is **committed as the new canonical primary row** (tagged `saturday_coupling apply`, demotion in the same transaction, panel rebuilt) so stored metrics always reflect the last helpful adjustment — reject/dry-run rolls back; fresh baseline (persisted, self-healing) only when no canonical row exists, and there is NO staleness guard (old row = nothing changed since). **Weekly fleet re-backtest RETIRED**: `weekend_saturday.sh` step 5 = `eligibility_assigner --all` only; `refresh_backtests.sh` is a manual tool; `openclaw-strategy-backtest-refresh.timer` DISABLED. **max_hold is strategy config baked into every backtest**: `run_backtest(max_hold_days=None)` default resolves from `strategy_regime_params` (MAX of non-null per-regime values, default 21, gate-OFF→legacy; CLI `--max-hold-days` default now None) — curation, coupling, and manual runs all inherit it, so applied hold changes compound (a bracket reject no longer silently drops the size rec), and on an APPLY with a stop candidate **re-anchors broker stops on open positions to the validated DISTANCE from entry** (long `entry·(1−stop_pct)` / short `entry·(1+stop_pct)`) via `alpaca_replace_stop` — replacing `position_recommender`'s old pre-validation price-relative replacement (which tightened long stops for a positive "widen" δ); that path is now permanently report-only (digest preview). (2) **Confidence auto-apply**: `proposal_manager.auto_apply_batch()` (+CLI `--auto-apply-batch`, new step **4b** in `weekend_saturday.sh` between coupling and weights) auto-approves size/eligibility proposals with confidence **strictly > 0.8** through the same `set_params` path as a dashboard click (`source='auto-approval'`); lower/missing confidence or rail-skips → NEW status `'noted'` (operator-decidable; `supersede_pending` now sweeps pending+noted so next Saturday's review re-evaluates them). `position_recommender` persists memo `confidence` and inserts `action_taken='noted'` when NOT >0.8 — `trade_handoff_builder` (pending/applied filter) therefore never applies low-confidence size recs. Env: `OPENCLAW_PROPOSAL_AUTOAPPROVE=1`, `_MIN_CONFIDENCE=0.8`, `_MAX_SIZE_DELTA=2.0` (non-binding, 0..2 domain). Migration **142** (confidence + coupling_outcome + 'noted' in the action_taken CHECK; applied live). Dashboard "⚙️ Strategy Adjustments" reframed: **Recent changes** (all sources incl. auto-approval, Via badge, ΔSharpe where backtest-coupled — `/api/regime-proposals/applied` widened) + **Noted** table (`/api/regime-proposals/noted`: noted proposals w/ approve/reject + noted sizing recs). **Timers `openclaw-weekend-saturday` + `openclaw-strategy-backtest-refresh` re-enabled** (dead since 06-27/28; stamp files touched first so `Persistent=true` did NOT fire a mid-day catch-up; next Sat 12:00 / Sun 10:00 UTC). (3) **Stacked brackets**: `bracket_stacking.stacked_bracket` cross-block combine is now the **effective-Sharpe-weighted mean** of block reps' stop/tp/t2 gaps (ω=s⁺/Σs⁺; equal weights if all ≤0; t2 renormalized over finite-t2 reps, clamped past t1) — value replication: the single bracket reproducing the aggregate exit value of Sharpe-proportional sub-positions is the size-weighted mean of levels; replaces min-stop/max-TP (within-block top-Sharpe rep unchanged; `_select_bracket` fallback unchanged; backtest engine unaffected — it never used stacking). Fixed the stale-failing `test_sizer_cadence_stop_norm` (pinned the pre-06-23 sum-TP). (4) **Per-position S_adj**: sizer attaches `corr_cum_sharpe = gate_net_sharpe[ticker]` to each equity order; persisted per (run_date, ticker) into `cycle_contributing_strategies.corr_cum_sharpe` (migration **143**, applied live; COALESCE-preserving upsert); `/api/portfolio/positions` enriches rows with the latest value; tiles (heatmap strip + treemap bottom row, hidden on small/tiny tiers) + expanded alpha-bars badge (`S_adj = x.xx` next to `net contrib`) + tooltips. **Cleanup**: removed 14 orphaned `tests/test_oxf_*.py` (implementations reaped 07-14) + repointed `test_bflow_run_phase1c_energy.py` off its pruned worktree path + refreshed the stale `test_weekend_saturday_dryrun.sh` (universe-recs grep → ladder sentinel + step-4b ordering asserts). **johnbot restart required** for the dashboard changes (bot.js hosts :3000 server.js); py curators/sizer are spawn-per-run.
- **2026-07-01 (later): Interim backtest-Sharpe plausibility clamp — DEPLOYED LIVE** (branch `feat/intraday-regime-15min-prefetch`, origin synced `884bfb4..1f21881`, NO migration). The §7 metric-source reconciliation (owed by W4-Tier3) confirmed a backtest metric-methodology defect: `unified_backtest._portfolio_daily_returns` smears each trade's TOTAL realized `pnl_pct` flat across its holding days (constant per day) rather than using mark-to-market daily returns, so the reconstructed daily curve has almost no volatility (residual variation only from day-to-day trade-composition changes, which shrink as trade overlap grows). Consequence in `aggregate_metrics`: **Sharpe/Sortino/Calmar are inflated 10-50× and non-comparable across strategies** (magnitude driven by overlap not risk; sign by the 5% RF hurdle), and **max-DD is understated** (same smeared curve hides mid-trade drawdowns — e.g. `low_volatility_us` shows a 2.99% max-DD across the 2020 COVID window). `return_pct` is ~fine. This was the intended "Fix A 2026-05-14" (which killed spurious 99% drawdowns) with an unnoticed side effect. The production sizer (`strategy_weights.rebuild`) sets `weight = effective_sharpe` directly, so inflated Sharpes over-weight high-overlap strategies (via the Tier-1 per-regime canonical `strategy_backtest_regimes`; the registry `backtest_sharpe` mirror is Tier-3 fallback only and feeds no live sizing). **Interim guard (operator-approved, Approach A):** `strategy_weights._load_backtest_sharpe` now clamps each per-regime `bt_sharpe` to `[−CAP,+CAP]` via the pure helper `_clamp_bt_sharpes`, with `CAP` read every rebuild from `pipeline_config` key `bt_sharpe_plausibility_cap` (default 3.0; `_get_bt_sharpe_cap`, fail-safe to 3.0 on any read error OR non-positive value — a negative cap would break sign-preservation). **Sign-preserving** → bounds over-weighting + the corr-gate's signed contributions WITHOUT changing any inclusion/exclusion decision. Activated via a manual `python3 -m execution.strategy_weights --rebuild` (`OPENCLAW_AUTO_DEMOTE=0`): 54/194 entries clamped, POST max `effective_sharpe`=3.000 (CRISIS was 10.53), per-regime positive-weight counts unchanged (41/39/25/21) — verified sign-preserving, no demotions, no `johnbot` restart (the weekly `weekly_live_sharpe.js` cron re-runs the on-disk module idempotently). **Reversible:** `UPDATE pipeline_config SET value='999' WHERE key='bt_sharpe_plausibility_cap'` + rebuild. 13 tests. **This is an INTERIM guard that only bounds the damage — there is currently no trustworthy risk-adjusted metric (Sharpe/Sortino/Calmar/DD) for ANY strategy until the true fix.** OWED (operator-gated): (1) the **true-MTM engine fix** — emit per-day mark-to-market marks from `simulate_trade`/`_per_bar_simulate` (the daily price path is already walked) so `_portfolio_daily_returns` uses real marks → correct Sharpe AND DD, preserving the "Fix A" curve behavior — plus a **full ~189-strategy re-backtest** (2-core/8GB no-swap → OOM-prone); (2) **retire the registry mirror (Option B)** — point the gate-fallback (`promotion_service.evaluatePromotionGate`), `strategy_forensics`, `mastermind_chat/snapshot`, `routes_research` at canonical; (3) **F2-round-2 governance re-triage** (~19 approved canon-sub-floor Sharpe / ~24 fail canonical DD — a conservative undercount) per-strategy, ONLY on corrected metrics. Spec/Plan: `docs/archive/superpowers/{specs,plans}/2026-07-01-interim-sharpe-plausibility-clamp*`; recon record `docs/2026-07-01-metric-source-reconciliation-findings.md`.
- **2026-07-01: Correlation-adjusted cumulative-Sharpe gate is now the SOLE conviction gate; legacy cum-Sharpe retired** (branch `feat/remove-legacy-cumsharpe`; dashboard part deployed live, sizer part staged for post-first-real-floor-cycle deploy). The corr-adjusted gate (`S_adj = Σwᵢ²dᵢ / √Σwᵢwⱼdᵢdⱼρᵢⱼ`, `wᵢ = daily_weight = effective_sharpe/√cadence`) has been the live conviction gate since 2026-06-30 (drives BOTH ticker selection and sizing weight; per-regime floor `regime_sizer_params.min_corr_cum_sharpe`, migration 140, dashboard-controlled). This change removes the now-dead legacy machinery: (1) `regime_blended_sizer._sharpe_cadence_path` — the corr gate is **unconditional** (dropped the `OPENCLAW_STRATEGY_CORR_CUMSHARPE`/`_SHADOW` flags), the legacy naive `ticker_net_sharpe` gate + the `CORR_WEIGHT`-gated `deflated_net_sharpe` gate branch + the corr-cumsharpe shadow rail are gone; `ticker_net_sharpe` is retained only as the naive baseline for the #botjohn-log diagnostic; the similarity matrix now loads unconditionally (was already always-on in prod via FOLD/BRACKET_STACK). (2) Dashboard `server.js` Conviction Gates card — legacy `min_cumulative_sharpe` slider + descriptions + `active_conviction_gate` badge + SP-7 √ln(N) proposal chips removed; only the corr slider remains; relabeled "corr-adjusted cumulative Sharpe per regime". (3) **Legacy-remnant excision (2026-07-01, follow-up commit):** removed `_resolve_min_cumulative_sharpe` + `min_cum_sharpe` + the naive `ticker_net_sharpe`; the corr #botjohn-log diagnostic now compares against the corr gate at the current corr floor (no naive baseline); the inert OPTION consolidation path (`_consolidate_option_orders`) lost its legacy conviction floor (options are inert — TODO: option-appropriate gate at first option-strategy promotion); the dead ORTHO_SHADOW *deflation* shadow block + `orthogonalization.deflated_net_sharpe`/`block_conviction`/`k_eff`/`_mean_pairwise` (+ their unit tests) deleted; the SP-7 `/api/universe-threshold-proposals` GET/apply/reject endpoints deleted. The ORTHO_SHADOW *flag* + the BRACKET_STACK bracket-shadow stay (separate live-flagged feature). The `min_cumulative_sharpe` DB column + the `universe_threshold_proposals` table are deprecated in code only — NOT dropped (append-only). **Prod behavior is byte-identical to current live on the happy path** (all ortho flags were already ON + the similarity matrix loads every cycle → corr already fired). **One behavior change on the rare similarity-matrix load-FAILURE path:** old code fell back to the retired legacy naive gate when `load_groups()` raised; new code runs the corr gate unconditionally with sparse-default (assume-independent) correlations + a loud warning — which under-deflates correlated bets on a correlation-blind day (partially mitigated by the separate asset-corr cap, which uses a different correlation source). Pinned by `test_matrix_load_failure_degrades_to_sparse_default`. Rollback = git revert (operator-accepted). 21 corr tests + 221 sizer regression green (1 pre-existing unrelated failure: `test_sizer_cadence_stop_norm` TP=max/sum). Spec/Plan: `docs/archive/superpowers/{specs,plans}/2026-06-30-corr-adjusted-cumulative-sharpe-gate*`.
- **2026-05-30: Backtest signal[t] → execute[t+1] fill model** (branch `feat/backtest-t-plus-1-execution`; NOT merged/deployed — operator-gated). `_per_bar_simulate` in `src/backtest/unified_backtest.py` now fills each signal at the NEXT bar's close (`close[t+1]`) instead of the signal-bar close, overriding any strategy-supplied `entry_price` at the simulate boundary (127/140 strategies set it to the signal-day close, so the old `else close[t]` fallback was almost never hit). Brackets are re-anchored to the new fill preserving pct shape (new `_reanchor_bracket` helper, mirrors the live executor); `entry_date` is stamped to the fill bar (t+1) while `entry_regime` stays the signal-day (t) regime (so `aggregate_per_regime` keeps decision-day partitioning). Exits walk from t+2 (fill date passed to `simulate_trade`). Signals on the last available bar are skipped (no fill possible). Scope: `unified_backtest._per_bar_simulate` only — shared by `run_backtest` (weekly refresh + coupling) and `universe_grid_cli`; `quick_backtest`/`regime_blended_backtest`/`intraday_regime_backtest`/`regime_performance_analyzer`/`options_backtest`/`auto_backtest` unchanged (they don't use per-trade close[t] fills). **Conservatism note:** live fills ~same-day close (`close[t]`); backtest now fills `close[t+1]`, so the backtest is deliberately MORE conservative than live — and the coupling decision is backtest-to-backtest so the level shift cancels in the ΔSharpe. **Operator rollout (NOT auto):** let the Sat 12:00 UTC weekend run finish on the OLD model, then merge, then run `python3 -m backtest.unified_backtest --all-live` once to rebuild every live strategy's metrics on t+1 (the next weekend `refresh_backtests.sh` does this automatically thereafter), then verify a couple of short-hold strategies' Sharpe shifted + the dashboard panel refreshed. **Leave the 2 in-flight strategies out of any manual rebuild** — the 8 AM ET `refresh_backtests.sh` re-backtests them on the new model. 8 TDD tests in `tests/test_unified_backtest_t_plus_1.py`; regression green (73 unit tests). Spec: `docs/archive/superpowers/specs/2026-05-30-backtest-t-plus-1-execution-design.md`. Plan: `docs/archive/superpowers/plans/2026-05-30-backtest-t-plus-1-execution.md`.
- **2026-05-27: SP-4 Phase 0 — Synthetic greeks options engine (built on branch `feat/sp4-phase-0-greeks-engine`; NOT merged/pushed/deployed — awaiting operator merge decision)**. First sub-project of SP-4 (Weekly Research Uplift). Builds a synthetic Black-Scholes options backtest so the research stack can later *originate* option strategies. (1) `OptionSpec` dataclass added to `src/strategies/base.py:Signal` (optional `option_spec`; the API boundary Phase B/C will emit — underlying/right/strike_rule/target_delta/dte_target/structure(single|straddle|strangle)/hedge(none|delta)/roll_dte). Equity/crypto/etp signals leave it None → byte-identical. (2) `src/backtest/options_pricing.py` (py_vollib BS price/greeks/IV-inversion/strike-from-target-delta/monthly-expiry calendar) + `src/backtest/options_backtest.py` (`simulate()` priced-contract path: single-leg + delta-hedged straddle/strangle + roll-then-reopen; returns the `_per_bar_simulate` dict shape so `aggregate_metrics`+DB-write are reused). `run_backtest` dispatches via `_simulate_for(instrument_class)` — **branches ONLY on `'option'`; equity/etp/crypto byte-identical** (regression-verified). (3) **CRITICAL data finding (corrects the SP-3.1 handoff's "7 weeks of real IV"):** `options_eod.parquet` is really only **3 calm-regime days** (2026-05-22/25/26; 31 of 34 dates are 1-row placeholders). A realized-vol×scalar IV model is **~25% off real IV even ATM** (can't capture the vol surface; `options_aggregates_enriched.parquet` has 2yr but its IV is unreliable — SPY iv30 corr w/ VIX only 0.375). **Resolution (operator-approved):** `src/backtest/synthetic_iv.py` now **VIX-anchors** ATM IV for liquid index/ETF underlyings (`src/backtest/vol_index.py`: `VALID_OPTION_UNDERLYINGS={SPY,SPX,^GSPC,QQQ,IWM}`, per-name beta SPY 0.90/QQQ 1.25/IWM 1.36; VIX = 10y of REAL regime-diverse 30d SPX IV in `prices.parquet`), realized-vol×vrp fallback otherwise. **Authoritative parity GATE = IV-MAE on the supported index/ETF-ATM envelope: 0.082 PASS** (vs 0.15; IWM 0.065/QQQ 0.074/SPY 0.108) — `scripts/options_parity_check.py:run_iv_gate`. Price-level MAE (0.43) demoted to a documented diagnostic (irreducible ~22% floor: no option-snapshot underlying / dividends / American≠European / small-cap quote spreads — a DATA property, not an engine flaw). **Options origination is scoped to index/ETF ATM near-term; single-name + OTM-wing options deferred until real-chain history accrues** (the daily archive grows ~1 day/trading day). (4) Greeks-aware delta-dollar option sizing in `src/execution/instrument_class_sizer.py` (legacy `|delta|` fallback preserved; **inert live** — no option strategy promoted). (5) `PROMOTION_THRESHOLDS` calibrated + **operator-signed-off 2026-05-27**: `option={min_sharpe:0.80, max_drawdown:0.30}` (conservative — higher than equity's 0.5, offsets synthetic-engine uncertainty), `crypto={min_sharpe:0.50, max_drawdown:0.70}` (resolves the stale `TODO(SP-3.2)`; single live crypto strategy so 0.50 floor). (6) Reference strategy `S_short_straddle_vrp` (SPY ATM short straddle, `instrument_class='option'`, **candidate-only**) — proves the pipeline end-to-end; backtest Sharpe **−1.38** (CAGR 2.8% < 5% rf → correctly fails promotion; it's a proof, not an alpha). **Live options EXECUTION is OUT of Phase 0** (reuses `OptionSpec` when the first option strategy promotes). 48 tests green (engine + regression). **New Python runtime deps: `py_vollib` + `scipy` (both already installed on the VPS; the repo tracks NO pip manifest — deps are provisioned externally, so any rebuild/new-env must include them).** Safety guards from final review: `auto_backtest.run_backtest` REFUSES `instrument_class='option'` (it's an equity-only loop — would produce wrong delta-1 metrics), and `options_backtest` warns when an option underlying falls back to the low-fidelity realized-vol IV (unsupported underlyings are NOT trustworthy for promotion). Spec/Plan: `docs/archive/superpowers/{specs,plans}/2026-05-27-sp4-{weekly-research-uplift,phase-0-greeks-engine}*`. **Operator gates honored: parity surfaced, threshold sign-off obtained, merge/deploy NOT done. Next SP-4 phases (A ingestion→D review-awareness) on separate plans.**
- **2026-05-26: SP-3.1 Phase D — Reference crypto strategy + ACTIVATION (MERGED to main `89041b2`/`b5b4c20` + pushed + johnbot restarted; crypto stack now LIVE — all 3 gates ON)**. Final SP-3.1 phase; **flips the whole crypto stack from inert to live.** (1) Crypto enablement constants: `INSTRUMENT_COST_BPS['crypto']=25.0` (`unified_backtest.py`, record-only), `PROMOTION_THRESHOLDS['crypto']={min_sharpe:0.5, max_drawdown:0.70}` (`lifecycle.py` — **0.70 is a deliberate operator sign-off**: BTC is a 60-80% DD asset, the 0.40 placeholder was unrealistic; not gamed), `crypto ∈ ROUTED_INSTRUMENT_CLASSES`. (2) Migration **119** `alpaca_submissions.qty INT→NUMERIC` (additive widen; fractional crypto qty was truncating to 0; **applied live via psycopg2**; round-trip test rolls back). (3) Reference strategy `src/strategies/implementations/S_btc_momentum.py` (+`.requirements.json`) — single-asset BTC-USD 90-day momentum with absolute-momentum cash gate (flat when 90d ret ≤ 0) + 20% vol-target sizing; `instrument_class='crypto'`; **backtested Sharpe 1.02 / MaxDD 65% / +1369% over 10y** on existing `prices.parquet` BTC bars; **PROMOTED candidate→live**. (4) Broker-side crypto resting stops: `_submit_crypto_stop` (only `stop_limit` accepted — plain `stop`→422; limit 0.5% below stop) placed after entry, sized to the **actual filled qty** via `_poll_crypto_fill` (entry fills async + short on fees); `_route_crypto_close` now cancels open crypto stops first (a resting stop reserves the position → close 403s). Both call sites guarded against `subprocess.TimeoutExpired` (must never abort a post-entry path → orphan position). Grounding: `docs/archive/superpowers/specs/sp3.1-phase-d-crypto-stop-snapshot.md` (live paper spike). (5) **Daily crypto bars were already in `prices.parquet`** (BTC-USD 3698 bars 2016→now via SP-1 collector) — no backfill needed. **ACTIVATION (operator-approved, executed 2026-05-26):** merged+pushed → promoted S_btc_momentum live → flipped `OPENCLAW_INSTRUMENT_CLASS_ROUTING=1`+`OPENCLAW_CRYPTO_REGIME=1`+`OPENCLAW_CRYPTO_REDEPLOY=1` in prod `.env` → **backfilled `crypto_bars_1h.parquet` (9888 BTC+ETH hourly bars; was missing — Phase B smoke wrote it in a since-removed worktree)** → ran crypto regime detector once (LOW_VOL 0.92, seeded `crypto_regime_latest.json` + DB row) → restarted johnbot (healthy, 0 restarts). **Live smoke ✓:** $20 BTC order via real `execute_single`→`record_submission` wrote `alpaca_submissions.qty=0.000261957` (fractional preserved — migration 119 validated end-to-end), equity book UNCHANGED (19→19 positions), flattened. First real strategy trade fires on the next 10 AM ET daily cycle. Final opus whole-delta review CHANGES_REQUESTED→fixed (TimeoutExpired guard). Spec/Plan: `docs/archive/superpowers/{specs,plans}/2026-05-26-sp3.1-phase-d-crypto-reference-strategy*`. **Known follow-up: the 10s fill-poll timed out in the smoke → resting stop was SKIPPED (fail-safe held, entry stood); bump the poll timeout (→~45s) or place stops on a later reconcile pass before relying on intraday broker stops.** SP-3.1 (A→D) COMPLETE.
- **2026-05-26: SP-3.1 Phase C — Crypto redeploy + per-strategy regime gating (built; gates default-OFF → INERT until Phase D)**. A confirmed crypto regime transition triggers a **dedicated crypto-only redeploy** that NEVER touches the equity book. (1) `src/execution/engine.py:run_strategies` now selects the regime PER-STRATEGY by `instrument_class`: crypto strategies gate on `crypto_regime_latest.json` (via `regime.crypto_regime.load_crypto_regime_state`); equity/etp/option keep the equity regime (byte-identical `else` branch). (2) New `src/execution/crypto_redeploy_sizer.py:size_crypto_positions` — reuses the regime-blended weight model (`strategy_weights.load_current` → daily_weight → ticker_weight) scoped to crypto strategies, sizes to crypto's PROPORTIONAL weight slice of λ×NAV (margin-funded, clamped [0,1]), and **filters broker positions to crypto tickers BEFORE any delta/orphan logic** (normalizes Alpaca `BTC/USD`→`BTC-USD` first) so equity positions are STRUCTURALLY invisible — the load-bearing equity-untouched test (mixed SPY+BTC book → zero equity orders) is the guardrail. Funded by 4x margin; next daily cycle corrects total leverage; equity NEVER reduced (operator design). (3) New `scripts/redeploy_crypto.py` — gated dedicated driver (`OPENCLAW_CRYPTO_REDEPLOY`), own cooldown namespace `redeploy:crypto:*`, NO RTH gate (crypto 24/7), min-signal gate, submits via Phase A's `execute_single`/`_route_crypto_order`. (4) `scripts/run_crypto_market_state.py` spawns `redeploy_crypto.py` detached on confirmed transition + sets `fired_redeploy=TRUE` (mirrors the equity intraday spawn). Gate `OPENCLAW_CRYPTO_REDEPLOY` default-OFF; **ACTIVATION NEEDS BOTH GATES** — live crypto submission also requires `OPENCLAW_INSTRUMENT_CLASS_ROUTING=1` (the executor only routes crypto when that's ON), else sized orders SKIP at submit. T2 safety review (opus) confirmed the equity-untouched invariant holds + caught the broker symbol-format bug (now fixed). Spec/Plan: `docs/archive/superpowers/{specs,plans}/2026-05-26-sp3.1-phase-c-crypto-redeploy*`. Phase D (reference crypto strategy + daily crypto bars in prices.parquet + crypto `INSTRUMENT_COST_BPS`/`PROMOTION_THRESHOLDS`/`ROUTED_INSTRUMENT_CLASSES` + broker stops + `alpaca_submissions.qty INT→NUMERIC` migration) activates it all.
- **2026-05-26: SP-3.1 Phases A+B — Crypto execution lane + 24/7 regime detector (MERGED to main `490d36f` + pushed to origin + johnbot restarted; both gates default-OFF → INERT)**. Builds on SP-3's `instrument_class` enum (crypto was reserved-but-raising). **Phase A (crypto execution lane):** single `_route_crypto_order(order, equity, coid)` intercept at the top of `alpaca_executor.py:execute_single` (after coid, before the `_normalize_alpaca_symbol`-None skip), GATED on `OPENCLAW_INSTRUMENT_CLASS_ROUTING==1` (equity path byte-identical when OFF; crypto falls through to the equity "unsupported" SKIP). Crypto orders use fractional `--qty` (the CLI's `--notional` is locked to `day` TIF which crypto rejects), `--type market --time-in-force gtc`, `order_class='simple'` (NO bracket, NO broker-side stop — Phase D), symbol `BTC-USD`→`BTC/USD`; qty = notional/`alpaca data crypto latest-trades` price; close path = `position close [--percentage]`. `instrument_class_sizer.py`: crypto moved to pass-through (futures still raises). Live $20 paper buy+close smoke ✓ (account PAXXXXXXXXXX). Grounding: `docs/archive/superpowers/specs/sp3.1-task0-crypto-cli-snapshot.md` (gtc TIF; `day`→422; **$10 min cost basis**; `id` order-key; 9-dp qty; `notional` null on qty-orders). **Phase B (crypto regime detector):** parallel 24/7 HMM, fully isolated from the equity regime (`engine.py`/`regime_gate.py`/`run_market_state.py`/`run_intraday_market_state.py` UNTOUCHED). New `src/regime/crypto_regime.py` (pure: 5 price-derived RV features [`btc_rv_24h` first so HMM states order ascending-vol→LOW_VOL..CRISIS], 4-state GaussianHMM fit/score mirroring `run_market_state.py`, hysteresis/confirmed-transition mirroring `run_intraday_market_state.py` but reading `fired_redeploy`). New `src/ingestion/crypto_bars.py` → append-only `data/master/crypto_bars_1h.parquet` (added to NEVER-DELETE family; fetched independent of `union_universe`). New `scripts/run_crypto_market_state.py` (gated driver: fetch→features→fit/load→score→hysteresis→persist `crypto_regime_latest.json` + `crypto_regime_states` row; **never spawns redeploy — `fired_redeploy` always FALSE**, that's Phase C). Migration **118** `crypto_regime_states` (mirrors 067, `fired_liquidation`→`fired_redeploy`; APPLIED to live DB). Gated hourly cron `0 * * * *` UTC in `cron-schedule.js`, gate `OPENCLAW_CRYPTO_REGIME` (default OFF). Live smoke ✓ (4318 bars backfilled → LOW_VOL). **Both phases reviewed (Phase A final review caught a gate gap — now fixed; Phase B whole-delta review APPROVED). To activate: flip `OPENCLAW_INSTRUMENT_CLASS_ROUTING=1` (crypto sizing/exec) and `OPENCLAW_CRYPTO_REGIME=1` (regime detector — RUN BACKFILL FIRST to seed `crypto_bars_1h.parquet`, else first tick crashes).** Phase C (instrument-class-scoped crypto redeploy + regime gating) + Phase D (reference crypto strategy, daily crypto bars in prices.parquet, crypto `INSTRUMENT_COST_BPS`/`PROMOTION_THRESHOLDS`/`ROUTED_INSTRUMENT_CLASSES`, broker-side stops, **`alpaca_submissions.qty INT→NUMERIC` migration** — fractional crypto qty currently rounds to 0 in the audit row) follow on separate plans. Spec/Plan: `docs/archive/superpowers/{specs,plans}/2026-05-26-sp3.1-phase-{a,b}-*`.
- **2026-05-26: SP-3 — Asset-class expansion MVP (MERGED to local `main` 2026-05-26 `c2f72ca`; NOT pushed to origin, NOT deployed — gate OFF, no service restart yet)**. Adds a strategy-level `instrument_class` enum (`equity`/`option`/`etp` routed; `crypto`/`futures` reserved with no handler → raise) threaded through lifecycle + sizer + backtest + promotion guards, WITHOUT changing behavior for existing all-equity strategies. (1) `StrategyRecord.instrument_class` (default `equity`, top-level, always-emitted) threaded through `from_manifest` (both constructions) + `to_dict` (silent-strip-safe; regression test `tests/test_lifecycle_instrument_class.py`); enum validated at load (`VALID_INSTRUMENT_CLASSES`/`ROUTED_INSTRUMENT_CLASSES`). (2) Resolver `src/strategies/instrument_class.py:instrument_class_for`. (3) Sizing dispatcher `src/execution/instrument_class_sizer.py` (equity/etp literal pass-through; option scales notional by |delta| fail-open; crypto/futures raise), wired into `regime_blended_sizer_live._build_sized_payload` behind default-OFF kill-switch `OPENCLAW_INSTRUMENT_CLASS_ROUTING` — equity path byte-identical when OFF, and equity/etp short-circuit even when ON. (4) Per-class promotion thresholds `PROMOTION_THRESHOLDS` in `lifecycle.py` (equity/etp keep 0.5/0.20; option placeholder `TODO(SP-4)`); `can_transition` reads `rec.instrument_class`. (5) Class-aware backtest cost model `INSTRUMENT_COST_BPS`/`resolve_cost_model_bps` + `run_backtest(instrument_class=...)` — RECORD-ONLY log, no sim-math change, no migration. (6) Reference strategy `S_commodity_etp_momentum` (GLD/SLV/USO 90-day momentum rotator, `instrument_class=etp`, **candidate-only** — backtest Sharpe 1.08 / MaxDD 42.6% / 2455 trades over 10y on existing price history). NO schema migration; NO master-data/`universe_config` mutation (DBC dropped — zero coverage; coverage-freshness + DBC backfill are PRE-LIVE-PROMOTION operator-only). Crypto + 24/7 session logic deferred to **SP-3.1**; greeks-aware option sizing/backtest + leveraged-ETP decay are fast-follows. 22 SP-3 tests + 41 sizer + 34 lifecycle/backtest regression green; final review APPROVED. Spec/Plan: `docs/archive/superpowers/{specs,plans}/2026-05-26-sp3-asset-class-expansion*`. **Merged to main + pushed to origin + johnbot restarted 2026-05-26 (SP-3 code now LIVE on disk, healthy). Behavior still INERT: gate `OPENCLAW_INSTRUMENT_CLASS_ROUTING` default-OFF (equity path), reference strategy candidate-only. To activate per-class sizing routing: flip the gate to `1` after soak (separate deliberate step). Reference strategy stays candidate until operator promotes (+ its pre-promotion universe_config/DBC coverage).**
- **2026-05-25: SP-2 Phase D — predicate-at-mint shipped** (branch `feat/sp2-phase-d-research-hooks`, PR #11; completes SP-2 universe-expansion A→D). PaperHunter prompt §5 infers ONE of the 12 real `CANDIDATE_PREDICATES` (or null) → `hunter_result_json.inferred_universe_filter`; StrategyCoder emits `from src.strategies.universe_default import <name> as universe_filter` at module scope when a filter is injected; the orchestrator (`research-orchestrator.js`) validates against the CANDIDATE_PREDICATES whitelist (gated, falls back to null on unknown) and threads the validated name into the coder context + queued `strategy_spec`; `lifecycle.register()` AST-detects the module-scope import and sets `universe_filter_ref="src.strategies.universe_default:<name>"` (the resolver-loadable `module:attr` form) via `save_manifest`. NO schema migration — the field rides `research_candidates.hunter_result_json` (JSONB). New system_check `papermint_predicate_coverage` (agents + strategies tag) + operator dashboard tile. Gate `OPENCLAW_PHASE_D_PREDICATE_AT_MINT` (spec: default ON; code treats absent as OFF — live VPS `.env` must add the line). Runbook: `docs/runbooks/sp2-papermint-runbook.md`. Spec/Plan: `docs/archive/superpowers/{specs,plans}/2026-05-22-sp2-phase-d-research-hooks*`.
- **2026-05-24: SP-2 Phase C — Mastermind universe-recs shipped** (branch `feat/sp2-phase-c-mastermind-universe-recs`, PR #10, stacked on Phase B which merged to main). New `--mode universe-recs` on `run_mastermind.js`; Sat 20:00 ET timer `docs/universe-recs.{service,timer}` (installs as `openclaw-universe-recs.*`); gated `OPENCLAW_UNIVERSE_RECS=1` (default OFF, `--dry-run` bypasses). Per manifest-`state='live'` strategy (~52): a `UniverseResolver`→**regime-blended** 8-metric backtest grid over the 4 cap-independent candidate predicates (`sp500/no_adr/no_otc/options_eligible_only`) via new `src/backtest/universe_grid_cli.py` (built on `MockResolver` in `universe_resolver.py` + a `resolver=` param on `unified_backtest.run_backtest` sharing a new `_per_bar_simulate` helper + new `sortino`/`calmar`/`mean_universe_size` metrics in `aggregate_metrics`). Opus 4.7 1M picks → strict JSON → `strategy_universe_recommendations` + Discord `#universe-recs` post. Operator ✅/❌/⏸ reactions (`bot.js` `messageReactionAdd`, numeric `Partials` enum — string partials are no-ops in discord.js v14) → dashboard :7870 `POST /api/universe-recs/:id/:action` → `lifecycle.adopt_universe_recommendation` (atomic manifest+DB+`lifecycle_audit_log` two-phase rename; writes `universe_filter_ref="src.strategies.universe_default:<candidate>"`, the resolver-loadable `module:attr` form). 28-day adopt-skip. Dashboard tile (last 14d) with bypass-Discord buttons. Migration **117** `lifecycle_audit_log` (116 was taken by market_news). Doctor `universe_recs_freshness` + system_check `universe_recs_health` (both gated). Known gaps: the 8 cap-gated candidates (`r1000/r3000/large_cap/...`) resolve empty for historical dates (FMP Starter market_cap 403) — marked `cap_unverified` until the shares-outstanding backfill; the `migrate()` runner has a pre-existing connection-poisoning wart on re-run (non-idempotent 008/027/069) so **verify `lifecycle_audit_log` exists post-deploy**. Spec: `docs/archive/superpowers/specs/2026-05-22-sp2-phase-c-mastermind-universe-recs-design.md`. Plan: `docs/archive/superpowers/plans/2026-05-22-sp2-phase-c-mastermind-universe-recs.md`.
- **2026-05-22: SP-2 Phase B — 5y backfill machinery shipped** (branch `feat/sp2-phase-b-5y-backfill`, PR #9). 14 implementation tasks: builder + frozen artifact + SP500 history CSV + migration 115 + quarantine filter + 6-consumer wiring + driver scaffolding + prices/metadata/options targets + doctor + system_checks + dashboard tiles + smoke. Quarantine filter wired into 3 sites (universe_resolver coverage, unified_backtest.load_prices_panels, quick_backtest._load_prices) + collector.js startup hook; 3 spec-named backtest engines documented as unfilterable (regime_blended reads only historical_regimes.parquet, intraday uses ts_utc, regime_performance_analyzer reads no parquets). Master parquets are SINGLE FILES not partitioned datasets — single documented append-only-invariant exception (`pyarrow.parquet write` with explicit zero-existing precondition) lives in `scripts/backfill_universe_5y.py:_promote_chunk`. Daily writer DRY-refactored to share `build_month_snapshot` helpers with the backfill builder. Phase B v1 known gaps: historical market_cap = None (FMP Starter 403; fallback unbuilt); backfill seed = 404 tickers pre-Task-7-completion; AAPL/MSFT first_seen_at = 2026-05-14. Runbook: `docs/runbooks/sp2-backfill-runbook.md`. Spec: `docs/archive/superpowers/specs/2026-05-22-sp2-phase-b-5y-backfill-design.md`. Plan: `docs/archive/superpowers/plans/2026-05-22-sp2-phase-b-5y-backfill.md`. Migration 115 (backfill_audit). New doctor checks (backfill_progress, backfill_universe_coverage; both slow=True). New system_checks (backfill_progress on storage tag, ticker_metadata_history_depth on strategies tag). Phase C (Mastermind universe-recs) + Phase D (research hooks) follow on separate plans.
- **2026-05-22: SP-2 Phase A — Universe-slice machinery shipped** (branch `feat/sp2-phase-a-universe-machinery`, PR pending). Predicate-per-strategy universe model: each strategy declares `universe_filter(meta, as_of) -> bool` (or inherits the default `sp500`). New `UniverseResolver` service + `ticker_metadata_snapshots` table + 12 candidate predicates. Backtest engines (`unified`/`quick`/`regime_blended`/`intraday_regime`/`regime_performance_analyzer`) accept resolver for per-bar point-in-time universe. Lint enforces `(meta, as_of)` signature + bans `datetime`/`time`/`os` imports in predicate modules (CI gate `.github/workflows/lint-universe-predicates.yml`); lifecycle sandbox check rejects clock-dependent predicates at transition. All 102 existing strategies on default predicate post-deploy — byte-identical behavior. `union_universe(today, ['live'])` drives collector / options-archive / news / sentiment data-fetch envelope. Migrations 111-114. Doctor checks: `metadata_snapshot_freshness`, `union_universe_size` (slow=True). New system_checks: `universe_resolution` (pipeline tag, 15s latency gate), `metadata_snapshot_freshness` (strategies tag). Dashboards: operator `/api/universe-slice` + user `/api/pipelines/universe-inflation` with UI tiles. Default env `OPENCLAW_UNIVERSE_RESOLVER=1` (kill switch). Spec: `docs/archive/superpowers/specs/2026-05-22-sp2-universe-expansion-design.md`. Plan: `docs/archive/superpowers/plans/2026-05-22-sp2-phase-a-universe-machinery.md`. Phase B (5y backfill) + Phase C (Mastermind universe-recs) + Phase D (research hooks) follow on separate plans.
- **2026-05-22: Pipeline Diagnostics dashboard tab + LangGraph orchestrator LIVE**. New "Pipeline Diagnostics" tab in `src/channels/api/server.js` (port 3000 → nginx :80, right of Research) inspecting LangGraph runs via 4 routes under `/api/pipelines/*` (`summary`, `runs`, `runs/:thread_id`, `stream` SSE). Backend: `src/channels/api/routes_pipelines.js`. Merges in-memory traceBus runs with durable `langgraph.checkpoints` Postgres state. Includes gantt-style node timeline, collapsible checkpoint JSON viewer, color-coded graph pills, animated status pulse, live SSE stream with pause/clear. **OPENCLAW_LANGGRAPH_ORCHESTRATOR=1 flipped LIVE 2026-05-22 07:23 UTC** — today's 10 AM ET cron now dispatches to `src/agent/graphs/daily-cycle.js` (PostgresSaver checkpoints, traceBus → dashboard SSE) instead of legacy `pipeline_orchestrator.py`. Pre-flip dry-run validated: graph compiles, step ordering correct, subprocess invocation works, state accumulates, error path propagates cleanly. Known pre-existing dry-run-only gap: `trade_handoff_builder.py` + `regime_blended_sizer_live.py` + `daily_health_digest.js` don't accept `--dry-run` flag (live cron doesn't inject it, so non-blocking). SP-1 follow-ups also closed: data_provider_health writer (`src/maintenance/provider_health.py`) wired into alpaca_options + alpaca_news + cboe_vol_indices; open_interest gap formally accepted as not-fixable on Alpaca/FMP (master parquet was 99.95% NULL on the column already, engine.py degrades gracefully). Commits: `eb384fc` (diagnostics tab), `6511904` (provider_health + UI polish).
- **2026-05-22: SP-1 provider cutover shipped** (branch `feat/sp1-provider-cutover`, deploys Saturday 2026-05-23). Polygon + Yahoo + Massive S3 (Polygon-affiliated) fully removed from the data stack. Alpaca AAT Plus is now P1 for equity quotes, options chain, news, screener, corp-actions, and historical options EOD self-archive (new daily 16:30 ET timer `openclaw-options-archive.timer`). FMP Starter stays P2 for fundamentals + macro + insider flags + universe ref. yfinance bounded to `src/ingestion/cboe_vol_indices.py` (CI lint `scripts/lint_provider_guards.py` enforces — only file allowed to import yfinance, now a blocking GHA gate). OPTIONS_DATA_SOURCE env var removed; Alpaca CLI is the only options source. Greeks-validity filter integrated at engine.py read boundary (`src/execution/engine.py:_drop_zero_greeks`) AND inline in `src/pipeline/alpaca_options.js`. Doctor preflight expanded: AAT Plus tier check + archive freshness + vol indices freshness. New tables: migration 109 (`ticker_sentiment_daily.alpaca_news_*`), migration 110 (`data_provider_health`). Operator dashboard (:7870) gains Data Health tile. .env updates: REMOVE POLYGON_API_KEY, MASSIVE_*, OPTIONS_DATA_SOURCE, OPENCLAW_OPTIONS_BACKFILL_DAYS. ADD ALPACA_NEWS_INGEST=1, ALPACA_OPTIONS_ARCHIVE=1, ALPACA_DATA_TIER=algo_trader_plus, ALPACA_SOAK_MODE_UNTIL=2026-05-30. Spec: docs/archive/superpowers/specs/2026-05-21-sp1-provider-cutover-design.md. Plan: docs/archive/superpowers/plans/2026-05-21-sp1-provider-cutover.md. Handoff (SP-2/3/4/5): docs/archive/superpowers/specs/2026-05-21-data-provider-overhaul-handoff.md. **Task 15 + 15a shipped 2026-05-22** (commits `3507919`, `6bcbe39`, `f7095c5`, `5b1cf72`): full non-equity provider matrix wired. `fillPricesAlpaca` (equity + ETF) via `alpaca data bars --adjustment all`; share-class `BRK-B` → `BRK.B` normalized at API boundary. `fillPricesAlpacaCrypto` (BTC-USD → BTC/USD) via `alpaca data crypto bars`. `fillPricesFmpHistorical` (^GSPC / ^DJI / ^IXIC / ^RUT / ^FTSE / ^N225 / ^HSI / ^STOXX50E / ^VIX + EURUSD=X → EURUSD style) via FMP stable historical-price-eod. `runMarketPricesYFinance` renamed → `runMarketPricesNonEquity` with suffix-based dispatcher. EOD coverage 0/456 → 426/456 (+93%) after launch. market_news ingestion: alpaca_news.py now dual-writes ticker_sentiment_daily.alpaca_news_* + raw articles to market_news (migration 116 fixed a constraint drift where UNIQUE was on primary_ticker not uuid, causing 6 months of silent INSERT-rejects). 16 confirmed-delisted equities (PXD/WRK/MRO/JNPR/ANSS/HES/PARA/IPG/K/MMC/CMA/BLL/PKI/HOLX/SEE/CTRA) flipped to `active=false` in universe_config — active equity universe 390→374. **Remaining gaps (NOT in SP-1 scope)**: 7 futures (CL=F/GC=F/etc.) require FMP Premium or new provider; 5 tier-gated FMP indices (^TNX/^TYX/^FVX/^GDAXI/DX-Y.NYB) same. **Sentiment confirmer (OPENCLAW_CONFIRMER_SENTIMENT) stays OFF**: audit 2026-05-22 showed alpaca_news_* coverage today only 18.9% (86/456); the handoff still reads legacy `news_*` columns which were 0% covered because market_news was constraint-broken. Now that market_news populates, `news_*` will fill from the next sentiment cycle. Re-audit after ≥1 week of soak; flip when coverage stabilizes above 50%.
- **2026-05-19: Regime transitions trigger pipeline redeploy, not liquidation** (commits `f8ce217` → `244214b`, direct to `main`). Five phases shipped:
  - Phase 0: `pipeline_orchestrator.py` accepts `--steps <subset>` + `--reason <tag>` so any caller can run a fragment of the canonical 11-step cycle with audit attribution.
  - Phase 1: removed both auto-liquidation hooks — daily 9 AM cron no longer invokes `regime_liquidator.py`, and intraday HMM no longer calls `liquidate_on_regime_change`. Daily HMM stays as a read-only regime writer.
  - Phase 2: new `scripts/redeploy_pipeline.py` synchronously runs `signals,handoff,trade,alpaca,reconcile` with cooldown (`redeploy:cooldown:{date}` 60min) + sentinel (`redeploy:fired:{date}:{reason}` 24h) + RTH ship-safety gate. Intraday HMM detector spawns it detached on confirmed transition; cron extended `*/5 9-19 * * 1-5` for after-hours coverage. Default-OFF gate `OPENCLAW_REDEPLOY_EXTENDED_HOURS=1` unlocks the 16:00-20:00 ET window once Phase 3 ships and operator approves.
  - Phase 3: `alpaca_executor.py` session-aware (`_alpaca_session_kind() → rth|premarket|afterhours|closed`); pre/after-hours uses `--type limit --extended-hours` with marketable bid/ask offset; closed session refuses; OPG completely removed from the codebase. Brackets stay RTH-only.
  - Phase 4: `regime_liquidator.py` (now operator-only via `scripts/run_forced_liquidation.sh`) hardened: refuses outside RTH, polls each order to terminal state, writes real `result_status` ∈ {filled, partial, pending, rejected, submit_error}. May-9 sentinel guard preserved + tightened to count `outcome == 'filled'`.
  - Plan: `docs/archive/superpowers/plans/2026-05-19-regime-redeploy-not-liquidate.md`. Background: 2026-05-18 daily liquidator fired `TRANSITIONING → LOW_VOL`; 214 of 224 OPG closes expired at the open cross while DB lied about success.
- **2026-05-15: Phase 1 of fincept-imports master plan complete** (branch `feat/fincept-imports-phase1`). Seven sub-projects merged: arXiv expansion (cs.LG/AI/CL/stat.ML categories for PaperHunter), backtest oracles (kernc/backtesting.py contract reproduced clean-room), FinanceToolkit + AlphaPurify + FinBERT + PyPortfolioOpt installed, DBnomics + Polymarket spike clients (migration 094), dashboard group-by-strategy positions view, PyPortfolioOpt HRP shadow alt-sizer (migration 095/096, default-OFF gate `OPENCLAW_PYPORTFOLIOOPT_SHADOW=1`, wired into pipeline_orchestrator between `report` and `health` as non-fatal sidecar). FinBERT-Tone service live on `127.0.0.1:7872` (`finbert-sentiment.service`). Plan: `docs/archive/superpowers/plans/2026-05-15-fincept-imports-master-plan.md`.

