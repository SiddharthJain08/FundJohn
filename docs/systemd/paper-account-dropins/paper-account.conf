# Pre-live cutover pin (2026-07-21): the Saturday research/adjustment stack
# stays on the PAPER account after the main .env flips to live credentials.
# Loaded AFTER the unit's EnvironmentFile=.env, so these keys win. Today
# (.env still paper) this is a no-op.
[Service]
EnvironmentFile=/root/openclaw/.env.paper
