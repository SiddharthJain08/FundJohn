[Service]
# Honest-cost epoch daytime co-run (2026-07-27): the fleet driver may share the
# trading day with johnbot + the live cycle on this 2-core/8GB no-swap box.
# Make the backtest python ALWAYS the kernel's first OOM victim — never a live
# process (mirrors sp7-ladder.service's continuous-mode protection).
OOMScoreAdjust=1000
