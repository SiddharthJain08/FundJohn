# Drop-in: after the research finisher (saturday_brain_finisher.js) exits 0,
# trigger the root-scoped auto-commit of the research output. Event-driven and
# robust to the Sat/Sun timer swap (keys off the service, not a clock).
# See docs/runbooks/2026-07-20-auto-commit-research-output-design.md
[Unit]
OnSuccess=openclaw-research-commit.service
