# Guards (operator directive 2026-08-10): cgroup-contained OOM + retry into
# the week (max 3, RTH-avoiding). Review runs peak ~1.3G; 3G is generous.
[Unit]
OnFailure=openclaw-research-retry@%n.service

[Service]
MemoryMax=3G
MemorySwapMax=0
