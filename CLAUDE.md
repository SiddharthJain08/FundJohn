# CLAUDE.md — FundJohn / OpenClaw System

This project contains all system optimizations for FundJohn, a bot-network quantitative hedge fund built on OpenClaw using Claude Code agents.

## Core invariant — NEVER DELETE FROM THE MASTER DATABASE
The master parquets and Postgres tables under `data/master/` are append-only.
Columns may be ADDED at any time. Tickers may be ADDED at any time. Date
ranges may only grow. **No code path is allowed to drop columns, drop
tickers, truncate the date axis, deprecate columns, or delete rows from
prices.parquet, options_eod.parquet, financials.parquet, macro.parquet,
insider.parquet, earnings.parquet, prices_30m.parquet,
historical_regimes.parquet, or crypto_bars_1h.parquet.** Same rule applies to the canonical
Postgres tables (`execution_signals`, `signal_pnl`, `alpaca_submissions`,
`data_coverage`, `data_columns`). The system's job is to grow the data
forever and let strategies opt into whichever subset they need; it is
NOT to optimize storage by pruning. Any future "deprecation" must be a
flag (`active=false`) on a metadata row, never a `DELETE`.

## Recent Changes
- **2026-05-26: SP-3 — Asset-class expansion MVP (MERGED to local `main` 2026-05-26 `c2f72ca`; NOT pushed to origin, NOT deployed — gate OFF, no service restart yet)**. Adds a strategy-level `instrument_class` enum (`equity`/`option`/`etp` routed; `crypto`/`futures` reserved with no handler → raise) threaded through lifecycle + sizer + backtest + promotion guards, WITHOUT changing behavior for existing all-equity strategies. (1) `StrategyRecord.instrument_class` (default `equity`, top-level, always-emitted) threaded through `from_manifest` (both constructions) + `to_dict` (silent-strip-safe; regression test `tests/test_lifecycle_instrument_class.py`); enum validated at load (`VALID_INSTRUMENT_CLASSES`/`ROUTED_INSTRUMENT_CLASSES`). (2) Resolver `src/strategies/instrument_class.py:instrument_class_for`. (3) Sizing dispatcher `src/execution/instrument_class_sizer.py` (equity/etp literal pass-through; option scales notional by |delta| fail-open; crypto/futures raise), wired into `regime_blended_sizer_live._build_sized_payload` behind default-OFF kill-switch `OPENCLAW_INSTRUMENT_CLASS_ROUTING` — equity path byte-identical when OFF, and equity/etp short-circuit even when ON. (4) Per-class promotion thresholds `PROMOTION_THRESHOLDS` in `lifecycle.py` (equity/etp keep 0.5/0.20; option placeholder `TODO(SP-4)`); `can_transition` reads `rec.instrument_class`. (5) Class-aware backtest cost model `INSTRUMENT_COST_BPS`/`resolve_cost_model_bps` + `run_backtest(instrument_class=...)` — RECORD-ONLY log, no sim-math change, no migration. (6) Reference strategy `S_commodity_etp_momentum` (GLD/SLV/USO 90-day momentum rotator, `instrument_class=etp`, **candidate-only** — backtest Sharpe 1.08 / MaxDD 42.6% / 2455 trades over 10y on existing price history). NO schema migration; NO master-data/`universe_config` mutation (DBC dropped — zero coverage; coverage-freshness + DBC backfill are PRE-LIVE-PROMOTION operator-only). Crypto + 24/7 session logic deferred to **SP-3.1**; greeks-aware option sizing/backtest + leveraged-ETP decay are fast-follows. 22 SP-3 tests + 41 sizer + 34 lifecycle/backtest regression green; final review APPROVED. Spec/Plan: `docs/superpowers/{specs,plans}/2026-05-26-sp3-asset-class-expansion*`. **Merged to main + pushed to origin + johnbot restarted 2026-05-26 (SP-3 code now LIVE on disk, healthy). Behavior still INERT: gate `OPENCLAW_INSTRUMENT_CLASS_ROUTING` default-OFF (equity path), reference strategy candidate-only. To activate per-class sizing routing: flip the gate to `1` after soak (separate deliberate step). Reference strategy stays candidate until operator promotes (+ its pre-promotion universe_config/DBC coverage).**
- **2026-05-25: SP-2 Phase D — predicate-at-mint shipped** (branch `feat/sp2-phase-d-research-hooks`, PR #11; completes SP-2 universe-expansion A→D). PaperHunter prompt §5 infers ONE of the 12 real `CANDIDATE_PREDICATES` (or null) → `hunter_result_json.inferred_universe_filter`; StrategyCoder emits `from src.strategies.universe_default import <name> as universe_filter` at module scope when a filter is injected; the orchestrator (`research-orchestrator.js`) validates against the CANDIDATE_PREDICATES whitelist (gated, falls back to null on unknown) and threads the validated name into the coder context + queued `strategy_spec`; `lifecycle.register()` AST-detects the module-scope import and sets `universe_filter_ref="src.strategies.universe_default:<name>"` (the resolver-loadable `module:attr` form) via `save_manifest`. NO schema migration — the field rides `research_candidates.hunter_result_json` (JSONB). New system_check `papermint_predicate_coverage` (agents + strategies tag) + operator dashboard tile. Gate `OPENCLAW_PHASE_D_PREDICATE_AT_MINT` (spec: default ON; code treats absent as OFF — live VPS `.env` must add the line). Runbook: `docs/sp2-papermint-runbook.md`. Spec/Plan: `docs/superpowers/{specs,plans}/2026-05-22-sp2-phase-d-research-hooks*`.
- **2026-05-24: SP-2 Phase C — Mastermind universe-recs shipped** (branch `feat/sp2-phase-c-mastermind-universe-recs`, PR #10, stacked on Phase B which merged to main). New `--mode universe-recs` on `run_mastermind.js`; Sat 20:00 ET timer `docs/universe-recs.{service,timer}` (installs as `openclaw-universe-recs.*`); gated `OPENCLAW_UNIVERSE_RECS=1` (default OFF, `--dry-run` bypasses). Per manifest-`state='live'` strategy (~52): a `UniverseResolver`→**regime-blended** 8-metric backtest grid over the 4 cap-independent candidate predicates (`sp500/no_adr/no_otc/options_eligible_only`) via new `src/backtest/universe_grid_cli.py` (built on `MockResolver` in `universe_resolver.py` + a `resolver=` param on `unified_backtest.run_backtest` sharing a new `_per_bar_simulate` helper + new `sortino`/`calmar`/`mean_universe_size` metrics in `aggregate_metrics`). Opus 4.7 1M picks → strict JSON → `strategy_universe_recommendations` + Discord `#universe-recs` post. Operator ✅/❌/⏸ reactions (`bot.js` `messageReactionAdd`, numeric `Partials` enum — string partials are no-ops in discord.js v14) → dashboard :7870 `POST /api/universe-recs/:id/:action` → `lifecycle.adopt_universe_recommendation` (atomic manifest+DB+`lifecycle_audit_log` two-phase rename; writes `universe_filter_ref="src.strategies.universe_default:<candidate>"`, the resolver-loadable `module:attr` form). 28-day adopt-skip. Dashboard tile (last 14d) with bypass-Discord buttons. Migration **117** `lifecycle_audit_log` (116 was taken by market_news). Doctor `universe_recs_freshness` + system_check `universe_recs_health` (both gated). Known gaps: the 8 cap-gated candidates (`r1000/r3000/large_cap/...`) resolve empty for historical dates (FMP Starter market_cap 403) — marked `cap_unverified` until the shares-outstanding backfill; the `migrate()` runner has a pre-existing connection-poisoning wart on re-run (non-idempotent 008/027/069) so **verify `lifecycle_audit_log` exists post-deploy**. Spec: `docs/superpowers/specs/2026-05-22-sp2-phase-c-mastermind-universe-recs-design.md`. Plan: `docs/superpowers/plans/2026-05-22-sp2-phase-c-mastermind-universe-recs.md`.
- **2026-05-22: SP-2 Phase B — 5y backfill machinery shipped** (branch `feat/sp2-phase-b-5y-backfill`, PR #9). 14 implementation tasks: builder + frozen artifact + SP500 history CSV + migration 115 + quarantine filter + 6-consumer wiring + driver scaffolding + prices/metadata/options targets + doctor + system_checks + dashboard tiles + smoke. Quarantine filter wired into 3 sites (universe_resolver coverage, unified_backtest.load_prices_panels, quick_backtest._load_prices) + collector.js startup hook; 3 spec-named backtest engines documented as unfilterable (regime_blended reads only historical_regimes.parquet, intraday uses ts_utc, regime_performance_analyzer reads no parquets). Master parquets are SINGLE FILES not partitioned datasets — single documented append-only-invariant exception (`pyarrow.parquet write` with explicit zero-existing precondition) lives in `scripts/backfill_universe_5y.py:_promote_chunk`. Daily writer DRY-refactored to share `build_month_snapshot` helpers with the backfill builder. Phase B v1 known gaps: historical market_cap = None (FMP Starter 403; fallback unbuilt); backfill seed = 404 tickers pre-Task-7-completion; AAPL/MSFT first_seen_at = 2026-05-14. Runbook: `docs/sp2-backfill-runbook.md`. Spec: `docs/superpowers/specs/2026-05-22-sp2-phase-b-5y-backfill-design.md`. Plan: `docs/superpowers/plans/2026-05-22-sp2-phase-b-5y-backfill.md`. Migration 115 (backfill_audit). New doctor checks (backfill_progress, backfill_universe_coverage; both slow=True). New system_checks (backfill_progress on storage tag, ticker_metadata_history_depth on strategies tag). Phase C (Mastermind universe-recs) + Phase D (research hooks) follow on separate plans.
- **2026-05-22: SP-2 Phase A — Universe-slice machinery shipped** (branch `feat/sp2-phase-a-universe-machinery`, PR pending). Predicate-per-strategy universe model: each strategy declares `universe_filter(meta, as_of) -> bool` (or inherits the default `sp500`). New `UniverseResolver` service + `ticker_metadata_snapshots` table + 12 candidate predicates. Backtest engines (`unified`/`quick`/`regime_blended`/`intraday_regime`/`regime_performance_analyzer`) accept resolver for per-bar point-in-time universe. Lint enforces `(meta, as_of)` signature + bans `datetime`/`time`/`os` imports in predicate modules (CI gate `.github/workflows/lint-universe-predicates.yml`); lifecycle sandbox check rejects clock-dependent predicates at transition. All 102 existing strategies on default predicate post-deploy — byte-identical behavior. `union_universe(today, ['live'])` drives collector / options-archive / news / sentiment data-fetch envelope. Migrations 111-114. Doctor checks: `metadata_snapshot_freshness`, `union_universe_size` (slow=True). New system_checks: `universe_resolution` (pipeline tag, 15s latency gate), `metadata_snapshot_freshness` (strategies tag). Dashboards: operator `/api/universe-slice` + user `/api/pipelines/universe-inflation` with UI tiles. Default env `OPENCLAW_UNIVERSE_RESOLVER=1` (kill switch). Spec: `docs/superpowers/specs/2026-05-22-sp2-universe-expansion-design.md`. Plan: `docs/superpowers/plans/2026-05-22-sp2-phase-a-universe-machinery.md`. Phase B (5y backfill) + Phase C (Mastermind universe-recs) + Phase D (research hooks) follow on separate plans.
- **2026-05-22: Pipeline Diagnostics dashboard tab + LangGraph orchestrator LIVE**. New "Pipeline Diagnostics" tab in `src/channels/api/server.js` (port 3000 → nginx :80, right of Research) inspecting LangGraph runs via 4 routes under `/api/pipelines/*` (`summary`, `runs`, `runs/:thread_id`, `stream` SSE). Backend: `src/channels/api/routes_pipelines.js`. Merges in-memory traceBus runs with durable `langgraph.checkpoints` Postgres state. Includes gantt-style node timeline, collapsible checkpoint JSON viewer, color-coded graph pills, animated status pulse, live SSE stream with pause/clear. **OPENCLAW_LANGGRAPH_ORCHESTRATOR=1 flipped LIVE 2026-05-22 07:23 UTC** — today's 10 AM ET cron now dispatches to `src/agent/graphs/daily-cycle.js` (PostgresSaver checkpoints, traceBus → dashboard SSE) instead of legacy `pipeline_orchestrator.py`. Pre-flip dry-run validated: graph compiles, step ordering correct, subprocess invocation works, state accumulates, error path propagates cleanly. Known pre-existing dry-run-only gap: `trade_handoff_builder.py` + `regime_blended_sizer_live.py` + `daily_health_digest.js` don't accept `--dry-run` flag (live cron doesn't inject it, so non-blocking). SP-1 follow-ups also closed: data_provider_health writer (`src/maintenance/provider_health.py`) wired into alpaca_options + alpaca_news + cboe_vol_indices; open_interest gap formally accepted as not-fixable on Alpaca/FMP (master parquet was 99.95% NULL on the column already, engine.py degrades gracefully). Commits: `eb384fc` (diagnostics tab), `6511904` (provider_health + UI polish).
- **2026-05-22: SP-1 provider cutover shipped** (branch `feat/sp1-provider-cutover`, deploys Saturday 2026-05-23). Polygon + Yahoo + Massive S3 (Polygon-affiliated) fully removed from the data stack. Alpaca AAT Plus is now P1 for equity quotes, options chain, news, screener, corp-actions, and historical options EOD self-archive (new daily 16:30 ET timer `openclaw-options-archive.timer`). FMP Starter stays P2 for fundamentals + macro + insider flags + universe ref. yfinance bounded to `src/ingestion/cboe_vol_indices.py` (CI lint `scripts/lint_provider_guards.py` enforces — only file allowed to import yfinance, now a blocking GHA gate). OPTIONS_DATA_SOURCE env var removed; Alpaca CLI is the only options source. Greeks-validity filter integrated at engine.py read boundary (`src/execution/engine.py:_drop_zero_greeks`) AND inline in `src/pipeline/alpaca_options.js`. Doctor preflight expanded: AAT Plus tier check + archive freshness + vol indices freshness. New tables: migration 109 (`ticker_sentiment_daily.alpaca_news_*`), migration 110 (`data_provider_health`). Operator dashboard (:7870) gains Data Health tile. .env updates: REMOVE POLYGON_API_KEY, MASSIVE_*, OPTIONS_DATA_SOURCE, OPENCLAW_OPTIONS_BACKFILL_DAYS. ADD ALPACA_NEWS_INGEST=1, ALPACA_OPTIONS_ARCHIVE=1, ALPACA_DATA_TIER=algo_trader_plus, ALPACA_SOAK_MODE_UNTIL=2026-05-30. Spec: docs/superpowers/specs/2026-05-21-sp1-provider-cutover-design.md. Plan: docs/superpowers/plans/2026-05-21-sp1-provider-cutover.md. Handoff (SP-2/3/4/5): docs/superpowers/specs/2026-05-21-data-provider-overhaul-handoff.md. **Task 15 + 15a shipped 2026-05-22** (commits `3507919`, `6bcbe39`, `f7095c5`, `5b1cf72`): full non-equity provider matrix wired. `fillPricesAlpaca` (equity + ETF) via `alpaca data bars --adjustment all`; share-class `BRK-B` → `BRK.B` normalized at API boundary. `fillPricesAlpacaCrypto` (BTC-USD → BTC/USD) via `alpaca data crypto bars`. `fillPricesFmpHistorical` (^GSPC / ^DJI / ^IXIC / ^RUT / ^FTSE / ^N225 / ^HSI / ^STOXX50E / ^VIX + EURUSD=X → EURUSD style) via FMP stable historical-price-eod. `runMarketPricesYFinance` renamed → `runMarketPricesNonEquity` with suffix-based dispatcher. EOD coverage 0/456 → 426/456 (+93%) after launch. market_news ingestion: alpaca_news.py now dual-writes ticker_sentiment_daily.alpaca_news_* + raw articles to market_news (migration 116 fixed a constraint drift where UNIQUE was on primary_ticker not uuid, causing 6 months of silent INSERT-rejects). 16 confirmed-delisted equities (PXD/WRK/MRO/JNPR/ANSS/HES/PARA/IPG/K/MMC/CMA/BLL/PKI/HOLX/SEE/CTRA) flipped to `active=false` in universe_config — active equity universe 390→374. **Remaining gaps (NOT in SP-1 scope)**: 7 futures (CL=F/GC=F/etc.) require FMP Premium or new provider; 5 tier-gated FMP indices (^TNX/^TYX/^FVX/^GDAXI/DX-Y.NYB) same. **Sentiment confirmer (OPENCLAW_CONFIRMER_SENTIMENT) stays OFF**: audit 2026-05-22 showed alpaca_news_* coverage today only 18.9% (86/456); the handoff still reads legacy `news_*` columns which were 0% covered because market_news was constraint-broken. Now that market_news populates, `news_*` will fill from the next sentiment cycle. Re-audit after ≥1 week of soak; flip when coverage stabilizes above 50%.
- **2026-05-19: Regime transitions trigger pipeline redeploy, not liquidation** (commits `f8ce217` → `244214b`, direct to `main`). Five phases shipped:
  - Phase 0: `pipeline_orchestrator.py` accepts `--steps <subset>` + `--reason <tag>` so any caller can run a fragment of the canonical 11-step cycle with audit attribution.
  - Phase 1: removed both auto-liquidation hooks — daily 9 AM cron no longer invokes `regime_liquidator.py`, and intraday HMM no longer calls `liquidate_on_regime_change`. Daily HMM stays as a read-only regime writer.
  - Phase 2: new `scripts/redeploy_pipeline.py` synchronously runs `signals,handoff,trade,alpaca,reconcile` with cooldown (`redeploy:cooldown:{date}` 60min) + sentinel (`redeploy:fired:{date}:{reason}` 24h) + RTH ship-safety gate. Intraday HMM detector spawns it detached on confirmed transition; cron extended `*/5 9-19 * * 1-5` for after-hours coverage. Default-OFF gate `OPENCLAW_REDEPLOY_EXTENDED_HOURS=1` unlocks the 16:00-20:00 ET window once Phase 3 ships and operator approves.
  - Phase 3: `alpaca_executor.py` session-aware (`_alpaca_session_kind() → rth|premarket|afterhours|closed`); pre/after-hours uses `--type limit --extended-hours` with marketable bid/ask offset; closed session refuses; OPG completely removed from the codebase. Brackets stay RTH-only.
  - Phase 4: `regime_liquidator.py` (now operator-only via `scripts/run_forced_liquidation.sh`) hardened: refuses outside RTH, polls each order to terminal state, writes real `result_status` ∈ {filled, partial, pending, rejected, submit_error}. May-9 sentinel guard preserved + tightened to count `outcome == 'filled'`.
  - Plan: `docs/superpowers/plans/2026-05-19-regime-redeploy-not-liquidate.md`. Background: 2026-05-18 daily liquidator fired `TRANSITIONING → LOW_VOL`; 214 of 224 OPG closes expired at the open cross while DB lied about success.
- **2026-05-15: Phase 1 of fincept-imports master plan complete** (branch `feat/fincept-imports-phase1`). Seven sub-projects merged: arXiv expansion (cs.LG/AI/CL/stat.ML categories for PaperHunter), backtest oracles (kernc/backtesting.py contract reproduced clean-room), FinanceToolkit + AlphaPurify + FinBERT + PyPortfolioOpt installed, DBnomics + Polymarket spike clients (migration 094), dashboard group-by-strategy positions view, PyPortfolioOpt HRP shadow alt-sizer (migration 095/096, default-OFF gate `OPENCLAW_PYPORTFOLIOOPT_SHADOW=1`, wired into pipeline_orchestrator between `report` and `health` as non-fatal sidecar). FinBERT-Tone service live on `127.0.0.1:7872` (`finbert-sentiment.service`). Plan: `docs/superpowers/plans/2026-05-15-fincept-imports-master-plan.md`.

## System Overview
Autonomous quant PM system + hardcoded data pipeline:
- **BotJohn** (claude-opus-4-7): Orchestrator and portfolio manager (this agent — Claude Code).
- **DataPipeline** (hardcoded, src/execution/pipeline_orchestrator.py): 10-step base daily cycle (10:00 ET) — `collect → signals → ic_gate → handoff → trade → alpaca → reconcile → report → pyportfolioopt_shadow → health`. `sentiment` is inserted between `collect` and `signals` when `OPENCLAW_SENTIMENT_INGEST=1` (ON in production → 11 steps). `ic_gate` early-exits unless `OPENCLAW_IC_GATE=1` (default-OFF). `trade` = `regime_blended_sizer_live.py` (LIVE production sizer; OPENCLAW_REGIME_BLENDED_LIVE=1 since 2026-05-12). `pyportfolioopt_shadow` = shadow alt-sizer (non-fatal, gated on OPENCLAW_PYPORTFOLIOOPT_SHADOW=1, never routes orders). `health` = `daily_health_digest.js`. The orchestrator also accepts `--steps <subset> --reason <tag>` so external triggers (intraday redeploy) can run a fragment of the cycle without re-running data collection. (queue_drain removed 2026-04-28; trade_parity_capture + correlation_sidecar removed 2026-05-20; legacy single TradeJohn LLM step replaced by formula sizer + per-ticker confirmer.)
- **Regime detection + redeploy** (since 2026-05-19): regime change no longer triggers liquidation — it triggers a delta-based pipeline redeploy via `scripts/redeploy_pipeline.py` (signals→handoff→trade→alpaca→reconcile). The sizer's existing `delta = target − current_broker_position` math means positions get netted, not blown out. Daily HMM (`scripts/run_market_state.py` at 9 AM ET) writes regime_latest.json + market_regime row only — read-only. Intraday HMM (`scripts/run_intraday_market_state.py` every 5 min, 9-19 ET) spawns the redeploy on confirmed transition with hysteresis(3)+confidence(<70%)+cooldown(60min) gates. After-hours redeploys (16:00-20:00 ET) gated by `OPENCLAW_REDEPLOY_EXTENDED_HOURS=1` (default OFF); when ON, alpaca_executor submits `--type limit --extended-hours` for eligible symbols and skips the rest. Total-portfolio liquidation is operator-only via `scripts/run_forced_liquidation.sh` → `regime_liquidator.py --force` (refuses outside RTH; poll-to-terminal audit).
- **TradeJohn confirmer** (claude-sonnet-4-6): Per-ticker approve/veto/scale confirmer inside `regime_blended_sizer_live.py`. Runs only in LOW_VOL/TRANSITIONING regimes. Fail-open on LLM error. Invoked via `claude-bin --print --output-format json --model sonnet --max-budget-usd 0.50`.
- **PaperHunter** (claude-sonnet-4-6, upgraded from Haiku 2026-04-23): Per-paper extraction + 4 rejection gates.
- **StrategyCoder** (claude-sonnet-4-6): On-demand strategy implementation.
- **MastermindJohn** (claude-opus-4-7, 1M ctx): Opus orchestrator with four weekly modes.
  - `mode=corpus` (Sat 10:00 ET via `openclaw-mastermind-corpus.service`/`.timer`) — rates the full `research_corpus` in batched calls and promotes high-confidence picks to `research_candidates`.
  - `mode=comprehensive-review` (Sat 18:00 ET via `openclaw-strategy-review.service`/`.timer`) — deep per-strategy lifetime review: every closed trade, counterfactual tuning of size / stop / target / max-hold for greater profitability. Writes to `strategy_memos`; posts each memo to `#strategy-memos`.
  - `mode=position-recs` (Sat 19:00 ET via `openclaw-position-recs.service`/`.timer`) — reads the latest `strategy_memos`, distils them into exact per-strategy sizing + bracket deltas, writes to `strategy_sizing_recommendations`, posts consolidated table to `#position-recommendations`. Feeds TradeJohn's Monday handoff via `trade_handoff_builder.py`.
  - `mode=paper-expansion` (Sun 08:00 ET via `openclaw-paper-expansion.service`/`.timer`) — Opus + WebSearch/WebFetch steers an open-ended source-discovery sweep beyond arXiv/OpenAlex (journals, working-paper series, blogs, conference proceedings), scrapes, dedupes, imports into `research_corpus`. Logs to `paper_source_expansions`.
  - Was `CorpusCurator` prior to 2026-04-22 Phase 3; legacy `corpus-curator` subagent type still resolves to the same prompt for backward compat. Legacy `strategy-stack` mode + `mastermind_weekly_reports` table were deleted 2026-04-24 — they were pipeline-building scaffolding, not production features.

## Context Retention
Retain all context and memory of:
- File locations on the VPS (/root/openclaw/)
- Current strategy lifecycle states (from manifest.json)
- Agent architecture and responsibilities
- Changes made and bottlenecks fixed

At every step maintain a complete understanding of:
- Which strategies are live/paper/deprecated
- What data collections are active
- Current portfolio state
- Any pending lifecycle transitions

## LangGraph Orchestration (added 2026-04-22)
The cycle and paper-hunt flows run through LangGraph.js:
- `src/agent/graph.js` — daily cycle StateGraph (datajohn → tradejohn → HITL → botjohn); PostgresSaver checkpointer in `langgraph` schema; `interruptBefore: ['botjohn']` for operator approval; conditional edge skips botjohn if tradejohn produced zero signals. (ResearchJohn retired 2026-05-02 — mastermind handles research via saturday_brain.js + comprehensive_review.js, not this graph.)
- `src/agent/graphs/paperhunter.js` — Send-based parallel fan-out for paper extraction.
- `src/agent/graphs/index.js` — graph registry. Add new flows here.
- `src/agent/traceBus.js` — in-memory event ring buffer fanning out to dashboard SSE.
- `bin/run-graph.js` — CLI runner: `node bin/run-graph.js list | cycle '<json>' | cycle:resume '<json>' | cycle:state <threadId>`.
- Dashboard: `src/channels/dashboard/server.js` on 127.0.0.1:7870 (systemd: `fundjohn-dashboard.service`). SSH-tunnel to view. Surfaces bots, subagents, analyses, verdicts, trades, checkpoints, workspaces, graph runs + live traces, HITL approve/veto buttons.
- Smoke tests: `node test/graph-smoke.js` (cycle HITL + veto), `node test/paperhunter-smoke.js` (fan-out parallelism).
- Set `LANGSMITH_API_KEY` in `.env` to auto-enable LangSmith tracing (project=`fundjohn`).

## Key Paths (VPS: /root/openclaw/)
- `src/strategies/lifecycle.py` — strategy state machine
- `src/strategies/manifest.json` — strategy registry
- `src/strategies/implementations/` — strategy Python files
- `src/agent/main.js` — agent orchestrator entry point
- `src/agent/prompts/subagents/` — agent prompt files
- `src/agent/curators/mastermind.js` — MastermindJohn corpus-mode orchestrator
- `src/agent/curators/comprehensive_review.js` — Saturday per-strategy memos
- `src/agent/curators/position_recommender.js` — Saturday sizing recs from memos
- `src/agent/curators/paper_expansion_ingestor.js` — Sunday Opus-steered paper hunt
- `src/agent/curators/run_mastermind.js` — CLI entry (`--mode {corpus|comprehensive-review|position-recs|paper-expansion}`)
- `src/agent/research/gate-decisions.js` — structured `paper_gate_decisions` emitter
- `src/ingestion/arxiv_discovery.py` — broad arXiv q-fin harvest into `research_corpus`
- `src/ingestion/openalex_discovery.py` — SSRN/NBER/JFE/RFS/JF/JFQA/QF + author watchlist + citation graph
- `src/database/migrations/032..038_*.sql` — corpus + calibration + ROI schema
- `docs/mastermind-corpus.{service,timer}` — Saturday 10:00 ET corpus rater (installed at `/etc/systemd/system/openclaw-mastermind-corpus.*`)
- `docs/strategy-review.{service,timer}` — Saturday 18:00 ET comprehensive review
- `docs/position-recs.{service,timer}` — Saturday 19:00 ET sizing recs
- `docs/paper-expansion.{service,timer}` — Sunday 08:00 ET paper expansion ingestion
- `src/system_checks/` — runnable diagnostic probes for live state. Tagged by domain (`pipeline`, `broker`, `regime`, `strategies`, `agents`, `storage`). `python3 -m system_checks [--tag X] [--check NAME] [--json]`. See `src/system_checks/README.md` for the contract. Distinct from `tests/` (pytest unit tests) and `src/maintenance/doctor.py` (sub-second preflight). When fixing a bug class that shouldn't recur, add a regression check here.
- `src/agent/run_maintenance.js` — Daily 12:00 ET (Mon-Fri) BotJohn maintenance driver: doctor + system_checks + digest + investigate + fix + post to #general
- `docs/botjohn-maintenance.{service,timer}` — Mon-Fri 12:00 ET maintenance run (installed at `/etc/systemd/system/openclaw-botjohn-maintenance.*`)
- `docs/botjohn-saturday-maintenance.{service,timer}` — Sat 16:00 ET research-pipeline audit + surgical recovery (installed at `/etc/systemd/system/openclaw-botjohn-saturday-maintenance.*`)
- `docs/botjohn-saturday-verify.{service,timer}` — Sun 12:00 ET read-only verification of any Saturday recovery (installed at `/etc/systemd/system/openclaw-botjohn-saturday-verify.*`)
- `src/agent/run_maintenance.js` — single wrapper for all three maintenance modes; dispatches on `--mode {daily,saturday,saturday-verify}` (default `daily`)
- `agents/` — agent identity and soul files
- `data/` — master parquet datasets
- `johnbot/` — Discord bot
- `.env` — secrets and config

## Deployment Workflow
Changes flow: local edit → git commit + push → VPS `git pull`
For large files: python3 base64 decode command → paste on VPS → git add/commit/push
